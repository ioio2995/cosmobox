# Contrat de continuité — lot actif (anti-compactage)

Ce document est un contrat de reprise, pas une documentation scientifique. Il doit être relu intégralement (avec les documents qu'il référence) avant toute action de code, commit ou push si le contexte conversationnel a été compacté ou paraît incomplet.

## Lot outillage/performance CI-tests (parallèle, indépendant de la chaîne 1B)

Diagnostic de performance du workflow de test, aucune logique scientifique modifiée.

**Sous-lot 1 — CI/diagnostic, accepté au commit `8c7f92e41f375390e73c983fb39065dcf08b2b79`** : `on.push.branches` retiré de `research/**` — désormais `push`/`pull_request` limités à `main` uniquement, plus `workflow_dispatch: {}` pour un lancement manuel de la suite complète à tout moment ; `actions/setup-python` gagne `cache: "pip"`. Baseline mesurée : suite complète (1441 tests) = **293.56s** (real 4m56s), ~77 % du temps concentré dans `tests/scripts/level1b_campaign/test_runner.py`. Diagnostic : le vrai goulot n'était pas la diagonalisation/BLAS mais `serialization._validator()` non caché (reconstruit et re-valide le méta-schéma à chaque `validate_document`).

**Sous-lot 2 — PERF-VALIDATOR-CACHE, accepté** : `@lru_cache(maxsize=1)` ajouté à `serialization._validator()` (une ligne, même motif que `_load_schema()` juste au-dessus), confirmé sans effet de bord (`Draft202012Validator.iter_errors(instance)` ne stocke aucun état par document entre appels, vérifié dans le code de `jsonschema==4.26.0`). Résultat mesuré :

```text
run_single_case (triangle S1, profilé isolément) : ~123s → 0,54s (≈228×)
test_runner.py (57 tests)                        : ~231-293s → 12,81s (real 16,6s)  (≈14-18×)
suite complète (1449 tests, +8 tests de cache)    : 293,56s → 40,99s (real 44,69s)   (≈7,2×)
```

Nouveau profil de coût (`--durations=30` après correction) : plus aucun test dominant — le plus lent est `tests/test_symmetry_campaign.py::test_run_campaign_full_grid_all_succeed_and_no_truncated_group_failure` à 4,54s ; tout le reste est sous ~2s.

**xdist re-benchmarké sur le nouveau profil (suite complète, non capée)** : `-n2` = 170,48s (**4,2× plus lent** que série), `-n4` = 182,24s (encore pire). Arrêt des benchmarks à ce point (résultat sans ambiguïté). Explication : le coût dominant est désormais réparti sur des centaines de tests courts (≈28ms/test en moyenne) — la coordination inter-processus de xdist (IPC par test, démarrage des workers) domine largement tout gain de parallélisme possible sur un tel profil. **Recommandation finale : série, pas de xdist.** `pytest-xdist` reste installé dans le venv local pour le benchmark uniquement, jamais ajouté aux dépendances du projet.

**Stratégie FAST/SUBSYSTEM/FULL révisée** : FAST = fichier(s) directement concerné(s) (quasiment tout le dépôt tient maintenant sous quelques secondes) ; SUBSYSTEM = suite du sous-système + dépendances proches ; FULL = suite complète — **~41-45s en série, redevenue lançable localement à chaque itération**, plus de raison de la réserver à un jalon ou à la CI seule.

**État local volontaire de `.gitignore`** : modification de Lionel (ajout de `results/`), non liée à ce lot, non stagée, non commitée, laissée telle quelle.

**1B-9b** : accepté définitivement depuis, voir « Dernier commit accepté » et « Lot 1B-9b » ci-dessous.

## Dernier commit accepté

```text
b9b0a21262e26a945b974cfbdcdb1a1ffbc3867c
```

Mis à jour par `1C-8j` : `1C-8h` + `1C-8h-fix` sont désormais `ACCEPTÉS DÉFINITIVEMENT ENSEMBLE`. `1C-8i` (audit read-only du launcher normatif global) a produit un rapport d'audit accepté, sans aucun commit — le pointeur n'a donc pas bougé entre-temps. Voir « Lot 1C-8j » ci-dessous pour l'implémentation du launcher normatif lui-même (ce commit), toujours `pending`, non avancé au-delà de `b9b0a212...`.

Corrigé par `1C-8e-fix` : ce pointeur était resté stale à `79c0b8b8a5f2208acb6c4b8776ef6323ad8bbd98` (précédant `1C-8d`) alors que `1C-8d` + `1C-8d-fix` sont `ACCEPTÉS DÉFINITIVEMENT ENSEMBLE` depuis l'audit de 1C-8e. `a7de0a4163a1f1ceeeab22a0a090a8c29b98c4bc` (1C-8e) n'est PAS encore accepté définitivement — voir « Lot 1C-8e » et « Lot 1C-8e-fix » ci-dessous : le code scientifique est acceptable mais l'audit reste suspendu pour `REFLECTION_VALIDITY_EQUIVALENCE_WORDING` + `STALE_CURRENT_ACCEPTED_HEAD`, corrigés par `1C-8e-fix`, lui-même `pending ChatGPT audit`.

**Level 1B : CLOS.** 1B-9g (clôture scientifique documentaire) accepté définitivement. Voir « Lot 1B-9g » ci-dessous et `docs/levels/level1/level1b-conclusion.md`. **Le manifeste Level 1B (`experiments/level1/preregistered-manifest-v1.json`) reste inchangé.**

**Level 1C : lots 1C-0, 1C-1, 1C-3a, 1C-3b, 1C-4a, 1C-4b, 1C-4c, 1C-5a, 1C-5b, 1C-6a, 1C-6b, 1C-6d, 1C-6e, 1C-6f, 1C-6g, 1C-6h, 1C-6i, 1C-6j, 1C-6k acceptés définitivement** (commits respectifs cités dans leurs sections dédiées ci-dessous ; 1C-4b, 1C-5a, 1C-6a, 1C-6b, 1C-6d, 1C-6f, 1C-6i sont des audits de lecture seule sans commit propre ; 1C-6k est une exécution unique réelle, sans commit propre, artefact non versionné). **1C-2a/1C-2b/1C-2c** : voir la section dédiée ci-dessous — statut corrigé (1C-2b/1C-2c seuls acceptés définitivement en tant que jalons ; 1C-2a suspendu puis subsumé, jamais accepté séparément). **Housekeeping `.gitignore`** (`chore: ignore generated results`, commit `a2dd016a4dbb035782340667b15fc35c6aa19e46`) : intègre l'ignorance de `results/` comme politique permanente du dépôt, n'est plus une exception locale. **1C-6l** (gel documentaire du résultat de préflight 1C-6k accepté, traçabilité, et readiness de campagne) clôture documentairement 1C-6h/1C-6i/1C-6j/1C-6k — voir les sections dédiées ci-dessous. **`GRID_PREFLIGHT_PASSED = YES`, `J0_CAMPAIGN_READY = YES`** (depuis 1C-6l — voir `docs/levels/level1c/identifiability-preregistration.md` §19). **1C-7a/1C-7b/1C-7b2/1C-7b3** (audits de conception en lecture seule sur le contrat de production normative J0×S, le tracking inter-J0 et la calibration de non-régression, aucun commit propre chacun) acceptés — voir sections dédiées ci-dessous. **1C-7c** (gel documentaire du contrat de production normative, du tracking inter-J0 v1 et de la méthodologie de calibration de non-régression, commit `484d0100e71e3e8478091569afbc533c640332a0`) + **1C-7c-fix** (correction documentaire minimale de la justification de `FLOAT64_ZERO_ERROR_REFERENCE` en §20.22, commit `1429613cbb4a4bea5c4586df389bc5547e88f2db`) **acceptés définitivement ensemble** — voir `docs/levels/level1c/identifiability-preregistration.md` §20 et les sections dédiées ci-dessous. État conceptuel gelé : `INTER_J0_TRACKING_CONTRACT_READY = YES`, `NON_REGRESSION_CALIBRATION_CONTRACT_READY = YES`, `NON_REGRESSION_CTT_ABS_TOL = PENDING_CALIBRATION`, `NON_REGRESSION_RHO_ABS_TOL = PENDING_CALIBRATION`, `NON_REGRESSION_TOLERANCE_STATUS = NEEDS_CALIBRATION`, `NORMATIVE_PRODUCTION_CONTRACT_READY = CONDITIONAL`, `NORMATIVE_CAMPAIGN_IMPLEMENTATION_READY = NO`. **1C-7c2** (implémentation initiale de l'outil de calibration `T_max`, commit `e98950fc92f2e55ebc4aca3e0bbd9906fe6f17de`) — **NON ACCEPTÉE** : audit ChatGPT a identifié trois blocages (`CEIL_DECADE_POLICY` pouvant produire `TOL<E`, `SUCCESS` possible sans `rho_QQ` numérique, absence de provenance historique prouvant l'identité de `T_max`). **1C-7c2-fix** (audit read-only des deux premiers défauts + audit exhaustif de provenance historique `T_max`) a établi `HISTORICAL_TMAX_PROVENANCE_READY=NO` : la campagne Level1B n'a jamais persisté `target_id → spectral_window_group_index` (D022), rendant le hold-out `T_max` inexploitable sans nouvelle diagonalisation ni assertion opérateur libre (les deux rejetées) — **STOP sans commit**, rapport read-only accepté comme audit de blocage. **1C-7c2a** (redesign read-only) a établi conceptuellement un hold-out `TARGET_ID_FREE_PERSISTED_GROUP_HOLDOUT` (groupes persistés filtrés structurellement, jamais par sémantique de target) — accepté conceptuellement, sans gel documentaire. **1C-7c2a2** (audit read-only) a vérifié réellement l'existence/validité/utilisabilité des cinq artefacts historiques proposés (`triangle S=1`, `ring4 S=1/S2/S3`, `ring5 S=1`, tous `hamiltonian_case_id=reference`) sous `/workspaces/level1b_campaign_output/` (exécution normative 1B-8g déjà documentée, `repository_commit=0ff65ac66b4aa054f739b350cd384c26ecd19752`) — `HOLDOUT_ARTIFACT_SET_READY=YES`, accepté read-only. **1C-7c2b** (amendement documentaire remplaçant le hold-out `T_max` par le hold-out `TARGET_ID_FREE_PERSISTED_GROUP_HOLDOUT` gelé, ce commit) — voir `docs/levels/level1c/identifiability-preregistration.md` §20.22/§20.30 et la section dédiée ci-dessous. `e98950f...` reste non accepté, sera corrigé/subsumé par un futur `1C-7c2c`.

Historique récent accepté, dans l'ordre (aucun réécrit, aucun rebase/cherry-pick) :

- `0ff65ac66b4aa054f739b350cd384c26ecd19752` — 1B-8g, première exécution normative Level1B. Verdict validé : `CAMPAGNE NORMATIVE 1B — EXÉCUTION VALIDÉE` (11/11 cas `success`, 11/11 `validate_existing_case_run(...).is_valid == True`, 8 286 `ResultRecord` v2). Les 11 runs mono-cas sous `/workspaces/level1b_campaign_output/runs/` sont **immuables** : aucun recalcul mono-cas, aucune réécriture.
- `3e4b393c8fa5dc4a2cb06353502b80593b060376` — livraison initiale du sous-lot 1B-9b (chargement, provenance, reconstruction de `SpectralGroupMatchKey`).
- `efb917f6eb6f3a12101526ba93a734044c097741` — premier correctif d'immutabilité de 1B-9b (gel profond `_freeze`/`_freeze_document`, accepté sur le fond, avec un défaut résiduel borné corrigé ci-dessous).
- `8c7f92e41f375390e73c983fb39065dcf08b2b79` — lot CI/performance, **accepté**, indépendant de la chaîne 1B (voir section dédiée ci-dessus).
- `f67642b542be964febcffee31b51f5678f2d2a77` — PERF-VALIDATOR-CACHE, **accepté**, indépendant de la chaîne 1B (voir section dédiée ci-dessus).
- `540b08096b7d67278a6f2eea8ce93dbf039887fe` — second correctif d'immutabilité de 1B-9b (`_is_deeply_frozen`, validation récursive de la forme gelée), **accepté**. **1B-9b est désormais accepté définitivement dans son ensemble** (livraison initiale + les deux correctifs).
- `89fbfc27dc57469bcb9cbf818b9b6a4f405630ad` — livraison candidate 1B-9c (matching inter-S), conforme sur le matching lui-même mais acceptation initialement suspendue : défaut principal (`low_window_truncated` incorrect sur le chemin sparse) et durcissement secondaire (sélection des deux plus grands spins non garantie distincte).
- `e4cc72598f0b334a41220d1873f539cd0210c8f6` — correctif 1B-9c (les deux points ci-dessus). **1B-9c est désormais accepté définitivement dans son ensemble** (`89fbfc27...` + `e4cc725...`). Résultat structurel accepté sur la campagne normative : 3 couples inter-S (`triangle`/`ring4`/`ring5` reference S3→S2), 7 groupes high analysés, 7 `exact_label_match`.
- `7ef4669b92f1986a507313c5ef5a62e6dddfe839` — livraison candidate 1B-9d (extraction/comparaison des observables inter-S), conforme sur l'extraction/comparaison elle-même mais acceptation initialement suspendue pour deux défauts de contrat strictement bornés.
- `994efe0ef19a18023a19697516f9ca66311adf65` — correctif 1B-9d (exigence `normalization == "raw_G"` pour `flavor_singular_value_ratio` ; durcissement des constructeurs publics). **1B-9d est désormais accepté définitivement dans son ensemble** (`7ef4669...` + `994efe0...`). Résultat accepté sur la campagne normative : 712 comparaisons inter-S — `O_ij_raw` 416 (252 `gamma_O` null), `rho_QQ` 96 (28 null high, 28 null low), `C_TT_conn` 96 (0 null), `flavor_singular_value_ratio` 104.
- `77899f123bae83d687e38053870de0b7202bb057` — 1B-9e (application des verdicts de robustesse existants), **accepté définitivement** (mandat 1B-9f : « Les lots suivants sont considérés comme acceptés... 1B-9e »). Résultat accepté sur la campagne normative : 684 évaluations (`gamma_O` 416, `rho_QQ` 90, `C_TT_conn` 96, `flavor_singular_value_ratio` 82), 28 non évaluables (source null), 684 `robust`/0 `non_robust`/0 `indeterminate`, 252 `gamma_O` null tous verdict `robust`.
- `8a14a040f569d4eb85de9572b7d23bb587737549` — livraison candidate 1B-9f (synthèse déterministe des résultats inter-S), conforme sur la synthèse statistique elle-même mais acceptation initialement suspendue : les groupes/couples étaient découverts depuis `comparison_report.comparisons` au lieu de `matching_report.matches`, faisant disparaître silencieusement de `groups`/`couples` tout `exact_label_match` sans comparaison 1B-9d.
- `a6450e994b7b1ce5c8d7d1fec876128a2a9622e9` — correctif structurel 1B-9f (source `groups`/`couples` depuis `matching_report.matches`, plus l'invariant `len(groups) == exact_match_count`). **1B-9f est désormais accepté définitivement dans son ensemble** (`8a14a040...` + `a6450e99...`). Résultat accepté sur la campagne normative : 7 groupes appariés (7 `exact_label_match`), 712 comparaisons source → 684 évaluées / 28 non évaluables, 684 `robust`/0 `non_robust`/0 `indeterminate` (global, par couple, par groupe, par observable). Voir « Lot 1B-9f » ci-dessous.
- `60ab94f89237a6489b89d2af08b459fc2cb1db23` — livraison candidate 1B-9g (clôture scientifique documentaire de Level 1B, `docs/levels/level1/level1b-conclusion.md`), conforme scientifiquement sur le fond mais acceptation initialement suspendue pour deux corrections terminologiques/de portée bornées (confusion « troncature spectrale `S` » / portée `j_break`).
- `8f12a2f077426df7562b1a9b917f4c1e9bc3b0b9` — correctif terminologique 1B-9g (les deux corrections ci-dessus, aucune valeur ni conclusion scientifique modifiée). **1B-9g est désormais accepté définitivement dans son ensemble** (`60ab94f...` + `8f12a2f...`). **Level 1B est CLOS.** Voir « Lot 1B-9g » ci-dessous.

**Le pointeur ci-dessus (`8f12a2f077426df7562b1a9b917f4c1e9bc3b0b9`) reste le dernier commit formellement accepté.**

1B-9a — conception de la couche d'analyse inter-S sur artefacts normatifs — **conception acceptée, y compris son addendum**, aucun code livré par ce lot (design uniquement). Décisions gelées pour l'implémentation, reprises et mises en œuvre par 1B-9c/1B-9d/1B-9e/1B-9f ci-dessous :

- exactement 3 couples inter-S potentiels dans cette campagne : `triangle` reference S3→S2, `ring4` reference S3→S2, `ring5` reference S3→S2 (les cas `j_break` de `triangle`/`ring5`, à un seul point S, n'ont aucune comparaison admissible) ;
- unité scientifique inter-S = **groupe spectral sérialisé** (déduplicé par `spectral_window_group_index`), jamais `target_id` — jamais reconstruit ni inventé ;
- `spectral_window_group_index`/`representative_energy` : identité intra-cas uniquement, jamais dans un `SpectralGroupMatchKey`, jamais critère de matching inter-S (D022) ;
- `twice_T is None` : bloqué dès la construction de `CampaignArtifactIndex` (1B-9b) — 1B-9c peut considérer cet invariant garanti, aucun nouveau statut de matching pour ce cas ;
- aucun changement de `matching.py`/`robustness.py` dans aucun lot 1B-9 tant que non explicitement autorisé ;
- `flavor_singular_value_ratio` est path-dependent (chemin minimal complet requis, pas seulement `(i,j)`) ;
- `flavor_singlet` n'est pas éligible à un verdict de robustesse (liste fermée `ROBUSTNESS_OBSERVABLE_KINDS`).

## Lot 1B-9b (accepté définitivement au commit `540b08096b7d67278a6f2eea8ce93dbf039887fe`)

Chargement validé et index immuable des artefacts mono-cas. Aucun matching inter-S, aucun `gamma_O`, aucun verdict de robustesse. Charge le manifeste, reconstruit `build_campaign_plan(manifest)`, valide les runs existants (`validate_existing_case_run`, inchangée), charge leurs documents v2 (`load_case_records`, inchangée), vérifie leur provenance croisée, regroupe les records par groupe spectral intra-cas (`spectral_window_group_index`), reconstruit exactement un `SpectralGroupMatchKey` par groupe, et fournit un index immuable (`CampaignArtifactIndex`).

**Historique de ce sous-lot** : livraison initiale `3e4b393c8fa5dc4a2cb06353502b80593b060376` → premier correctif d'immutabilité `efb917f6eb6f3a12101526ba93a734044c097741` (gel profond `_freeze`/`_freeze_document` accepté sur le fond, mais `LoadedCase`/`IndexedSpectralGroup` ne vérifiaient que le type du document RACINE, `types.MappingProxyType`, sans prouver que son contenu imbriqué était lui-même gelé) → second correctif de validation récursive `540b08096b7d67278a6f2eea8ce93dbf039887fe` : `loader._is_deeply_frozen(value)` ajoutée comme primitive récursive unique, partagée par `loader.LoadedCase` et `indexing.IndexedSpectralGroup` (import direct, aucune duplication de logique) — accepte uniquement, récursivement : `types.MappingProxyType` (toutes les clés `str`, toutes les valeurs elles-mêmes conformes), `tuple` (tous les éléments conformes), ou un scalaire JSON (`str`/`int`/`float`/`bool`/`None`). Un document forgé partiellement gelé est **rejeté à la construction**, jamais re-gelé silencieusement.

Fichiers de ce lot : `scripts/level1b_analysis/loader.py` (`LoadedCase`, `CampaignLoadError`, `FrozenDocument`, `_is_deeply_frozen`, `load_validated_cases`), `scripts/level1b_analysis/indexing.py` (`IndexedSpectralGroup`, `CampaignArtifactIndex`, `SpectralGroupIndexError`, `UnresolvedFlavorLabelError`, `build_campaign_artifact_index`), `tests/scripts/level1b_analysis/test_loader_and_indexing.py`.

## Lot 1B-9c (accepté définitivement, `89fbfc27dc57469bcb9cbf818b9b6a4f405630ad` + `e4cc72598f0b334a41220d1873f539cd0210c8f6`)

Matching inter-S sur index normatif. Prend le `CampaignArtifactIndex` accepté (1B-9b) et produit uniquement les appariements spectraux inter-S admissibles via la primitive scientifique existante `match_spectral_group` (`src/cosmobox/level1/matching.py`, inchangée). Aucun `gamma_O`, aucun verdict de robustesse, aucune comparaison `rho_QQ`/`C_TT_conn`/`flavor_singular_value_ratio` dans ce lot.

**Construction des couples** : deux `CampaignCaseSpec` forment un couple admissible ssi même `geometry`, même `hamiltonian_identity_without_spin` (réutilise `indexing._case_hamiltonian_identity_tuple`, jamais dupliqué), même `sector_id`. Les cas sont regroupés par leur propre `spin` ; seules les deux plus grandes valeurs de `spin` DISTINCTES sont retenues comme `(S_high, S_low)`. Une identité avec moins de deux spins distincts ne produit aucun couple (pas d'erreur). Sépare naturellement `reference`/`j_break` sans filtrage par nom. Si plusieurs `CampaignCaseSpec` partagent la même identité physique ET le même spin retenu, `_build_case_pairs` lève `InterSMatchingError` plutôt que de choisir arbitrairement (jamais observé dans la campagne actuelle).

**`structurally_applicable`** : toujours `True` — la construction du couple garantit déjà même geometry/hamiltonian identity/sector.

**`low_window_truncated`** : reconstruit depuis `CampaignCaseSpec`/`SpectrumOptions` du cas low en reproduisant uniquement la logique de COMPTAGE déjà figée par `cosmobox.level0.reports._compute_spectrum` (`_reconstruct_computed_eigenvalue_count`), jamais une diagonalisation : `dimension == 0` et le cas non calculable selon les guardrails lèvent `InterSMatchingError` ; `dimension == 1` → toujours 1 eigenvalue ; dense → `min(spectral_window, dimension)` ; sparse → `min(spectral_window, dimension - 1)` (donc toujours tronqué dès `dimension >= 2`, quel que soit `spectral_window` — correction du premier candidat, qui utilisait à tort `spectral_window < physical_dimension` sans distinguer dense/sparse).

**Résolution `MatchOutcome.matched_group` → `IndexedSpectralGroup` low concret** : exige exactement un groupe low dont `match_key == outcome.matched_group` ; 0 ou >1 → `InterSMatchingError`.

Fichiers de ce lot : `scripts/level1b_analysis/inter_s.py` (`InterSGroupMatch`, `InterSMatchingReport`, `InterSMatchingError`, `build_inter_s_matching_report`), `tests/scripts/level1b_analysis/test_inter_s.py`.

**Résultat structurel accepté sur la campagne normative** : 3 couples inter-S (`triangle`/`ring4`/`ring5` reference S3→S2), 7 groupes high analysés, 7 `exact_label_match` (2/2/3 groupes respectivement).

## Lot 1B-9d (accepté définitivement, `7ef4669b92f1986a507313c5ef5a62e6dddfe839` + `994efe0ef19a18023a19697516f9ca66311adf65`)

Extraction et comparaison des observables inter-S. Sélectionne les documents high/low réellement comparables pour la liste FERMÉE `O_ij_raw`/`rho_QQ`/`C_TT_conn`/`flavor_singular_value_ratio`, calcule `gamma_O` (uniquement pour `O_ij_raw`, via `cosmobox.level1.robustness.compute_gamma_o` — inchangée, jamais recodée), expose les paires numériques brutes des trois autres. Aucun verdict de robustesse dans ce lot.

**Désambiguïsation `record_kind` pour `O_ij_raw`** : `observable_kind == "O_ij_raw"` est produit sous TROIS `record_kind` différents partageant le même `identity.path`/`flavor_component` (`raw_observable`, `orbit_statistic`, `restricted_diagnostic`) — `record_kind` est toujours fixé en plus de `observable_kind` (dérivé des tables closes de `results.py`) avant toute recherche de correspondance. `C_TT_conn` est un `record_kind="raw_observable"` à payload `float` nu, jamais `NormalizedMoment` (écart constaté au mandat initial, corrigé sans blocage).

**Comparabilité (1B-9a, gelée)** : `identity.path` (tuple complet), `identity.flavor_component`, `identity.normalization` — tous exactement égaux, dans le contexte du groupe spectral déjà apparié. Pour `flavor_singular_value_ratio` spécifiquement, `identity.normalization` doit en outre valoir exactement `"raw_G"` des DEUX côtés (pas seulement leur égalité mutuelle) — un artefact `None`/`None` ou toute autre chaîne identique invalide est rejeté (`InterSObservableComparisonError`).

**Cardinalité** : document low dupliqué à la même clé → erreur ; document high éligible sans exactement un low correspondant → erreur ; document low sans high correspondant → jamais visité, jamais une erreur.

**API** : `GammaOComparison` (`O_ij_raw` : `high_value`/`low_value` complexes toujours présents et finis, `gamma_o: NormalizedMoment` jamais `None`) et `ScalarObservableComparison` (les 3 autres : `high_value`/`low_value: float | None` finis si non-null, `high_null_reason`/`low_null_reason`, aucun champ `gamma_o`) — constructeurs publics durcis : `InterSObservableComparisonReport.comparisons` doit être réellement un `tuple` d'éléments du bon type ; `ScalarObservableComparison` rejette `str`/`bool`/`NaN`/`±Inf` pour une valeur non-null et exige, pour `C_TT_conn` spécifiquement, `high_value`/`low_value` toujours non-`None` et `null_reason` toujours `None`.

**Ordre** : celui de `InterSMatchingReport.matches`, puis l'ordre canonique exact de `high_group.documents` (assembly.py) au sein d'un couple.

Fichiers de ce lot : `scripts/level1b_analysis/comparisons.py` (`GammaOComparison`, `ScalarObservableComparison`, `InterSObservableComparisonReport`, `InterSObservableComparisonError`, `build_inter_s_observable_comparison_report`), `tests/scripts/level1b_analysis/test_comparisons.py`.

**Résultat accepté sur la campagne normative** : 712 comparaisons — `O_ij_raw` 416 (252 `gamma_O` null par floor), `rho_QQ` 96 (28 null high, 28 null low), `C_TT_conn` 96 (0 null), `flavor_singular_value_ratio` 104.

## Lot 1B-9e (accepté définitivement, `77899f123bae83d687e38053870de0b7202bb057`)

Application des verdicts de robustesse aux comparaisons inter-S. Associe chaque comparaison à son `InterSGroupMatch` source exact, puis appelle STRICTEMENT `cosmobox.level1.robustness.evaluate_robustness(...)` pour chaque comparaison disposant de deux valeurs numériques, en conservant intégralement le `RobustnessResult` produit. Aucune formule, aucun seuil, aucun `null_reason` de robustesse recodé.

**Audit préalable des nulls réels** : les trois observables scalaires ne présentent jamais de null asymétrique — uniquement « les deux numériques » ou « les deux null » : `rho_QQ` 90/6, `flavor_singular_value_ratio` 82/22, `C_TT_conn` 96/0. Aucune décision scientifique supplémentaire nécessaire.

**`GammaOComparison`** : `evaluate_robustness` appelé systématiquement (`high_value`/`low_value` toujours présents). `comparison.gamma_o.value is None` (floor) n'empêche JAMAIS un verdict définitif — `gamma_o` null et verdict `robust`/`non_robust` sont deux faits indépendants du même appel, jamais fusionnés en `indeterminate`. Vérification de cohérence : `result.gamma_o == comparison.gamma_o`.

**Comparaisons scalaires** : `evaluate_robustness` appelé seulement si `high_value is not None and low_value is not None`. Sinon : aucun appel, aucun `RobustnessResult` fabriqué, aucune nouvelle `null_reason` scientifique inventée — conservée dans `unevaluable_comparisons`.

**Association comparaison → `InterSGroupMatch`** : par identité d'objet (`id()`), jamais par égalité de dataclass (`IndexedSpectralGroup`/`CampaignCaseSpec` portent des `numpy.ndarray` non hashables). 0 ou >1 match trouvé → `InterSRobustnessError`.

Fichiers de ce lot : `scripts/level1b_analysis/robustness_evaluation.py` (`InterSRobustnessEvaluation`, `InterSRobustnessReport`, `InterSRobustnessError`, `build_inter_s_robustness_report`), `tests/scripts/level1b_analysis/test_robustness_evaluation.py`.

**Résultat accepté sur la campagne normative** : 712 comparaisons source → 684 évaluées, 28 non évaluables (6 `rho_QQ` + 0 `C_TT_conn` + 22 `flavor_singular_value_ratio`). Par observable : `gamma_O` 416, `rho_QQ` 90, `C_TT_conn` 96, `flavor_singular_value_ratio` 82. Par verdict : 684 `robust`, 0 `non_robust`, 0 `indeterminate`. `gamma_O` null parmi les évaluations : 252, toutes verdict `robust`. 0 groupe `partial_subspace`.

## Lot 1B-9f (accepté définitivement, `8a14a040f569d4eb85de9572b7d23bb587737549` + `a6450e994b7b1ce5c8d7d1fec876128a2a9622e9`)

Synthèse déterministe des résultats inter-S.

**Lot de synthèse descriptive uniquement.** Aucune nouvelle physique, aucun contrat scientifique modifié, aucune conclusion physique automatique. Consomme exclusivement `InterSMatchingReport` + `InterSObservableComparisonReport` + `InterSRobustnessReport` acceptés ; ne recalcule jamais `difference`/`amplitude`/`gamma_O`/`threshold`/`verdict`/`null_reason` quand un `RobustnessResult` existe déjà — celui-ci reste l'unique source de vérité. N'appelle jamais `evaluate_robustness`/`compute_gamma_o`/`match_spectral_group`, ne relit jamais `records.jsonl`, ne diagonalise rien. Ne fusionne jamais les verdicts élémentaires en un verdict global (`global_robust`, `geometry_is_stable`, etc. explicitement interdits), ne moyenne rien entre observables ou groupes différents, n'extrapole rien en `S`.

**Historique de ce sous-lot** : livraison candidate `8a14a040f569d4eb85de9572b7d23bb587737549` — conforme sur la synthèse statistique elle-même, mais acceptation initialement suspendue : les groupes/couples étaient découverts en parcourant `comparison_report.comparisons` plutôt que `matching_report.matches`, si bien qu'un `exact_label_match` valide sans aucune comparaison 1B-9d (liste fermée) disparaissait silencieusement de `groups`/`couples` tout en restant compté dans `matching_count`/`exact_match_count` → **correctif purement structurel décrit ci-dessous, livré au commit indiqué dans « Dernier commit accepté » ; aucune valeur scientifique modifiée**.

**Unité de synthèse et sa source de vérité (corrigé)** : le groupe spectral apparié inter-S (`InterSGroupMatch`, identifié par sa paire de `SpectralGroupMatchKey`, jamais par `representative_energy`/rang/ordre de fichier). La source de vérité pour l'EXISTENCE et l'ORDRE des groupes est `matching_report.matches` lui-même, jamais `comparison_report.comparisons` : un `InterSGroupSynthesis` est produit pour chaque match portant un `low_group` concret (`exact_label_match`), même sans aucune comparaison 1B-9d — un groupe vide (0 évaluation, 0 non-évaluable, verdicts 0/0/0, les 4 `observable_counts` à zéro) est légitime, jamais une erreur. Un match sans `low_group` concret (non exact) reste compté dans `matching_count`, mais ne produit jamais de `InterSGroupSynthesis`. Invariant vérifié avant implémentation : `InterSGroupMatch.__post_init__` (1B-9c, inchangé) garantit déjà `exact_label_match ⟺ low_group is not None` — d'où l'invariant global ajouté `len(groups) == exact_match_count`. `spectral_window_group_index` reste une métadonnée d'affichage intra-cas uniquement.

**Quatre niveaux de synthèse indépendants** :
- **Globale** (`InterSSynthesisReport`) : `matching_count`, `exact_match_count`, `source_comparison_count`, `evaluated_count`, `unevaluable_count`, `verdicts: VerdictCounts`, avec `len(groups) == exact_match_count`.
- **Par couple** (`InterSCoupleSynthesis`) : `high_spin`/`low_spin`, `groups` (ordre dérivé de `matching_report.matches`), comptages d'évaluations/non-évaluables/verdicts/par-observable dérivés exclusivement de `groups` (jamais un compte indépendant).
- **Par groupe apparié** (`InterSGroupSynthesis`) : couple, les deux `SpectralGroupMatchKey`, les deux `spectral_window_group_index`, `match_status`, et les `InterSRobustnessEvaluation`/`ScalarObservableComparison` SOURCES elles-mêmes (références directes, jamais copiées ni remplacées par une agrégation) — **durci** : chaque évaluation/comparaison non-évaluable est vérifiée appartenir exactement à CE groupe (`match_key` ET index intra-cas des deux côtés, via `evaluation.match`/`comparison.high_group`/`low_group` — jamais l'énergie, jamais la proximité, jamais l'ordre), pour empêcher qu'une évaluation d'un autre groupe du même couple ne s'y glisse silencieusement.
- **Par observable** (`InterSObservableSynthesis`, 4 exemplaires, ordre fixe `gamma_O`/`rho_QQ`/`C_TT_conn`/`flavor_singular_value_ratio`) : comptages + statistiques descriptives.

**Invariants `observable_counts`** : pour `InterSGroupSynthesis` et `InterSCoupleSynthesis`, `observable_counts` doit contenir EXACTEMENT les quatre `observable_kind` de `ROBUSTNESS_EVALUATION_ORDER`, dans cet ordre — pas seulement quatre comptes dont la somme tombe juste.

**Statistiques descriptives** (`DescriptiveStatistics` : `count`/`minimum`/`maximum`/`median`, `count == 0 ⟺` les trois autres `None`) : `difference` et `amplitude` lues directement sur chaque `RobustnessResult` ; `median` = `statistics.median` du stdlib, jamais réimplémenté (règle pair/impair déjà standard). `gamma_O` (`GammaOStatistics` : `defined_count`/`null_count`/`values`) calculé pour CHAQUE observable (chaque `RobustnessResult`, quel que soit l'`observable_kind` évalué, porte son propre `gamma_o` — champ générique de la primitive, pas spécifique à `"gamma_O"`).

**Association comparaison → groupe déjà existant** (jamais une recherche d'existence) : réutilise directement `robustness_evaluation._index_matches_by_group_identity`/`_find_source_match` (import direct de primitives privées, aucune duplication) — même mécanisme d'identité d'objet que 1B-9e, pour la même raison (`numpy.ndarray` non hashables) — uniquement pour répartir chaque comparaison dans le groupe déjà déterminé par `matching_report.matches`, jamais pour décider si ce groupe existe.

**Provenance** : `campaign_id`/`manifest_fingerprint`/`repository_commit` conservés depuis les rapports source ; les trois rapports d'entrée doivent partager exactement la même provenance, sinon `InterSSynthesisError` avant toute construction.

**Ordre** : dérivé de `matching_report.matches` (couples/groupes, restreint aux entrées à `low_group` concret) et de l'ordre fixe des 4 observables — jamais un tri par `difference`/`gamma_O`/`amplitude`/`verdict`/`representative_energy` (le tri interne pour la médiane reste transitoire, jamais l'ordre de sortie).

**Interdictions strictes de ce lot** : aucune modification de `src/cosmobox/level1/{matching,robustness,results,serialization}.py`, `scripts/level1b_campaign/*`, `experiments/level1/*`, `schemas/*`, `.github/workflows/*` ; aucun `evaluate_robustness`/`compute_gamma_o`/`match_spectral_group`/`run_single_case`/`run_campaign`/`launch_normative_campaign`/`run_level1b_campaign` ; aucun nouveau seuil/formule/`null_reason`/verdict ; aucun fichier écrit sous `/workspaces/level1b_campaign_output` ni ailleurs.

Fichiers de ce lot : `scripts/level1b_analysis/synthesis.py` (`InterSSynthesisReport`, `InterSCoupleSynthesis`, `InterSGroupSynthesis`, `InterSObservableSynthesis`, `DescriptiveStatistics`, `GammaOStatistics`, `VerdictCounts`, `ObservableEvaluationCounts`, `InterSSynthesisError`, `build_inter_s_synthesis_report`), `tests/scripts/level1b_analysis/test_synthesis.py`.

**Smoke-test réel mesuré** (`/workspaces/level1b_campaign_output`, lecture seule, non hardcodé, invariants 1B-9e exactement reproduits, inchangés après correctif) : 7 groupes appariés (7 `exact_label_match`, `len(groups) == exact_match_count` vérifié), 712 comparaisons source → 684 évaluées / 28 non évaluables. Par observable : `gamma_O` 416 (`gamma_o` défini=164/null=252), `rho_QQ` 90 (unevaluable=6), `C_TT_conn` 96 (unevaluable=0), `flavor_singular_value_ratio` 82 (unevaluable=22). Verdicts : 684 `robust`/0 `non_robust`/0 `indeterminate` partout (global, par couple, par groupe, par observable). Par couple : `triangle` 2 groupes/72 éval/12 non-éval ; `ring4` 2 groupes/192 éval/16 non-éval ; `ring5` 3 groupes/420 éval/0 non-éval. Statistiques (`difference`/`amplitude`/`gamma_O`) mesurées par observable, non hardcodées. `runs/` inchangé. Ceci est un relevé technique, aucune conclusion physique.

1B-9f clôt la chaîne d'analyse mécanique (artefacts → indexation → matching → comparaisons → verdicts → synthèse). Il ne clôt pas, par lui-même, la science de Level 1B : cette clôture scientifique documentaire est le périmètre exact du lot 1B-9g ci-dessous, écrit sous responsabilité conceptuelle de ChatGPT et livré par Claude Code en documentaire uniquement, aucune conclusion de type « géométrie robuste »/« limite continue confirmée » n'ayant jamais été autorisée dans le code de 1B-9f.

**État local de `.gitignore`** : modification volontaire de Lionel (ajout de `results/` aux chemins ignorés), hors périmètre de ce lot — laissée telle quelle, non stagée, non commitée.

## Lot 1B-9g (accepté définitivement, `60ab94f89237a6489b89d2af08b459fc2cb1db23` + `8f12a2f077426df7562b1a9b917f4c1e9bc3b0b9`)

Clôture scientifique documentaire de Level 1B.

**Lot documentaire uniquement.** Aucun nouveau calcul scientifique, aucune campagne, aucun code, aucune nouvelle observable, aucune nouvelle formule, aucun nouveau seuil, aucun nouveau verdict, aucune nouvelle `null_reason`. Consomme exclusivement les résultats déjà acceptés de 1B-9f (`campaign_id=level1b-reference-v1`, `manifest_fingerprint=159660cac738518dc620b9627ec95fd67c5dbc283707fdf72e572886364693ab`, `repository_commit source=0ff65ac66b4aa054f739b350cd384c26ecd19752`) et l'audit scientifique en lecture seule qui l'a suivi (aucune campagne relancée, artefacts `runs/` inchangés).

**Livrable** : `docs/levels/level1/level1b-conclusion.md` (structure fixe en 16 sections : objet et périmètre, provenance, résultat global, robustesse inter-S, secteur de saveur maximale, structure SU(2), valeurs singulières et invariance de saveur, traitement des nulls, `gamma_O` défini vs sous floor, sensibilité par observable, groupe le plus discriminant, ce qui est établi, ce qui est compatible mais non établi, ce qui n'est pas démontré, relation avec le scénario général, transition vers Level 1C). Distingue explicitement les résultats normatifs (théorème du secteur de saveur maximale V11, contrat de normalisation nulle V15) des interprétations analytiques dérivées (annulation du corrélateur habillé en secteur de saveur maximale, structure SU(2) proportionnelle à l'identité — cross-check de V16, jamais un nouveau théorème pré-enregistré).

**Fichiers autorisés (strict)** : `docs/governance/current-task.md`, `docs/levels/level1/level1b-conclusion.md`, `docs/README.md`. Aucun autre fichier. Aucun Python, aucun manifeste, aucun schéma, aucun artefact, aucun test scientifique modifié.

**1B-9g livré / Level 1B clos / aucun Level 1C démarré à ce stade / aucune nouvelle campagne 1B autorisée.**

**Historique de ce sous-lot** : livraison candidate `60ab94f89237a6489b89d2af08b459fc2cb1db23` — conforme scientifiquement sur le fond, mais acceptation initialement suspendue pour deux corrections terminologiques/de portée strictement bornées, aucune valeur ni conclusion scientifique modifiée :

1. confusion terminologique « troncature spectrale `S` » (`S` désigne la troncature en spin de lien / degré de liberté de jauge, notion distincte de la troncature spectrale — `spectral_window`/fenêtre d'eigenvalues/`partial_subspace`/`lower_bound_only`) — corrigé dans `level1b-conclusion.md` §1 et §4 (et vérifié absent ailleurs dans le document) ;
2. formulation de §13 laissant entendre qu'une robustesse inter-S avait déjà été testée sous `j_break` — corrigée : les cas `j_break` de `triangle`/`ring5` n'ont qu'un seul point `S` chacun dans cette campagne et ne produisent donc aucun couple inter-S admissible ; `j_break` a servi ailleurs de contrôle de réduction de symétrie sur cas unique, jamais de comparaison de robustesse inter-S.

Correctif livré au commit `8f12a2f077426df7562b1a9b917f4c1e9bc3b0b9`. **1B-9g est désormais accepté définitivement dans son ensemble** (`60ab94f...` + `8f12a2f...`).

Fichiers de ce lot : `docs/levels/level1/level1b-conclusion.md`, `docs/README.md`, `docs/governance/current-task.md`.

**État local de `.gitignore`** : modification volontaire de Lionel (ajout de `results/` aux chemins ignorés), hors périmètre de ce lot — laissée telle quelle, non stagée, non commitée.

## Lot 1C-0 (accepté définitivement, `fcd05b7444c621680a42764be7ed2da319a9ff29` + `77615bf4fa6c0b0ae36e3b5acd796b3e8a830b53`)

État des lieux et cadrage conceptuel de Level 1C.

**Ce mandat n'a pas autorisé l'implémentation scientifique de Level 1C.** Il a ouvert uniquement le cadrage conceptuel préalable. Aucune campagne Level 1C, aucun code Level 1C, aucune formule de distance, aucun choix de candidat géométrique (`R1`/`R2`/`S1`/`S2`) comme hypothèse privilégiée.

**Question scientifique centrale enregistrée** : les données relationnelles invariantes de jauge validées et robustes de Level 1B contiennent-elles suffisamment de structure pour permettre la reconstruction d'une organisation géométrique effective sans imposer a priori sa dimension, sa topologie ou son plongement ? Level 1C ne part pas de l'hypothèse qu'une géométrie existe ; « aucune géométrie classique simple n'est compatible avec les données » reste un résultat explicitement admissible.

**Livrable** : `docs/levels/level1c/conceptual-framing.md` (11 sections : état hérité de Level 1B, question scientifique, hypothèse nulle, résultats admissibles, inventaire des données relationnelles disponibles, risques de circularité, candidats de représentation géométrique sans favori, décisions scientifiques encore nécessaires — notamment le choix de la donnée relationnelle primaire pour construire une dissimilarité, décision réservée à ChatGPT après audit —, hypothèse directrice générale avec statut de chaque proposition, ce que 1C ne testera pas encore, proposition de découpage des futurs sous-lots en conception uniquement).

**Historique de ce sous-lot** : livraison candidate `fcd05b7444c621680a42764be7ed2da319a9ff29` — conforme sur le fond et sur la gouvernance, mais acceptation initialement suspendue pour une confusion conceptuelle strictement bornée à `docs/levels/level1c/conceptual-framing.md` §5.4 : le document attribuait à tort l'invariance des invariants de saveur (`flavor_singlet`, `flavor_frobenius_squared`, `flavor_singular_values`, `flavor_singular_value_ratio`) à `Psi -> Psi V` en la qualifiant de « rotation de saveur SU(2) » — or `Psi -> Psi V` est le changement de base interne du multiplet spectral dégénéré (justifie `rho_M = Pi_M/d`, spécification §4), une notion distincte de la rotation SU(2) agissant sur les indices `(alpha, beta)` de `G^{alpha,beta}`. Correctif livré au commit `77615bf4fa6c0b0ae36e3b5acd796b3e8a830b53` (aucune formule, aucun résultat Level 1B, aucune décision scientifique Level 1C, aucune définition scientifique modifiée). **1C-0 est désormais accepté définitivement dans son ensemble** (`fcd05b7...` + `77615bf...`).

Fichiers de ce lot : `docs/governance/current-task.md`, `docs/levels/level1c/conceptual-framing.md`, `docs/README.md`.

**État local de `.gitignore`** : modification volontaire de Lionel (ajout de `results/` aux chemins ignorés), hors périmètre de ce lot — laissée telle quelle, non stagée, non commitée.

## Audit accepté — degrés de liberté relationnels (lecture seule, aucun commit propre)

Audit scientifique en lecture seule, réalisé sur le HEAD `77615bf4fa6c0b0ae36e3b5acd796b3e8a830b53`, accepté comme base conceptuelle pour 1C-1. N'a produit aucun commit (interdiction stricte du mandat : aucune modification, aucun fichier créé). Portée : les 7 groupes spectraux appariés de la campagne `level1b-reference-v1`, observables `C_TT_conn`/`rho_QQ`, au point de référence `J_i` uniforme.

**Résultats acceptés** :

```text
Orbites de paires (point de référence) :
  triangle : 1 orbite      ring4 : 2 orbites      ring5 : 2 orbites

DOF relationnels indépendants après symétries et contraintes exactes :
  C_TT_conn : 1-2 par groupe en général, 0 au secteur de saveur maximale
  rho_QQ    : 0-1 par groupe, null structurel au secteur de saveur maximale
```

Symétrie `i<->j` : égalité bit-exacte sur 183/183 paires vérifiées (commutation d'opérateur, pas une optimisation logicielle — le runner recalcule chaque paire ordonnée indépendamment). Règle de somme de charge (`Q_tot=0`) : confirmée exactement (`sum_{j!=i} rho_QQ(i,j) = -1`, 6/6 groupes non-null). Règle de somme de saveur (`T(T+1)`) : fermable exactement uniquement au secteur de saveur maximale (diagonale `T_i^a` non serialisée ailleurs). Information mutuelle : non calculable depuis les artefacts actuels (vecteurs propres et matrices densité réduites jamais sérialisés).

**Conclusion méthodologique acceptée** : le point de référence maximalement symétrique est un excellent jeu de calibration analytique mais il est trop pauvre en degrés de liberté pour constituer, à lui seul et groupe par groupe, une base fortement falsifiable de reconstruction géométrique générique.

## Lot 1C-1 (accepté définitivement, `30990aa390e6adec650227a89fe22654433fe74a`)

Pré-enregistrement conceptuel de la stratégie d'identifiabilité géométrique. Documentaire/conceptuel uniquement — aucune campagne, aucun calcul scientifique, aucun fitting, aucune reconstruction, aucune transformation corrélateur→distance, aucun code scientifique.

**Objectif** : distinguer explicitement le choix de la sonde (quelle observable) de l'identifiabilité du problème géométrique (le signal porté a-t-il assez de DOF indépendants pour qu'une hypothèse géométrique puisse être réellement rejetée), et geler une stratégie conceptuelle en réponse à la pauvreté en DOF constatée par l'audit des degrés de liberté relationnels ci-dessus.

**Livrable** : `docs/levels/level1c/identifiability-preregistration.md` (14 sections : motivation issue de l'audit DOF, sonde primaire `C_TT_conn`, contrôle de concordance `rho_QQ`, observables différées `G[P]`/`gamma_O`, point de référence comme calibration, secteurs inéligibles à l'inférence, principe d'identifiabilité, `j_break` comme candidat de campagne informative, données supplémentaires nécessaires, information mutuelle, statut de `disk7`, hypothèse de cohérence inter-secteurs, décisions encore ouvertes, ce que 1C-1 n'autorise pas).

Fichiers de ce lot : `docs/governance/current-task.md`, `docs/levels/level1c/identifiability-preregistration.md`, `docs/README.md`.

**État local de `.gitignore`** : modification volontaire de Lionel (ajout de `results/` aux chemins ignorés), hors périmètre de ce lot — laissée telle quelle, non stagée, non commitée.

## Audits 1C-2a / 1C-2b / 1C-2c (lecture seule, aucun commit propre) — statut historique corrigé (1C-4c)

**Correction historique (1C-4c)** : une formulation antérieure de cette section présentait `1C-2a`/`1C-2b`/`1C-2c` comme tous acceptés définitivement, sans distinction. **Ceci était inexact.** Le fait historique correct est :

```text
1C-2b et 1C-2c ont été acceptés définitivement, chacun comme jalon à part.

Le rapport initial 1C-2a a été suspendu pour deux défauts précis :
  1. le nombre d'orbites de paires (triangle N_pair_orbits=2, ring5
     N_pair_orbits=6) avait été présenté à tort comme un nombre exact de
     DOF indépendants, alors que seule une borne supérieure avant
     fermeture (DOF_max) était réellement établie ;
  2. le comptage de paramètres d'un plongement R^2 générique (2N-3 si
     l'échelle reste physique, 2N-4 si une échelle globale est
     quotientée) n'avait pas encore de convention d'échelle gelée.

1C-2a corrigé n'a PAS ensuite été accepté séparément comme jalon
définitif à part entière. Ses fondations corrigées ne sont retenues que
dans la mesure où elles ont été reprises et subsumées par 1C-2b/1C-2c.
```

**Contenu retenu (subsumé par 1C-2b/1C-2c, jamais comme jalon 1C-2a autonome)** : identifiabilité sous `j_break` — perturbation sur site (`J_0=1.5`, `J_{i≠0}=1`), symétrie résiduelle exacte `{identité, réflexion fixant le site 0}`, `rho_QQ` fermé exactement par la règle de charge (triangle 1 DOF, ring5 5 DOF), secteur de saveur maximale à 0 DOF sous `j_break` (confirmé et étendu par 1C-4b).

- **1C-2b** (accepté définitivement) — matching inter-S et fermeture informationnelle sous `j_break` : `MATCHING_OK` (le contrat `symmetry_labels_match`/`SpectralGroupMatchKey` existant traite déjà correctement `translation=not_applicable`/`reflection=numeric` des deux côtés, `hamiltonian_identity_without_spin` protège déjà contre un appariement inter-`J_0`), self-correlator minimal identifié (`C_TT_conn(i,i) ≡ <T_i^2>` pour un multiplet complet), `DOF_max_after_closure` (pas `DOF_exact`) établi pour `C_TT_conn` une fois la diagonale connue.
- **1C-2c** (accepté définitivement) — contrat minimal des self-correlators et préflight conceptuel : contrat `SELF_CORRELATOR_CONTRACT=EXTEND_EXISTING_OBSERVABLE`, `path=(i,i)` (jamais `(i,)`), granularité par site, dispatch `partial_subspace` existant réutilisé sans simplification analytique, `VALIDATION_TOLERANCE=OPEN`, convention de ratio `r_X=sqrt(var_X/var_0)` recommandée non normative, `J0_BASELINE_STRATEGY=OPEN`, préflight `spectral_window` conçu (non exécuté).

Voir « Lot 1C-3a » ci-dessous pour l'implémentation du contrat self-correlator issu de ces audits.

## Lot 1C-3a (accepté définitivement, `161b279c35756fde4d0e29253a7c18c4ee028165`)

Implémentation minimale de `C_TT_conn(i,i)`.

Ajoute la production sérialisée de `C_TT_conn(i,i)`, pour chaque site et chaque groupe spectral déjà traité par le runner, en réutilisant l'observable et les primitives existantes — extension de l'observable binaire déjà existante `C_TT_conn(i,j)` au cas `j=i`, jamais une nouvelle famille conceptuelle (`T_self`/`rho_TT`/`local_T_variance` explicitement rejetés).

**Contrat sérialisé** : `observable_kind="C_TT_conn"`, `record_kind="raw_observable"`, `path=(i,i)` (jamais `(i,)`, réservé aux diagnostics à un seul site), `payload` un flottant nu identique en forme au cas hors diagonale, `flavor_component=None`, `normalization=None`. Réutilise exclusivement `local_observables.flavor_correlator_connected_group(generators_i, generators_i, group_state)`, déjà documentée comme acceptant `i==j` — aucune seconde formule. Dispatch `complete_multiplet`/`partial_subspace` déjà existant réutilisé tel quel, sans jamais simplifier en `<T_i^2>` pour un groupe partiel. Un enregistrement par site (jamais une compression par orbite). Le contrat D021 (`all_ordered_distinct_pairs`, `case.ordered_pairs`) reste inchangé — la diagonale est une production séparée (`_self_flavor_correlator_record`, appelée dans la même boucle `for node in lattice.nodes` que le diagnostic restreint de `C_QQ_raw`).

**Hors périmètre de ce lot** (conforme au mandat) : le contrôle global `sum_i C_TT_conn(i,i) + sum_{i!=j} C_TT_conn(i,j) = T(T+1)` (tolérance encore `OPEN`, 1C-2c), `rho_QQ`/le ratio `r_X=sqrt(var_X/var_0)`/le préflight `spectral_window`/la stratégie `J0` de référence (tous inchangés), toute campagne, toute diagonalisation de campagne, `matching.py`, `robustness.py`, le manifeste normatif, le schéma (`path` accepte déjà toute longueur ≥1 sans contrainte d'unicité des éléments — aucune modification nécessaire, confirmé par 1C-2c et revérifié ici).

**Compatibilité ascendante** : les artefacts normatifs Level 1B déjà produits (`/workspaces/level1b_campaign_output/runs/`) restent inchangés et valides selon le contrat sous lequel ils ont été produits ; leur absence de `C_TT_conn(i,i)` n'est jamais traitée comme une erreur de validation rétroactive. Aucun chargeur/validateur existant (`validate_existing_case_run`, `scripts/level1b_analysis/loader.py`) n'a été modifié ni ne requiert la nouvelle donnée pour les anciens runs. `schema_version`/`manifest`/`campaign_id` inchangés — la nouvelle production est additive et ne casse la lecture d'aucun artefact existant.

Fichiers de ce lot : `scripts/level1b_campaign/runner.py`, `tests/scripts/level1b_campaign/test_runner.py`, `docs/levels/level1/implementation-design.md`, `docs/governance/current-task.md`.

**État local de `.gitignore`** : modification volontaire de Lionel (ajout de `results/` aux chemins ignorés), hors périmètre de ce lot — laissée telle quelle, non stagée, non commitée.

## Lot 1C-3b (accepté définitivement, `df6022d79118c693ee5928a88c99e553543948a1`)

Validation du self-correlator diagonal et fermeture de la somme `T(T+1)`.

Valide scientifiquement et numériquement la production `C_TT_conn(i,i)` (1C-3a) : nouvelle fonction pure `local_observables.validate_flavor_total_sum(status, twice_T, diagonal_values, off_diagonal_values, *, tolerance=FLAVOR_TOTAL_SUM_TOLERANCE)`, retournant `FlavorTotalSumValidation(applicable, measured, expected, residual, is_valid)`. Vérifie `sum_i C_TT_conn(i,i) + sum_{i!=j} C_TT_conn(i,j) = T(T+1)` (`T=twice_T/2`), les deux ordres `(i,j)`/`(j,i)` sommés tels que sérialisés (jamais divisés par deux, jamais reconstruits depuis une moyenne d'orbite), jamais `C_TT_raw`.

**Tolérance retenue** : `FLAVOR_TOTAL_SUM_TOLERANCE = matching.FLAVOR_LABEL_TOLERANCE` (`1e-8`), réutilisée par import, jamais redéfinie — audit préalable (§2 du mandat) : `compute_twice_T` (`matching.py`) compare déjà une quantité calculée à la MÊME cible `T(T+1)` avec cette exacte tolérance absolue ; la tolérance analytique plus stricte `1e-10` (V11, `IMAGINARY_PART_TOLERANCE`) a été explicitement écartée car jamais gelée pour une somme de plusieurs termes (jusqu'à `N²≤25` termes sur les géométries actuelles) — l'accumulation de bruit légitime à travers cette somme pourrait dépasser `1e-10` alors qu'elle reste très en-deçà de `1e-8` (marge démontrée, pas seulement supposée).

**Politique `complete_multiplet`/`partial_subspace`** : contrôle applicable uniquement à `complete_multiplet` avec `twice_T` résolu ; `applicable=False` (tous les autres champs `None`) pour tout groupe `partial_subspace` — jamais un faux `T` ni un verdict approximatif, jamais une exception pour ce cas normal.

**Nouveau critère de validation** : `V23` (`docs/levels/level1/validation-plan.md`), gelé après audit de la numérotation existante (dernier numéro utilisé : V22).

**Couverture réelle** : `V23` vérifié sur `triangle` référence (2 groupes), `ring4` référence (tous groupes), `triangle j_break` (3 groupes, dont le secteur de saveur maximale `twice_T=3` — confirmé `C_TT_conn(i,i)=0.75` exactement par site, cohérent avec le théorème déjà établi) ; égalité de symétrie locale `D1≈D2` vérifiée sur `triangle j_break`. **Aucune fixture `ring5-j_break` n'existe** dans la suite de tests (coût de diagonalisation disproportionné, `dim=1000` à `S=2`) — `D1≈D4`/`D2≈D3` sur `ring5-j_break` restent non couvertes par un test automatisé, documenté comme trou de couverture explicite plutôt que fabriqué artificiellement.

**Compatibilité ascendante** : aucune modification de `validate_existing_case_run`, du schéma, du manifeste, de `matching.py`, de `robustness.py` ; les artefacts Level 1B historiques (sans `C_TT_conn(i,i)`) restent valides sous leur contrat d'origine, `V23` ne s'applique qu'aux productions enrichies qui contiennent explicitement la diagonale.

Fichiers de ce lot : `src/cosmobox/level1/local_observables.py`, `tests/level1/test_local_observables.py`, `tests/scripts/level1b_campaign/test_runner.py`, `docs/levels/level1/validation-plan.md`, `docs/levels/level1/implementation-design.md`, `docs/governance/current-task.md`.

**Historique de ce sous-lot** : livraison candidate `fe0c26aae9a7c4032106808ef1624bd6b9ec1e19` — conforme scientifiquement sur la règle `T(T+1)`, la tolérance retenue, la politique `partial_subspace` et les tests end-to-end, mais acceptation initialement suspendue pour un défaut structurel strictement borné : `validate_flavor_total_sum` ne vérifiait pas que `diagonal_values`/`off_diagonal_values` couvraient réellement `N` diagonales et `N(N-1)` paires ordonnées complètes pour un ensemble de sites cohérent — un corpus incomplet ou incohérent (direction manquante, paire absente, paire diagonale glissée en hors-diagonale, site étranger, diagonale vide) pouvait satisfaire `T(T+1)` par coïncidence numérique et être déclaré `is_valid=True`. **Correctif** : contrôle structurel complet ajouté avant tout calcul de somme — `sites=set(diagonal_values)` non vide, `set(off_diagonal_values)` exactement égal à `{(i,j): i,j∈sites, i≠j}`, `twice_T` validé comme `int>=0` réel, chaque valeur validée finie — toute incohérence lève `ValueError` (jamais `is_valid=False`, réservé exclusivement à un corpus structurellement complet dont la somme physique manque `T(T+1)`). `partial_subspace` continue de court-circuiter cette vérification entièrement (`applicable=False` avant toute inspection). Tolérance `1e-8` non rouverte. Correctif livré au commit `df6022d79118c693ee5928a88c99e553543948a1`. **1C-3b est accepté définitivement dans son ensemble** (`fe0c26aae9a7c4032106808ef1624bd6b9ec1e19` + correctif `df6022d79118c693ee5928a88c99e553543948a1`).

**État local de `.gitignore`** : modification volontaire de Lionel (ajout de `results/` aux chemins ignorés), hors périmètre de ce lot — laissée telle quelle, non stagée, non commitée.

## Lot 1C-4a (accepté définitivement, `17de4e86a399bc2fb25a5e96929994f796733a3c`)

Préflight spectral exploratoire `j_break` à `S=3`, en quatre phases (design, exécution, audit du script, fermeture déterministe des verdicts), puis commit de traçabilité de l'outil.

**Outil** : `scripts/level1c_preflight/{__init__.py,jbreak_spectral_preflight.py}` — script exploratoire, non normatif, disposable, réutilisant exclusivement des primitives déjà acceptées (diagonalisation Level0, `target_selection.select_target_group`, `matching.match_spectral_group`, `local_observables.validate_flavor_total_sum`, toutes inchangées). N'écrit jamais sous `/workspaces/level1b_campaign_output/`, n'appelle jamais `launch_normative_campaign`/`run_campaign`/`evaluate_robustness`/`compute_gamma_o`. Verdicts déterministes : `evaluate_window_verdict` (`WINDOW_SUFFICIENT`/`WINDOW_INSUFFICIENT`/`PREFLIGHT_INCONCLUSIVE`, priorité fixe, aucun seuil énergétique), `compute_preflight_integrity`, `evaluate_resource_verdict` (`RESOURCE_FEASIBLE`/`RESOURCE_BLOCKING`, aucun seuil quantitatif inventé).

**Résultat déjà observé et accepté** (non recalculé) :

```text
triangle-S3-j_break : candidate_window=16 SUFFISANTE (WINDOW_SUFFICIENT) --
  fundamental/first_excited/T_max présents, complete_multiplet, V23 PASS,
  réflexion PASS, marge structurelle observée, candidats exacts inter-S
  disponibles (MATCH_CANDIDATE_AVAILABLE).

ring5-S3-j_break : candidate_window=24 INSUFFISANTE (WINDOW_INSUFFICIENT) --
  cause UNIQUE observée : T_max absent même dans exploratory_window=48.
  fundamental/first_excited/T_3_2 : présents, complete_multiplet, V23 PASS,
  réflexion PASS, candidats exacts inter-S disponibles.

Ressources : RESOURCE_FEASIBLE pour les deux géométries (chemin dense,
  triangle 0.07s, ring5 3.96s).
```

**`exploratory_window=32/48` reste une fenêtre de préflight, jamais une nouvelle fenêtre normative** — aucun manifeste modifié, aucune campagne lancée. La décision sur la nécessité de `T_max` (donc sur toute éventuelle fenêtre future) est traitée séparément par 1C-4b/1C-4c ci-dessous.

## Lot 1C-4b (accepté définitivement, lecture seule, aucun commit propre)

Audit d'utilité informationnelle du secteur de saveur maximale `T_max` pour l'inférence géométrique Level 1C (trois tours : rapport initial, deux corrections strictement bornées — preuve de `O_ij_raw=0` par orthogonalité des secteurs d'occupation plutôt que par une conservation de `T²` non fondée ; correction historique 1C-2a, voir section dédiée ci-dessus).

**Verdicts acceptés** :

```text
TMAX_OCCUPANCY_THEOREM              = PROVEN
TMAX_CTT_SELF                       = FIXED       (= 3/4 exactement)
TMAX_CTT_OFFDIAGONAL                = FIXED       (= 1/4 exactement)
TMAX_RHO_QQ                         = STRUCTURAL_NULL
TMAX_GEOMETRY_INFORMATION_DOF_CTT   = 0
TMAX_PRIMARY_GEOMETRY_VALUE         = NON_INFORMATIVE
TMAX_CALIBRATION_VALUE              = HIGH
TMAX_LEVEL1C_POLICY_RECOMMENDATION  = CALIBRATION_ONLY
POST_HOC_BIAS_CONTROL               = ADEQUATE_IF_PREREGISTERED_NOW
RING5_DEEPER_WINDOW_NEEDED_NOW      = NO
```

Démonstration complète : occupation `n_i=1` exacte (théorème déjà gelé + demi-remplissage `Q_tot=0`, complété par un argument de tiroirs) ; self-corrélateur `C_TT_conn(i,i)=3/4` (Casimir local d'un spin-1/2 exact) ; hors-diagonale `C_TT_conn(i,j)=1/4` pour toute paire (règle de somme `T_tot²` + symétrie de permutation `S_N` du secteur de spin maximal, multiplicité 1, indépendante de la géométrie et de `j_break`) ; `rho_QQ` structurellement null (`Q_i≡0` exact) ; `G_ij^{alpha,beta}[P]=0` pour `i≠j` par orthogonalité exacte des secteurs d'occupation (Pauli + théorème d'occupation, jamais une conservation de `T²` par l'opérateur lui-même). Politique pré-enregistrée par **1C-4c** ci-dessous.

## Lot 1C-4c — pré-enregistrement documentaire de la politique `T_max` (ce commit)

Documentaire uniquement, aucune nouvelle science, aucune campagne, aucune diagonalisation. Inscrit dans `docs/levels/level1c/identifiability-preregistration.md` (nouvelle sous-section « Statut du secteur de saveur maximale `T_max` (1C-4b, gelé) », sous §6 « Secteurs inéligibles à l'inférence ») la politique **`CALIBRATION_ONLY`** pour `T_max` en Level 1C : cible non obligatoire pour l'inférence géométrique, utilisée comme contrôle analytique si naturellement présente, aucune fenêtre spectrale plus profonde exigée uniquement pour l'atteindre — règle universelle (toute géométrie, tout `J_0`/`S` futur) tant que `M=2`, `Q_tot=0` (demi-remplissage global), SU(2) de saveur exacte, `T=N/2`.

Corrige également, dans la présente section (voir ci-dessus, « Audits 1C-2a / 1C-2b / 1C-2c »), la formulation historique inexacte qui présentait les trois audits comme tous acceptés définitivement sans distinction.

**Manifeste Level 1B inchangé** : `T_max` reste une cible historique valide de Level 1B (`experiments/level1/preregistered-manifest-v1.json`, non modifié). La politique `CALIBRATION_ONLY` concerne exclusivement la future inférence géométrique Level 1C.

Fichiers de ce lot : `docs/levels/level1c/identifiability-preregistration.md`, `docs/governance/current-task.md`.

**État local de `.gitignore`** : modification volontaire de Lionel (ajout de `results/` aux chemins ignorés), hors périmètre de ce lot — laissée telle quelle, non stagée, non commitée.

## Lot 1C-5a (accepté définitivement, lecture seule, aucun commit propre)

Audit d'identifiabilité du jeu de cibles Level 1C (après retrait de `T_max`, cf. 1C-4b/1C-4c) et du statut scientifique de la variation `J0`. Trois tours : rapport initial, une correction (comptage DOF sur la matrice complète et non sur les seules orbites hors-diagonale ; distinction « réponse permise » / « réponse démontrée » ; suppression d'une revendication d'unicité non prouvée sur `BLIND_DEFECT_LOCALIZATION`), puis un durcissement méthodologique final (versionnement fermé du critère d'éligibilité, remplaçant un critère informel « aucun théorème connu ne ferme DOF=0 »).

**Verdicts acceptés** :

```text
INFERENCE_TARGET_CONTRACT             = DEFINED
FUNDAMENTAL_INFORMATION_VALUE         = MEDIUM
FIRST_EXCITED_INFORMATION_VALUE       = MEDIUM
T_3_2_INFORMATION_VALUE               = MEDIUM (ring5) / NOT_APPLICABLE (triangle)
MULTISTATE_COMMON_GEOMETRY_HYPOTHESIS = DEFENSIBLE
J0_STATUS                             = LOCAL_RESPONSE_PROBE
J0_EQ_1_ROLE                          = CALIBRATION_BASELINE
DELTA_CTT_USEFULNESS                  = PROMISING
BLIND_DEFECT_LOCALIZATION             = CONDITIONAL
RELABEL_INVARIANCE_REQUIREMENT        = REQUIRED
INTER_S_LEVEL1C_ROLE                  = ROBUSTNESS_ONLY
TARGET_POLICY_RECOMMENDATION          = ELIGIBILITY_BASED (via STRUCTURALLY_ELIGIBLE v1)
J0_CAMPAIGN_READY                     = NO
1C5A_READINESS                        = READY_FOR_PREREGISTRATION
```

**Comptage DOF corrigé, matrice `C_TT` complète sous `j_break`** :

```text
triangle : N_serialized_values=9,  N_diagonal_orbits=2 ({D0},{D1,D2}),
           N_offdiagonal_pair_orbits=2 ({01,02},{12}),
           N_symmetry_orbits_total=4, DOF_max_before_global_closure=4,
           DOF_max_after_global_closure=3, DOF_exact=UNKNOWN
ring5    : N_serialized_values=25, N_diagonal_orbits=3 ({D0},{D1,D4},{D2,D3}),
           N_offdiagonal_pair_orbits=6,
           N_symmetry_orbits_total=9, DOF_max_before_global_closure=9,
           DOF_max_after_global_closure=8, DOF_exact=UNKNOWN
```

Quantité historique distincte, conditionnelle (diagonales traitées comme déjà connues, hors-diagonale seule) : `offdiagonal_DOF_max` triangle `2→1`, ring5 `6→5` — **jamais confondue** avec le plafond de la matrice complète ci-dessus.

Politique pré-enregistrée documentairement par **1C-5b** ci-dessous.

## Lot 1C-5b — pré-enregistrement documentaire du contrat `STRUCTURALLY_ELIGIBLE` v1 et du statut de `J0` (ce commit)

Documentaire uniquement, aucune nouvelle science, aucune campagne, aucune diagonalisation. Inscrit dans `docs/levels/level1c/identifiability-preregistration.md` une nouvelle section « §15. Contrat `STRUCTURALLY_ELIGIBLE` v1 et statut de `J0` » qui fige : le contrat `STRUCTURALLY_ELIGIBLE` v1 (6 critères, `STRUCTURALLY_ELIGIBLE_CONTRACT_VERSION = 1`) ; l'ensemble analytique gelé v1.1–v1.4 (commutation `C_ij=C_ji`, symétrie spatiale résiduelle, fermeture globale V23, théorème `T_max` — v1.1/v1.2 attribués aux fondations corrigées de l'audit 1C-2a, retenues telles que subsumées par 1C-2b/1C-2c) ; les règles de gouvernance/versionnement (aucune identité nouvelle ne modifie silencieusement v1 ; audit séparé + nouvelle version explicite + gel avant usage ; le versionnement n'excuse jamais d'ignorer une contradiction scientifique réelle) ; l'application du contrat (triangle/ring5, `T_max` exclu, formulation « canaux observationnels distincts / groupes spectraux non dupliqués », sans revendication d'indépendance informationnelle) ; les tables DOF corrigées (matrice complète, plus la quantité conditionnelle historique gardée séparée) ; `TARGET_POLICY = ELIGIBILITY_BASED` ; l'hypothèse multi-état comme non testée ; `J0_STATUS = LOCAL_RESPONSE_PROBE` et `J0_EQ_1_ROLE = CALIBRATION_BASELINE` ; `DELTA_CTT_USEFULNESS = PROMISING` (aucun nouvel observable/schéma/seuil) ; la distinction réponse permise/démontrée ; `BLIND_DEFECT_LOCALIZATION = CONDITIONAL` ; `RELABEL_INVARIANCE_REQUIREMENT = REQUIRED` ; la comparaison structurelle triangle/ring5 (aucune distance radiale posée comme vérité-cible) ; `INTER_S_LEVEL1C_ROLE = ROBUSTNESS_ONLY` ; une sous-section « Questions empiriques non résolues » ; et la confirmation explicite `J0_CAMPAIGN_READY = NO` avec la liste de ce qui reste non décidé (valeurs de `J0`, grille, fenêtres, seuils, algorithmes).

Fichiers de ce lot : `docs/levels/level1c/identifiability-preregistration.md`, `docs/governance/current-task.md`.

**État local de `.gitignore`** : modification volontaire de Lionel (ajout de `results/` aux chemins ignorés), hors périmètre de ce lot — laissée telle quelle, non stagée, non commitée.

## Lot 1C-6a (accepté définitivement, lecture seule, aucun commit propre)

Design scientifique de la campagne de réponse locale `J0×S`, avant tout pré-enregistrement final. Verdicts acceptés : question expérimentale précisée (variation de `C_TT_conn` complète non incidente, covariante, non-artefact de `S`) ; hiérarchie `H0`–`H4` (`H4` requalifiée en axe transversal, jamais un palier terminal) ; `J0_GRID_SYMMETRY=SYMMETRIC_AROUND_1` (sous contrainte `J0>0`) ; `J0_POINT_COUNT_RECOMMENDATION=5` ; `AMPLITUDE_POLICY=PARAMETRIC_DELTA` (`δ` non fixé) ; `J0_1P5_STATUS=USEFUL_ANCHOR` (jamais `REQUIRED`) ; `S_SET_RECOMMENDATION={2,3}` ; `PRIMARY_ANALYSIS_SET`/`SECONDARY_ANALYSIS_SET`/`CALIBRATION_SET` définis par intersection structurelle triangle/ring5 (jamais post-hoc) ; unité d'analyse = matrice `C_TT_conn` complète par `(geometry,J0,S)` ; `NONLOCAL_RESPONSE_TEST=ADMISSIBLE` (partition binaire incident/non-incident uniquement) ; `BLIND_PROTOCOL=DEFINED`, `RELABEL_TEST_POLICY=EXHAUSTIVE` ; `PHYSICAL_RESPONSE_THRESHOLD=OPEN` (distinct de `NUMERICAL_EQUALITY_TOLERANCE`) ; `INTER_S_RESPONSE_ROBUSTNESS=PARTIALLY_DEFINED` ; `RUN_ORDER_POLICY=IRRELEVANT_BUT_FIXED` ; `GEOMETRY_RECONSTRUCTION_GATE=CONDITIONAL` ; `SPECTRAL_PREFLIGHT_REQUIRED=YES` ; `J0_CAMPAIGN_READY=NO`. Trois tours : rapport initial, un correctif majeur (identification du problème architectural central — `matching.py` ne peut jamais être détourné pour un suivi inter-`J0`, car `hamiltonian_identity_without_spin` inclut le tuple `J` complet ; nécessité d'un concept `J0_RESPONSE_GROUP_TRACKING` distinct ; découverte du problème de scission de multiplet sous réduction de symétrie), puis un correctif final (distinction `BRANCH_C1`/`BRANCH_C2`, `HYBRID_ARCHITECTURE` comme recommandation finale de tracking). Politique pré-enregistrée documentairement par **1C-6c** ci-dessous.

## Lot 1C-6b (accepté définitivement, lecture seule, aucun commit propre)

Audit scientifique de la baseline `J0=1` et du contrat `BRANCH_C2`. Quatre tours de correction, chacun strictement borné : (1) rapport initial — `LEVEL1B_REFERENCE_REUSE=PARTIAL_ONLY` (les artefacts Level 1B historiques ne portent pas `C_TT_conn(i,i)`, ajouté seulement en 1C-3a), `LEVEL1C_BASELINE_STRATEGY=RECOMPUTE_COMPLETE_BASELINE`, distinction `BRANCH_C1` (`PARENT_SUBGROUP_DECOMPOSITION`, `DEFINED`) / `BRANCH_C2` (`SUBPROJECTED_PARENT_EXPECTATION`, `NOT_DEFINED`) ; (2) correctif SU(2) — preuve formelle `[R,T^a]=0` (dérivée de la covariance déjà acceptée V06/V07, aucun nouveau calcul), `SUBPROJECTED_V23_STATUS` initialement `PRESERVED_BY_SU2`, correction de la formule de multiplicité (dimension brute de l'espace de réflexion ≠ multiplicité spatiale, facteur `2T+1` de la représentation SU(2) à soustraire) ; (3) correctif projecteur — distinction rigoureuse `P_parent`/`P_r_global`/`Q_parent,r = P_parent P_r_global`, démonstration `[Q_parent,r,T^a]=0` ; (4) correctif fixed-T (final) — distinction critique `AVERAGE_CASIMIR_RESOLVED` (`compute_twice_T`, déjà disponible) vs `FIXED_T_SUBSPACE` (`Psi†T²Psi ≈ T(T+1)I_d`, jamais démontré par le pipeline courant) ; `SUBPROJECTED_V23_STATUS` corrigé en **`PRESERVED_IF_FIXED_T`** (jamais inconditionnel) ; `REPEATED_IRREP_POLICY=MORE_STRUCTURE_REQUIRED` par défaut tant que `FIXED_T_SUBSPACE` n'est pas établi. Verdicts finaux acceptés : `J0_EQ_1_ROLE=CALIBRATION_BASELINE` (jamais `PRIMARY_INFERENCE_POINT`) ; `BASELINE_NON_REGRESSION_CHECK=REQUIRED` ; `BASELINE_S_SET={2,3}` ; `SUBPROJECTED_CANONICAL_DENSITY=NEW_ASSUMPTION` ; `BRANCH_C2_CONTRACT=NOT_DEFINED` ; `SPLIT_BRANCH_RESPONSE_POLICY=STRUCTURAL_ONLY` ; `DELTA_CTT_SPLIT_BRANCH=BLOCKED` ; `1C6B_READY_FOR_PREREGISTRATION=YES` ; `J0_DESIGN_READY_FOR_PREREGISTRATION=CONDITIONAL` ; `J0_CAMPAIGN_READY=NO`. Politique pré-enregistrée documentairement par **1C-6c** ci-dessous.

## Lot 1C-6c — pré-enregistrement documentaire du design de campagne `J0` et de la stratégie baseline/tracking (ce commit)

Documentaire uniquement, aucune nouvelle science, aucune campagne, aucune diagonalisation. Inscrit dans `docs/levels/level1c/identifiability-preregistration.md` une nouvelle section « §16. Design de la campagne de réponse locale `J0` (1C-6a/1C-6b, gelé) » qui fige, sans choisir aucune valeur numérique de `δ`, aucune fenêtre, aucun seuil : la question expérimentale précisée et la hiérarchie `H0`–`H4` ; `J0_STATUS=LOCAL_RESPONSE_PROBE`/`J0_EQ_1_ROLE=CALIBRATION_BASELINE` et le statut purement conceptuel de `delta_J` ; la forme paramétrique de grille `{1-2δ,1-δ,1,1+δ,1+2δ}` (`δ=OPEN`, `J0>0` requis, `J0=1.5` seulement `USEFUL_ANCHOR`) ; `S_SET_RECOMMENDATION`/`BASELINE_S_SET={2,3}` ; les ensembles `PRIMARY`/`SECONDARY`/`CALIBRATION` (rôles analytiques pré-enregistrés, jamais post-hoc, sans modifier `STRUCTURALLY_ELIGIBLE` v1) ; l'unité d'analyse (matrice `C_TT_conn` complète) et ses trois axes de comparaison ; la stratégie de baseline `LEVEL1C_BASELINE_STRATEGY=RECOMPUTE_COMPLETE_BASELINE` (avec le rôle `CALIBRATION_BASELINE`, jamais `PRIMARY_INFERENCE_POINT`, de `J0=1`) et `BASELINE_NON_REGRESSION_CHECK=REQUIRED` (distinct d'un test scientifique `J0`) ; le contrat de provenance minimal de la baseline ; `BASELINE_PREFLIGHT_REQUIRED`/`SPECTRAL_PREFLIGHT_REQUIRED=YES` ; `J0_GROUP_TRACKING_RECOMMENDATION=HYBRID_ARCHITECTURE` et l'exclusion de la translation de l'identité de tracking à la frontière `J0=1↔J0!=1` (`matching.py` non modifié, jamais détourné) ; la taxonomie conceptuelle `TRACKED_ONE_TO_ONE`/`TRACKED_SPLIT_BRANCH`/`AMBIGUOUS`/`DISCONTINUOUS`/`NOT_AVAILABLE` ; `BRANCH_C1` (`DEFINED`, classification uniquement, jamais une identité fille unique) et les projecteurs conceptuels `P_parent`/`P_r_global`/`Q_parent,r` ; le résultat SU(2) `[R,T^a]=0` et ses conséquences `[P_r_global,T^a]=0`/`[Q_parent,r,T^a]=0` ; la distinction `AVERAGE_CASIMIR_RESOLVED`/`FIXED_T_SUBSPACE` et `SUBPROJECTED_V23_STATUS=PRESERVED_IF_FIXED_T` ; la formule de multiplicité spatiale conditionnée à `FIXED_T_SUBSPACE` et `REPEATED_IRREP_POLICY=MORE_STRUCTURE_REQUIRED` par défaut ; `BRANCH_C2_CONTRACT=NOT_DEFINED`/`SUBPROJECTED_CANONICAL_DENSITY=NEW_ASSUMPTION` (aucune formule, aucune primitive, aucun observable créés) ; `SPLIT_BRANCH_RESPONSE_POLICY=STRUCTURAL_ONLY`/`DELTA_CTT_SPLIT_BRANCH=BLOCKED` ; `Delta_C_TT` comme quantité dérivée conditionnelle ; `NONLOCAL_RESPONSE_TEST=ADMISSIBLE` (partition binaire uniquement) ; le protocole blind et `RELABEL_TEST_POLICY=EXHAUSTIVE` ; `PHYSICAL_RESPONSE_THRESHOLD=OPEN` distinct de `NUMERICAL_EQUALITY_TOLERANCE` ; `INTER_S_RESPONSE_ROBUSTNESS=PARTIALLY_DEFINED` ; `RUN_ORDER_POLICY=IRRELEVANT_BUT_FIXED` et la gestion conceptuelle des échecs (aucun schéma créé) ; les rôles triangle/ring5 et `GEOMETRY_RECONSTRUCTION_GATE=CONDITIONAL` ; et la liste explicite des points encore ouverts après ce lot.

Fichiers de ce lot : `docs/levels/level1c/identifiability-preregistration.md`, `docs/governance/current-task.md`.

**État local de `.gitignore`** : modification volontaire de Lionel (ajout de `results/` aux chemins ignorés), hors périmètre de ce lot — laissée telle quelle, non stagée, non commitée. **Intégrée depuis par le housekeeping `a2dd016a4dbb035782340667b15fc35c6aa19e46` (`chore: ignore generated results`) : n'est plus une exception locale.**

## Lot 1C-6d (accepté définitivement, lecture seule, aucun commit propre)

Choix scientifique de `δ` et design du préflight spectral/resources de la campagne `J0×S`, avant toute nouvelle donnée. Deux tours : rapport initial, un correctif méthodologique (contradiction entre `DELTA_SELECTION_POLICY=FIX_BEFORE_PREFLIGHT` et un `J0_GRID_READY_TO_FREEZE=CONDITIONAL` initialement mal posé — le préflight ne peut jamais être une condition de gel de `δ`, seulement une vérification de faisabilité d'une grille déjà gelée). Verdicts finaux acceptés : `DELTA_SELECTION_POLICY=FIX_BEFORE_PREFLIGHT` ; `DELTA_RECOMMENDATION=0.25` (grille `{0.5,0.75,1.0,1.25,1.5}`, justification PRIMARY : symétrie, deux amplitudes `±25%`/`±50%`, `J0>0` strict, aucune ancienne valeur `C_TT_conn` réutilisée pour choisir `δ` ; SECONDARY : coïncidence avec l'ancre historique `J0=1.5`) ; `J0_GRID_READY_TO_FREEZE=YES` (distinct de `GRID_PREFLIGHT_PASSED=NOT_EVALUATED`, jamais confondus) ; `PREFLIGHT_CASE_SET=FULL_20_CASE_GRID` (2 géométries × 2 `S` × 5 `J0`, baseline `J0=1` incluse sans exemption) ; `SPECTRAL_MARGIN_POLICY=REUSE_1C4A` ; `PREFLIGHT_BLINDNESS_CONTRACT=DEFINED` (liste fermée des données structurelles autorisées vs des observables scientifiques interdits pour le choix de `δ`/grille/fenêtre) ; `PREFLIGHT_V23_POLICY=VALIDATE_WITHOUT_EXPOSING_VALUES` (avec le constat que l'outil `jbreak_spectral_preflight.py` existant expose actuellement `measured`/`expected`/`residual`/diagonales — nécessitera une adaptation future, non implémentée ici) ; `NON_REGRESSION_TIMING=FINAL_PRODUCTION` ; `FIXED_T_REQUIRED_FOR_FIRST_CAMPAIGN=NO` ; `PREFLIGHT_GLOBAL_POLICY=ALL_REQUIRED_CASES` ; `PREFLIGHT_DESIGN_READY=CONDITIONAL` ; `J0_CAMPAIGN_READY=NO`. Politique pré-enregistrée documentairement par **1C-6e** ci-dessous.

## Lot 1C-6e — pré-enregistrement documentaire du gel de `δ`, de la grille `J0` et du contrat de préflight (ce commit)

Documentaire uniquement, aucune nouvelle science, aucune campagne, aucune diagonalisation. Inscrit dans `docs/levels/level1c/identifiability-preregistration.md` une nouvelle section « §17. Gel de `δ`, de la grille `J0` et contrat de préflight (1C-6d/1C-6e, gelé) » qui fige : `DELTA_J0=0.25`, `J0_GRID={0.5,0.75,1.0,1.25,1.5}`, `J0_GRID_STATUS=FROZEN` (avec §16.4 conservée comme statut historique antérieur au gel) ; la justification PRIMARY/SECONDARY du gel (sans jamais réutiliser d'ancienne valeur `C_TT_conn`/`Delta_C_TT`, sans jamais qualifier `±25%`/`±50%` de « perturbatif ») ; l'ordre méthodologique gelé choix de `δ` → acceptation → gel de grille → préflight → décision de faisabilité → campagne uniquement si succès ; la distinction stricte `GRID_READY_TO_FREEZE`/`J0_GRID_STATUS=FROZEN`/`GRID_PREFLIGHT_PASSED=NOT_EVALUATED` ; la politique en cas d'échec du préflight (aucun ajustement silencieux, tout nouveau `δ` futur re-justifié et re-pré-enregistré séparément) ; `PREFLIGHT_CASE_SET=FULL_20_CASE_GRID` et les cibles requises par géométrie ; le contrat de complétude/marge spectrale (`REUSE_1C4A`) ; les fenêtres `candidate`/`exploratory` comme concepts seulement (aucune valeur numérique, anciennes fenêtres 1C-4a `HISTORICAL_REFERENCE_ONLY`), avec `FULL_DENSE_EXPLORATORY_WINDOW=CANDIDATE_DESIGN` explicitement non gelé ; `PREFLIGHT_BLINDNESS_CONTRACT=DEFINED` et `PREFLIGHT_V23_POLICY=VALIDATE_WITHOUT_EXPOSING_VALUES` (avec le constat d'adaptation future requise pour l'outil existant) ; `NON_REGRESSION_TIMING=FINAL_PRODUCTION` ; `FIXED_T_REQUIRED_FOR_FIRST_CAMPAIGN=NO` et le rôle de `BRANCH_C1` en préflight (faisabilité structurelle uniquement) ; les ressources (dimensions déjà connues, dispatch dense confirmé pour les 20 cas, distinction estimation de stockage/pic mémoire) ; les statuts de préflight et leur priorité déterministe, la frontière stricte préflight/analyse finale, la politique d'élargissement de fenêtre, `PREFLIGHT_GLOBAL_POLICY=ALL_REQUIRED_CASES`, la symétrie `±δ` indivisible, et l'ordre déterministe des 20 cas (auditabilité uniquement) ; et la liste explicite des points encore ouverts après ce lot.

Fichiers de ce lot : `docs/levels/level1c/identifiability-preregistration.md`, `docs/governance/current-task.md`.

## Lot 1C-6f (accepté définitivement, lecture seule, aucun commit propre)

Design final des fenêtres et architecture du préflight aveugle de la campagne `J0×S`. Un correctif méthodologique unique, strictement borné : `PRODUCTION_WINDOW_RULE` révisée de `B` vers `A`, après audit rigoureux du comportement réel de `lower_bound_only` (`cosmobox.level0.degeneracy::analyze_spectral_degeneracies`, décision de frontière séquentielle, jamais anticipative) — confirmant que `production_window = last_required_end + 1` suffit à prouver la complétude du dernier groupe `REQUIRED` sans conserver le groupe de marge entier dans la production, et que `SPECTRAL_MARGIN_POLICY = REUSE_1C4A` porte historiquement sur la seule suffisance du spectre exploratoire du préflight (jamais sur le contenu obligatoire de la production normative — vérifié directement dans `jbreak_spectral_preflight.py`). Verdicts finaux acceptés : `FULL_DENSE_EXPLORATORY_WINDOW=FREEZE` (aucune diagonalisation supplémentaire requise dans le chemin dense, `np.linalg.eigh` calculant déjà tout le spectre ; distingue rigoureusement `TARGET_NOT_IDENTIFIABLE` de `WINDOW_INSUFFICIENT`) ; `WINDOW_SELECTION_POLICY=FULL_SPECTRUM_DERIVED_PRODUCTION_WINDOW` ; `PRODUCTION_WINDOW_RULE=A` (avec repli `production_window=dimension` si `last_required_end==dimension`) ; `PRODUCTION_WINDOW_SCOPE=PER_CASE` (aucune égalité de fenêtre requise, ni pour `±δ`, ni inter-S — confirmé directement depuis `matching.py::SpectralGroupMatchKey`, qui ne porte aucun champ de fenêtre) ; `NUMERIC_WINDOW_FREEZE_POLICY=DERIVE_BY_PREREGISTERED_ALGORITHM` ; `ENERGY_PUBLIC_POLICY=INDICES_ONLY` ; `PREFLIGHT_INFORMATION_FIREWALL=DEFINED` (architecture à deux couches `INTERNAL_PREFLIGHT_COMPUTATION`/`PUBLIC_PREFLIGHT_REPORT`, avec identification concrète d'un risque de fuite par message d'exception de `validate_flavor_total_sum`) ; `PREFLIGHT_V23_POLICY=VALIDATE_WITHOUT_EXPOSING_VALUES` ; `PREFLIGHT_ARTIFACT_REUSE_FOR_NORMATIVE=NO` ; `PREFLIGHT_GLOBAL_POLICY=ALL_REQUIRED_CASES` (inchangé) ; `PREFLIGHT_IMPLEMENTATION_READY=CONDITIONAL` ; `GRID_PREFLIGHT_PASSED=NOT_EVALUATED` ; `J0_CAMPAIGN_READY=NO`. Politique pré-enregistrée documentairement par **1C-6g** ci-dessous.

## Lot 1C-6g — pré-enregistrement documentaire du design final des fenêtres et du firewall de préflight (ce commit)

Documentaire uniquement, aucune nouvelle science, aucune campagne, aucune diagonalisation. Inscrit dans `docs/levels/level1c/identifiability-preregistration.md` une nouvelle section « §18. Design final des fenêtres et firewall du préflight aveugle (1C-6f/1C-6g, gelé) » qui fige : `FULL_DENSE_EXPLORATORY_WINDOW=FROZEN` (`exploratory_window=full_spectrum_dimension` pour les 20 cas, tous dans le chemin dense) avec la nuance explicite de surcoût aval (jamais qualifié de nul) ; la distinction stricte `exploratory_window`/`production_window` ; `WINDOW_SELECTION_POLICY=FULL_SPECTRUM_DERIVED_PRODUCTION_WINDOW` et `NUMERIC_WINDOW_FREEZE_POLICY=DERIVE_BY_PREREGISTERED_ALGORITHM` (l'algorithme est gelé, pas un nombre) ; la déduplication des groupes `REQUIRED` et `last_required_end` ; `SPECTRAL_MARGIN_POLICY=REUSE_1C4A` réinterprétée explicitement comme critère du seul spectre exploratoire ; `PRODUCTION_WINDOW_RULE=A` avec sa justification et son cas limite ; `PRODUCTION_WINDOW_SCOPE=PER_CASE` et l'absence d'exigence d'égalité de fenêtre pour la symétrie `±δ`/l'inter-S ; le vocabulaire prudent `TARGET_NOT_IDENTIFIABLE`/`TARGET_ABSENT_IN_FULL_SPECTRUM`/`TARGET_SELECTION_AMBIGUOUS` (jamais « physiquement absente ») et la confirmation que `fundamental`/`first_excited`/`T_3_2`/`T_max` restent inchangés dans leur politique de sélection ; l'architecture du firewall d'aveuglement à deux couches, sa whitelist publique complète, `ENERGY_PUBLIC_POLICY=INDICES_ONLY`, `PREFLIGHT_V23_POLICY=VALIDATE_WITHOUT_EXPOSING_VALUES`, et les surfaces de fuite couvertes (exceptions, logs, repr, fichiers temporaires) ; le rôle minimal de `BRANCH_C1` en préflight et l'exigence documentée (non résolue) `R²=I` avec tolérance déjà gelée réutilisée ou audit séparé si aucune n'est applicable ; `TRACKING_PREFLIGHT_STATUS` distinct du vocabulaire de l'analyse finale ; `PREFLIGHT_GLOBAL_POLICY=ALL_REQUIRED_CASES` et `PREFLIGHT_ARTIFACT_REUSE_FOR_NORMATIVE=NO` ; l'architecture de code future (`scripts/level1c_preflight/j0_grid_preflight.py`, aucune nouvelle primitive scientifique, `matching.py` jamais modifié) et la liste des tests minimaux requis avant toute exécution ; et la liste explicite des points encore ouverts après ce lot.

Fichiers de ce lot : `docs/levels/level1c/identifiability-preregistration.md`, `docs/governance/current-task.md`.

## Lot 1C-6h (accepté définitivement, code/tests, commit `0afc99369411ed43bafdd619883194f73373dae1` + correctif `e37008c7947d3ce9ac8e6fd758fc2c4822664c4c`)

Implémentation de l'outil de préflight aveugle `J0×S` (`scripts/level1c_preflight/j0_grid_preflight.py` + `tests/scripts/level1c_preflight/test_j0_grid_preflight.py`) exactement conforme au contrat gelé (1C-6d..1C-6g) : grille des 20 cas, spectre exploratoire complet, `PRODUCTION_WINDOW_RULE=A`, portée `PER_CASE`, firewall d'information à deux couches, `V23` bool-only public, énergie `INDICES_ONLY`, agrégation `ALL_REQUIRED_CASES`. Aucune nouvelle primitive scientifique, `matching.py`/`target_selection.py`/manifeste jamais modifiés. **Correctif accepté** (`e37008c7...`) : la vérification `R²=I` de la réflexion, initialement testée sur l'opérateur global (défaut confirmé par audit ChatGPT — le carré d'un unitaire global de réflexion est trivialement l'identité, ce qui ne prouve rien sur son action une fois restreinte au groupe spectral sélectionné), corrigée pour tester `R_rest=Psi†R Psi` (réutilisation verbatim de `restricted.build_restricted_operator`, tolérance `automorphisms.UNITARITY_TOLERANCE` réutilisée verbatim). Aucun run des 20 cas dans ce lot.

## Lot 1C-6i (accepté définitivement, lecture seule, aucun commit propre)

Audit de readiness d'exécution du préflight au HEAD `e37008c7947d3ce9ac8e6fd758fc2c4822664c4c` : confirme `DENSE_PATH_ENFORCEMENT=IMPLICIT_BUT_SAFE`, `PREFLIGHT_EXECUTION_ATOMICITY=ACCEPTABLE`, mais identifie deux défauts **BLOCKING** avant toute exécution réelle : (1) `RUNTIME_FIREWALL_COMPLETE=NO` — plusieurs primitives déjà acceptées (`restricted._hermitian_aware_normalized_trace`, `matching.SymmetryLabel.__post_init__`) intègrent une valeur numérique physique brute dans leur propre message d'exception, appelées hors du seul chemin protégé (`validate_flavor_total_sum`) ; (2) `PREFLIGHT_PROVENANCE_READY=NO` — le rapport public ne portait aucun `code_commit`/`manifest_fingerprint`/version de contrat, rendant les futures fenêtres numériques non traçables. `PREFLIGHT_EXECUTION_READY=NO`. Correctifs minimaux proposés, jamais implémentés dans ce lot (lecture seule).

## Lot 1C-6j (accepté définitivement, code/tests, commit `b6cf68b772bc16ce71dc5032c677d23dd37ed827`)

Corrige les deux défauts bloquants de 1C-6i. `run_case` enveloppe désormais toute sa phase d'analyse réelle dans un unique `try` : `MemoryError` inchangé (`RESOURCE_BLOCKING`), toute autre exception (jamais `BaseException`) devient `PreflightInternalFailure` — message fixe et constant, `raise ... from None` (aucune cause brute, vérifié y compris sur le rendu de traceback standard et sur stdout/stderr). `PublicPreflightReport` porte désormais `PreflightProvenance` (`code_commit` via `git rev-parse HEAD` strictement validé 40 hex, jamais `"unknown"`/`"dirty"` ; `manifest_fingerprint` réutilisant `Manifest.fingerprint` verbatim ; `preflight_contract_version="level1c-j0-blind-preflight-v1"`), résolue avant le premier cas (`DIRTY_WORKTREE_POLICY=REFUSE_EXECUTION`, testé : `run_case` jamais appelé sur arbre sale). `RUNTIME_FIREWALL_COMPLETE=YES`. Tests d'intégration ajoutés avec spectre synthétique (seule `build_level0_report_with_eigenvectors` moquée) confirmant le câblage et l'absence de fuite. Aucun run des 20 cas dans ce lot.

## Lot 1C-6k (accepté définitivement, exécution unique réelle, aucun commit propre, artefact non versionné)

Exécution unique et réelle des 20 cas gelés au HEAD `b6cf68b772bc16ce71dc5032c677d23dd37ed827` (`python -m scripts.level1c_preflight.j0_grid_preflight --confirm-run-full-grid`), une seule invocation, aucune réexécution. Préconditions vérifiées avant run (HEAD, branche, arbre propre). Résultat : `stderr` vide, JSON public valide, 20 cas dans l'ordre exact sans doublon, dispatch dense 20/20, firewall public valide (aucune clé interdite), provenance interne cohérente (`code_commit` = HEAD, `manifest_fingerprint` = `Manifest.fingerprint` vérifié structurellement), `global_status = global_preflight_sufficient`. `PREFLIGHT_EXECUTION_RESULT = VALID_GLOBAL_SUFFICIENT`, `GRID_PREFLIGHT_PASSED = YES`. Artefact conservé localement, non commité (`results/` ignoré) : `results/level1c/preflight/j0_grid_preflight_b6cf68b772bc16ce71dc5032c677d23dd37ed827.json`, SHA-256 `a40a9c05a464ea584dabc69ef7246feb68bd7efa433508f1ff52cfe15a158146`, 52840 octets — audité indépendamment par ChatGPT. Politique pré-enregistrée documentairement par **1C-6l** ci-dessous.

## Lot 1C-6l — gel documentaire du résultat de préflight accepté et readiness de campagne (ce commit)

Documentaire uniquement, aucune nouvelle science, aucun run, aucun recalcul de fenêtre. Inscrit dans `docs/levels/level1c/identifiability-preregistration.md` une nouvelle section « §19. Gel du résultat de préflight accepté et readiness de campagne (1C-6h/1C-6l, gelé) » qui fige : le statut accepté de 1C-6h/1C-6i/1C-6j/1C-6k ; la référence d'artefact non versionnée (chemin, SHA-256, taille) ; la provenance (`code_commit`, `manifest_fingerprint`, `preflight_contract_version`) ; les distributions publiques acceptées (`resource_status`/`window_status`/`tracking_preflight_status`, 20/20 chacune sans exception, sans aucune conclusion physique) ; la signification stricte de `global_status=global_preflight_sufficient` (admissibilité structurelle uniquement, jamais un résultat physique) ; les dimensions et la table complète des 20 `production_window` dérivées par `PRODUCTION_WINDOW_RULE=A` (transcrites depuis 1C-6k, jamais recalculées), avec `PRODUCTION_WINDOW_SCOPE=PER_CASE` explicitement maintenu malgré les valeurs communes observées (baseline `J0=1`: triangle=9/ring5=15 ; points perturbés : triangle=5/ring5=11 ; symétrie `±δ` et égalité inter-S observées mais jamais normatives) ; la cohérence de marge (20/20), les targets (70/70 sélectionnées), V23 (50/50 `REQUIRED` valides), le firewall (valide), le tracking (`feasible` 20/20, jamais un verdict scientifique final) ; `LEVEL1C_J0_PREFLIGHT_STATUS=CLOSED_ACCEPTED` et `PREFLIGHT_ARTIFACT_REUSE_FOR_NORMATIVE=NO` (la future production doit être ré-exécutée sous provenance normative propre) ; `J0_CAMPAIGN_READY=YES` (readiness d'exécution de campagne, distincte de la readiness de décision physique) ; et la confirmation que `PHYSICAL_RESPONSE_THRESHOLD=OPEN`, `BRANCH_C2=NOT_DEFINED/deferred`, `FIXED_T_REQUIRED_FOR_FIRST_CAMPAIGN=NO` restent inchangés. Met à jour le pointeur « Dernier commit accepté » et documente l'historique des lots 1C-6h à 1C-6k (jamais documentés auparavant, leur scope de fichiers autorisé excluait `current-task.md`).

Fichiers de ce lot : `docs/levels/level1c/identifiability-preregistration.md`, `docs/governance/current-task.md`.

## Lot 1C-7a (accepté comme design de conception, lecture seule, aucun commit propre)

Design en lecture seule du contrat de production normative `J0×S` : séparation `P/T/R/G` ; unité d'artefact (`ONE_ARTIFACT_PER_CASE` + manifeste de campagne, modelé sur `scripts/level1b_campaign/outputs.py`) ; `C_TT_conn=REQUIRED`, `rho_QQ=REQUIRED`, `G=DEFERRED`, `flavor_singular_value_ratio=DEFERRED`, `Delta_C_TT=ANALYSIS_ONLY` ; baseline produite dans la même campagne ; recommandation d'un nouveau manifeste/schéma Level 1C dédié (le manifeste/schéma Level 1B `correlators-v2` ne peut exprimer la grille `J0` ni `production_window` par cas). Verdict ChatGPT : `SPECTRAL_STRUCTURE_PRODUCTION` révisé de la recommandation initiale (« Stratégie 1 : uniquement les groupes target ») vers `ALL_COMPLETE_GROUPS_IN_PRODUCTION_WINDOW` (tous les groupes complets de la fenêtre peuvent fournir des diagnostics structurels pour le tracking, même non-cibles) ; `PHYSICAL_OBSERVABLE_PRODUCTION=PREREGISTERED_TARGETS_ONLY` conservé tel que recommandé. Deux points scientifiques bloquants identifiés avant conception machine-readable : contrat procédural exact de tracking inter-J0, et politique exacte de non-régression entre la baseline Level 1C `J0=1` et la référence Level 1B.

## Lot 1C-7b (accepté comme audit intermédiaire, lecture seule, aucun commit propre)

Première fermeture, en lecture seule, du tracking inter-J0 et de la non-régression baseline. Acceptés : `INTER_J0_TRACKING_TOPOLOGY=BASELINE_CENTERED` (jamais un chaînage de proche en proche, cohérent avec `Delta_C_TT` intrinsèquement relatif à la baseline) ; contrat nécessaire et suffisant de `TRACKED_ONE_TO_ONE` ; `RESPONSE_ELIGIBILITY` orthogonal au statut de tracking (target category, branch identity et disponibilité d'observable jamais fusionnées). `TRACKED_SPLIT_BRANCH` proposé initialement dans une forme non acceptée (multiplicité réduite → split, multiplicité augmentée → discontinuous) — corrigée par 1C-7b2. Audit exhaustif des tolérances numériques déjà gelées du dépôt (`SYMMETRY_TOLERANCE`, `FLAVOR_LABEL_TOLERANCE`, `EXPECTATION_TOLERANCE`, etc.) : aucune n'est sémantiquement conçue pour comparer une observable physique recalculée de façon déterministe par un pipeline/commit différent à sa valeur historique — `NON_REGRESSION_TOLERANCE_STATUS=OPEN` confirmé, corrigeant une conclusion initialement trop rapide de ce même lot (qui proposait à tort de réutiliser une tolérance existante « sur la valeur »). `D018_REUSE_FOR_BASELINE_NON_REGRESSION=NO` établi (robustesse physique inter-S ≠ identité numérique de recalcul déterministe).

## Lot 1C-7b2 (accepté définitivement, lecture seule, aucun commit propre)

Fermeture définitive de la taxonomie de tracking v1, sur audit direct de ChatGPT. Corrige 1C-7b : `MULTIPLICITY_MISMATCH_V1=AMBIGUOUS` dans tous les cas (jamais smaller→split / larger→discontinuous selon le signe — le signe seul du mismatch ne prouve ni une daughter branch ni une incompatibilité dure) ; `DISCONTINUOUS=HARD_IDENTITY_INCOMPATIBILITY`, réservé théoriquement mais non émis mécaniquement en v1 (aucun chemin de l'algorithme de `class pool` ne peut le produire, `twice_T` étant un critère d'appartenance au pool) ; `TRACKED_SPLIT_BRANCH=RESERVED_BUT_NOT_EMITTABLE` (sans `BRANCH_C2`/`FIXED_T_SUBSPACE`, aucune décomposition quantitative n'est définissable) ; `NOT_AVAILABLE` pour toute absence propre dans `production_window`, jamais `DISCONTINUOUS` par simple limitation de fenêtre (la fenêtre garantit la suffisance pour les cibles `REQUIRED` sélectionnées par rang, jamais qu'une branche migrée y reste). `BRANCH_C1` confirmé classification-only, n'émettant jamais `TRACKED_SPLIT_BRANCH`. `INTER_J0_TRACKING_CONTRACT_READY=YES`.

## Lot 1C-7b3 (accepté définitivement, lecture seule, aucun commit propre)

Fermeture définitive de la méthodologie de calibration de non-régression, sans exécuter aucun calcul ni inventer de valeur. `T_max` (`J0=1`, géométries/spins de la campagne uniquement — triangle/ring5 × S∈{2,3}) confirmé comme jeu de calibration indépendant non circulaire, par vérification directe du manifeste Level 1B historique (`preregistered-manifest-v1.json` : `T_max` y produit les mêmes `single_case_observables` que `fundamental`/`first_excited`, absent de `REQUIRED_NON_REGRESSION`). Métriques `MAX_ABS` (`E_CTT`, `E_RHO`, jamais moyenne/RMS/quantile) ; politique de nullité stricte pour `rho_QQ` (désaccord de nullité historique/actuel = échec catégoriel, indépendant de toute tolérance) sans seuil d'exclusion arbitraire additionnel près de `NORMALIZATION_FLOOR` ; règle de dérivation déterministe `CEIL_DECADE_POLICY` (`TOL=10**ceil(log10(E))`, convention de gouvernance explicite, jamais une borne rigoureuse ni un facteur de sécurité inventé — corrige les propositions `PROPOSED_FIXED_FLOORS`/`PROPOSED_SAFETY_FACTOR_10` non acceptées de 1C-7b2, en éliminant le besoin plutôt qu'en re-justifiant les mêmes nombres) ; cas `E=0` traité par une référence d'epsilon machine à l'échelle de représentation (`1e-15`), explicitement jamais confondue avec une précision de solveur. Couplage strict à l'environnement d'exécution (`CALIBRATION_ENVIRONMENT=NORMATIVE_CAMPAIGN_ENVIRONMENT`, tout changement d'environnement impose une recalibration, sans notion de changement « suffisamment petit »). `NON_REGRESSION_CALIBRATION_CONTRACT_READY=YES`.

## Lot 1C-7c — gel documentaire du contrat de production normative, du tracking v1 et de la calibration de non-régression (commit `484d0100e71e3e8478091569afbc533c640332a0`, non accepté définitivement en l'état)

Documentaire uniquement, aucun code, aucun test, aucun schéma, aucun manifeste machine-readable, aucun outil de calibration, aucun run, aucune diagonalisation, aucune campagne. Inscrit dans `docs/levels/level1c/identifiability-preregistration.md` une nouvelle section « §20 » qui fige l'intégralité des décisions acceptées de 1C-7a/1C-7b/1C-7b2/1C-7b3 : séparation des phases `P/T/R/G` (`R`/`G` ne modifient jamais `P`) ; ensemble normatif des 20 cas (`geometry∈{triangle,ring5}`, `S∈{2,3}`, `J0∈{0.5,0.75,1.0,1.25,1.5}`) ; réutilisation des `production_window` déjà gelées en §19.8 comme INPUT en production (aucune redérivation, aucun deepening) ; unité d'artefact par cas + manifeste de campagne (forme JSON différée à 1C-7d) ; séparation structure (`ALL_COMPLETE_GROUPS_IN_PRODUCTION_WINDOW`) / observables physiques (`PREREGISTERED_TARGETS_ONLY`) et diagnostics structurels minimaux par groupe ; `C_TT_conn`/`rho_QQ` REQUIRED, `G`/`flavor_singular_value_ratio` DEFERRED ; targets et distinction `TARGET_PRODUCED`/`TARGET_PRIMARY_ANALYSIS`/`TARGET_CALIBRATION_ONLY` ; baseline `RECOMPUTE_COMPLETE_BASELINE` dans la même campagne ; topologie de tracking `BASELINE_CENTERED`, direction unique baseline→perturbed ; composants d'identité requis, `class pool`, taxonomie de tracking v1 complète et mutuellement exclusive (`TRACKED_ONE_TO_ONE`/`AMBIGUOUS`/`NOT_AVAILABLE`/`DISCONTINUOUS` réservé non émis/`TRACKED_SPLIT_BRANCH` réservé non émis) ; `RESPONSE_ELIGIBILITY` orthogonal, `Delta_C_TT` analysis-only, incident/non-incident et localisation blind hors `PHASE_P` ; rôle inter-S `ROBUSTNESS_ONLY` et son ordre (inter-J0 par S d'abord, inter-S ensuite) ; gate de non-régression baseline (périmètre `REQUIRED`, sémantique d'égalité, `D018` rejeté, politique d'échec préservant les artefacts) ; nature des tolérances (garde de reproductibilité empirique, jamais un seuil physique ni une borne rigoureuse) ; deux tolérances distinctes `PENDING_CALIBRATION` ; méthodologie de calibration complète (`CALIBRATION_SET`, métriques `MAX_ABS`, politique de nullité, `CEIL_DECADE_POLICY`, règle du cas zéro, absence de floor/safety factor inventés) ; environnement normatif et provenance de calibration (forme différée) ; politique d'exécution `SINGLE_ACCEPTED_RUN` et enchaînement des lots futurs ; politique d'échec de calibration ; `PHYSICAL_RESPONSE_THRESHOLD` toujours `OPEN`, ne bloquant ni `P` ni `T` ; statuts de readiness récapitulatifs. `J0_CAMPAIGN_READY=YES` n'est pas rétrogradé. Met à jour le pointeur « Dernier commit accepté ».

Fichiers de ce lot : `docs/levels/level1c/identifiability-preregistration.md`, `docs/governance/current-task.md`.

**Audit ChatGPT sur 1C-7c** : `NON ACCEPTÉ DÉFINITIVEMENT` — correctif documentaire minimal requis. L'intégralité du gel documentaire est acceptée SAUF une formulation techniquement incorrecte en §20.22 (`identifiability-preregistration.md`) : `FLOAT64_ZERO_ERROR_REFERENCE` y était décrit comme « la plus petite différence non nulle qu'une soustraction float64 de deux quantités d'échelle O(1) puisse produire », ce qui est faux — l'espacement des flottants float64 dépend de la valeur/de l'exposant considéré, ce n'est pas une constante unique. Le contrat numérique lui-même (`CEIL_DECADE_POLICY`, la règle `E>0`/`E=0`, la valeur dérivée `1e-15`) n'était pas mis en cause. Voir « Lot 1C-7c-fix » ci-dessous.

## Lot 1C-7c-fix — correction documentaire minimale de la justification float64 epsilon (ce commit)

Corrige l'unique formulation techniquement incorrecte identifiée par l'audit ChatGPT sur 1C-7c, en §20.22 de `docs/levels/level1c/identifiability-preregistration.md`. `FLOAT64_ZERO_ERROR_REFERENCE` n'est plus présenté comme une borne sur l'espacement des flottants (« la plus petite différence non nulle qu'une soustraction float64 de deux quantités d'échelle O(1) puisse produire » — incorrect, cet espacement dépend de la valeur/de l'exposant considéré) ; la nouvelle formulation dit exactement : `FLOAT64_ZERO_ERROR_REFERENCE` fournit uniquement une échelle représentationnelle canonique (epsilon machine autour de l'unité) utilisée par la convention de gouvernance quand `E_observed = 0`, et ne constitue NI une borne sur la plus petite différence non nulle possible entre deux valeurs float64, NI une borne sur l'erreur de diagonalisation, NI une borne sur la reproductibilité numérique du pipeline.

Aucune autre décision n'est modifiée : `CEIL_DECADE_POLICY`, la règle `si E>0 : TOL=10**ceil(log10(E))` / `si E=0 : TOL=10**ceil(log10(FLOAT64_ZERO_ERROR_REFERENCE))`, la valeur dérivée `1e-15`, et sa nature (`empirical/governance convention`, jamais une borne de solveur, jamais une borne d'erreur numérique rigoureuse, jamais un seuil physique) restent inchangées. Aucun code, aucun test, aucun schéma, aucun manifeste, aucun outil de calibration, aucun run, aucune diagonalisation, aucun calcul. Le statut de 1C-7c lui-même n'est pas réécrit comme accepté définitivement par ce lot : ce correctif porte uniquement sur la formulation de §20.22 ; l'acceptation définitive de l'ensemble `484d010` + ce commit reste soumise au prochain audit ChatGPT. Le pointeur « Dernier commit accepté » reste à `fd74dffca904c89038559465d18ead65af468073` (parent exact de `484d010`, lui-même non encore accepté définitivement).

Fichiers de ce lot : `docs/levels/level1c/identifiability-preregistration.md`, `docs/governance/current-task.md`.

## Lot 1C-7c2 — implémentation de l'outil de calibration T_max (code + tests, aucune exécution réelle, ce commit)

Implémente `scripts/level1c_calibration/tmax_nonregression.py` (~830 lignes) + `tests/scripts/level1c_calibration/test_tmax_nonregression.py` (75 tests, entièrement synthétiques/unitaires) conformément au contrat gelé en §20.22. L'outil compare un enregistrement historique Level 1B `T_max` (fourni explicitement par l'opérateur — aucune découverte par glob/mtime/« latest ») à un recalcul Level 1C `J0=1` frais, et dérive `NON_REGRESSION_CTT_ABS_TOL`/`NON_REGRESSION_RHO_ABS_TOL` via `CEIL_DECADE_POLICY`.

**Constat déterminant, découvert par audit direct du schéma/de `results.py`/`serialization.py`** : un enregistrement Level 1B v2 ne porte **aucun champ `target_id`** — son identité est purement structurelle (`geometry`, `hamiltonian`, `sector`, `spectral_group{status,multiplicity,twice_T,spectral_window_group_index,representative_energy}`, `path`). Reconstruire « quel enregistrement historique est `T_max` » à partir du seul `twice_T` n'est donc pas garanti non ambigu (un autre target sélectionné pourrait coïncidentellement partager le même `twice_T`). Conformément au mandat (« si l'extraction n'est pas non ambiguë : STOP, ne pas inventer un nouveau mécanisme »), l'outil **exige un sélecteur explicite** (`HistoricalGroupSelector{spectral_window_group_index,twice_T,multiplicity}`), fourni par l'opérateur via un fichier JSON de configuration historique explicite (`HistoricalCalibrationManifest`), jamais deviné — l'outil se contente de le **valider** (cohérence interne du corpus, `twice_T` du sélecteur concordant avec le `target_twice_T` déjà gelé du manifeste pour `T_max`), jamais de le dériver.

Réutilisation stricte des primitives déjà acceptées, aucune réimplémentation scientifique : `experiments.level1.target_selection.select_target_group` (recalcul courant, aucune nouvelle règle de sélection) ; `cosmobox.level1.results.build_spectral_group_identity` (identité structurelle du groupe courant, même constructeur que le runner Level 1B) ; `cosmobox.level1.matching.symmetry_labels_match`/`SYMMETRY_TOLERANCE` (comparaison des labels de translation/réflexion, jamais une tolérance inventée) ; `scripts.level1b_campaign.outputs.load_case_records` (lecture du `records.jsonl` historique, aucun second parseur JSONL) ; `scripts.level1c_preflight.j0_grid_preflight.resolve_code_commit`/`require_clean_worktree` (provenance git, réutilisées et enveloppées sous le nom propre `CalibrationInternalFailure` de cet outil, jamais une exception « Preflight » qui fuiterait vers un outil différent). `production_window` baseline (`triangle→9`, `ring5→15`) réutilisée comme **INPUT gelé** (§19.8/§20.4), jamais redérivée.

Contrat implémenté fidèlement : `CALIBRATION_METRIC_CTT`/`CALIBRATION_METRIC_RHO` = `MAX_ABS` uniquement (jamais moyenne/RMS/quantile) sur les paires hors-diagonale communes aux deux corpus ; `NULLITY_POLICY` stricte (désaccord de nullité, ou accord de nullité avec `null_reason` différent, → `CALIBRATION_FAIL` catégoriel, jamais absorbé dans la métrique numérique) ; absence de `rho_QQ` numérique sur l'ensemble de la calibration → statut distinct `NO_NUMERIC_RHO_CALIBRATION_RECORD`, aucune tolérance `rho` inventée ; comparaison structurelle stricte AVANT tout calcul numérique (`spectral_group.status`, `twice_T`, `multiplicity`, labels de translation/réflexion — chaque défaut de correspondance produit `CASE_STRUCTURAL_MISMATCH`, jamais ignoré silencieusement) ; disponibilité minimale « au moins un cas utilisable par géométrie », sinon échec avec raison explicite ; `derive_absolute_tolerance` = `CEIL_DECADE_POLICY` pure (`10**ceil(log10(E))`, cas `E=0` → `10**ceil(log10(float64_eps))=1e-15` comme convention de gouvernance à l'échelle de représentation, jamais une borne de solveur), robuste aux puissances de dix exactes (arrondi du `log10` avant `ceil`), sans floor ni safety factor. Fingerprint d'environnement (`python_version`/`numpy_version`/`scipy_version`/identité BLAS-LAPACK via `numpy.show_config(mode="dicts")`/`platform`/`architecture`) canonique et déterministe, comparaison exacte (aucune notion de version « suffisamment proche »). Artefact public (`CalibrationArtifact`) structurellement incapable de porter une valeur brute historique/actuelle, une énergie, ou une matrice complète (seuls comptes/statuts/`E_CTT`/`E_RHO`/tolérances dérivées/provenance/hashes) — vérifié par test. `CALIBRATION_RUN_POLICY=SINGLE_ACCEPTED_RUN` : CLI sûre par défaut (`--help`/absence de `--confirm-run-calibration` → aucun calcul, code de sortie 0), la calibration réelle exige `--confirm-run-calibration` — jamais passé dans ce lot.

**Déviation mineure, documentée, par rapport à la liste de fichiers proposée par le mandat** : `tests/scripts/level1c_calibration/__init__.py` n'a pas été créé — aucun sous-répertoire de `tests/scripts/` n'utilise `__init__.py` dans ce dépôt (collecte pytest par chemin, `pythonpath=["."]`, confirmé par inspection directe) ; en créer un aurait été incohérent avec la convention déjà établie. `scripts/level1c_calibration/__init__.py` a en revanche été créé, à l'identique du patron déjà établi par `scripts/level1c_preflight/__init__.py`.

Tests : 75 tests entièrement synthétiques (dérivation de tolérance y compris puissances de dix exactes et cas `E=0` ; JSON canonique ; fingerprint d'environnement et son égalité stricte ; chargement du manifeste de calibration historique explicite et tous ses refus ; extraction des enregistrements historiques et ses refus (doublon, désaccord de statut) ; `compare_case` (tous les mismatches structurels, toutes les combinaisons de nullité `rho_QQ`) ; agrégation `MAX_ABS` et disponibilité par géométrie ; sécurité CLI ; provenance git (arbre sale, SHA invalide, désaccord de SHA-256 historique, désaccord de `manifest_fingerprint` — refusé avant tout appel git) ; firewall (exception interne contenant une valeur fictive « secrète », jamais présente dans le message, la cause, ni le rendu de traceback) ; câblage complet de `run_calibration` de bout en bout avec `compute_current_tmax_case` (seul point de diagonalisation réelle) moqué par des données synthétiques contrôlées, confirmant que `E_CTT`/`E_RHO`/les tolérances dérivées sont exactement celles attendues et qu'aucune valeur brute ni aucun chemin de fichier temporaire ne fuite dans l'artefact public). Aucun test ne déclenche de diagonalisation triangle/ring5 réelle, aucun test n'invoque `--confirm-run-calibration`, aucun test ne lit un artefact Level 1B réel. Résultats observés par Claude (non exécutés par ChatGPT) : `tests/scripts/level1c_calibration` = 75 passed ; `tests/scripts/level1c_calibration + tests/scripts/level1c_preflight + tests/level1` = 708 passed ; suite complète du dépôt = 1738 passed, ~45s.

Fichiers de ce lot : `scripts/level1c_calibration/__init__.py` (nouveau), `scripts/level1c_calibration/tmax_nonregression.py` (nouveau), `tests/scripts/level1c_calibration/test_tmax_nonregression.py` (nouveau), `docs/governance/current-task.md`. Aucun autre fichier modifié : aucun `src/cosmobox/*`, aucun `experiments/level1/*`, aucun schéma, aucun manifeste Level 1B, aucun document scientifique Level 1C.

**Verdict ChatGPT sur ce commit (`e98950f...`)** : `NON ACCEPTÉ`. Trois blocages identifiés : `BLOCKER_1` (`CEIL_DECADE_POLICY` peut produire `TOL<E` à cause de `round(log10(E),9)`) ; `BLOCKER_2` (statut `SUCCESS` possible sans valeur `rho_QQ` numérique, donc sans `NON_REGRESSION_RHO_ABS_TOL`) ; `BLOCKER_3` (`HistoricalGroupSelector` fourni librement par l'opérateur sans provenance historique gelée prouvant qu'il s'agit réellement du `T_max` sélectionné en Level 1B). Ce commit reste `e98950fc92f2e55ebc4aca3e0bbd9906fe6f17de` = implémentation initiale 1C-7c2, non acceptée, sera corrigée/subsumée par un futur `1C-7c2c` (voir ci-dessous).

## Lot 1C-7c2-fix — audit read-only de blocage (aucun commit propre)

Lot ouvert pour corriger `BLOCKER_1`/`BLOCKER_2` et fermer `BLOCKER_3`, conditionné à la fermeture effective de ce dernier. Audit exhaustif read-only de la provenance historique `T_max` : `results/`, `scripts/level1b_campaign/*.py`, `scripts/level1b_analysis/*.py`, `experiments/level1/*.py`, `docs/` — confirme que la campagne Level1B n'a **jamais** persisté `target_id → spectral_window_group_index`, par choix d'architecture déjà gelé et documenté (D022, `scripts/level1b_analysis/indexing.py` : « never energy rank, never target_id, which is never reconstructed anywhere in this module » ; `IndexedSpectralGroup` : « Never carries a target_id »). Découverte supplémentaire : `records.jsonl` ne contient de records physiques QUE pour les groupes effectivement `SELECTED` par au moins une target (`runner.py::run_single_case`, `already_produced_group_indices`) — les candidats concurrents non sélectionnés ne laissent aucune trace, rendant impossible toute vérification a posteriori (sans nouvelle diagonalisation) que le candidat persisté était bien l'unique minimum d'énergie requis par la règle `flavor_label`. `HISTORICAL_TMAX_PROVENANCE_READY = NO`. Conformément au mandat, **STOP sans commit** — rapport read-only uniquement, aucune modification de fichier. Accepté comme audit de blocage.

## Lot 1C-7c2a — redesign read-only du hold-out (aucun commit propre)

Détermine si un hold-out de calibration indépendant de `target_id` est constructible uniquement à partir de faits persistés. Réponse : `CONDITIONAL`, via un changement de nature de la règle d'inclusion — d'une règle sémantique (« c'est le groupe que `T_max` a sélectionné ») vers une règle purement structurelle (« c'est un groupe complet, bien formé, aux observables comparables persistés, dans un cas hors grille normative Level1C »), jamais dépendante de savoir *pourquoi* un groupe a été persisté. Audite et classe les options de cas candidats : `triangle S=1`/`ring5 S=1` (retenu, primaire) ; `ring4 S∈{1,2,3}` (retenu, supplémentaire, comble le vide dimensionnel 48→496) ; `j_break` (rejeté, conflit avec la grille normative `J0=1.5` et confusion reproductibilité/réponse physique) ; groupes non-`REQUIRED` au sein des cas baseline `S∈{2,3}` (rejeté, même impossibilité structurelle que `T_max`, plus circularité de cas). Établit `spectral_window_group_index` comme composant d'identité **requis** ici (contrairement au tracking inter-J0), puisque le Hamiltonien est ici rigoureusement identique — un désaccord d'indice est un signal de non-reproductibilité réel, jamais un phénomène physique bénin. Établit explicitement `representative_energy` comme exclue de toute identité (circularité : utiliser la quantité dont on teste la reproductibilité comme précondition de comparaison). Documente une limite honnête : couverture dimensionnelle maximale 496, aucun point disjoint de la grille normative n'atteignant 1000/1504. Conclut `DOES_THIS_REQUIRE_DOCUMENTARY_AMENDMENT = YES`. Accepté conceptuellement, en lecture seule, sans gel documentaire (réservé à 1C-7c2b).

## Lot 1C-7c2a2 — audit read-only d'existence/validité des artefacts historiques (aucun commit propre)

Découverte majeure, absente des audits précédents (qui n'avaient cherché que sous `results/` et dans le dépôt git lui-même) : un répertoire de sortie de campagne Level1B réel et persistant existe **hors du dépôt**, à `/workspaces/level1b_campaign_output/runs/`, contenant les 11 cas de l'exécution normative 1B-8g déjà documentée (`repository_commit=0ff65ac66b4aa054f739b350cd384c26ecd19752`, `campaign_id=level1b-reference-v1`, `manifest_fingerprint=159660cac738518dc620b9627ec95fd67c5dbc283707fdf72e572886364693ab` — confirmé identique à `load_manifest().fingerprint` actuel). Vérification directe, lecture seule, réutilisant exclusivement les chargeurs déjà acceptés (`scripts.level1b_analysis.loader.load_validated_cases`, `scripts.level1b_analysis.indexing.build_campaign_artifact_index`, aucun nouveau parseur) pour les cinq cas requis (`triangle-S1`, `ring4-S1/S2/S3`, `ring5-S1`, tous `hamiltonian_case_id=reference`) : `run.json` valides (`run_status=success`, `errors=[]`, hash `records_sha256` vérifié octet pour octet contre `records.jsonl` recalculé — 5/5 et 11/11 correspondent) ; schéma v2 validé sans erreur pour les 11 cas ; index de 27 groupes construit sans lever d'exception (`SpectralGroupIndexError`/`UnresolvedFlavorLabelError`) ; 11 groupes éligibles au filtre structurel (`complete_multiplet` + `twice_T` résolu + labels `numeric` + jeux de paires `C_TT_conn`/`rho_QQ` exactement complets `n*(n-1)`) sur les 5 cas cibles, aucun rejeté ; au moins une valeur `rho_QQ` numérique par cas ; toutes les valeurs non-null finies. `HOLDOUT_ARTIFACT_SET_READY = YES`. Accepté en lecture seule.

## Lot 1C-7c2b — amendement documentaire du hold-out de calibration (ce commit)

Documentaire uniquement, aucun code, aucun test, aucun run, aucune diagonalisation, aucune calibration, aucun artefact nouveau, aucun schéma, aucun manifeste machine-readable. Remplace, dans `docs/levels/level1c/identifiability-preregistration.md` §20.22, le hold-out `CALIBRATION_SET=T_max` (marqué `[HISTORIQUE — SUPERSEDED BY 1C-7c2b]`, jamais effacé, avec la raison exacte de sa supersession) par le contrat `TARGET_ID_FREE_PERSISTED_GROUP_HOLDOUT` entièrement audité et vérifié par 1C-7c2a/1C-7c2a2 : `CALIBRATION_CASE_FILTER` (exactement `triangle S=1`, `ring4 S=1/S2/S3`, `ring5 S=1`, tous `reference`, `ALL_5_HOLDOUT_CASES_REQUIRED=YES`, aucun fallback) ; `j_break` et `triangle/ring5 S∈{2,3}` explicitement exclus avec leur raison ; `CALIBRATION_GROUP_FILTER` purement structurel (jamais `target_id`) ; `CURRENT_MATCH` incluant `spectral_window_group_index` comme composant requis (distinction explicite avec le tracking inter-J0, où il n'est jamais une identité) et excluant `representative_energy` ; fenêtres/tolérance de dégénérescence/garde-fous historiques repris à l'identique (jamais un deepening) ; métriques `MAX_ABS`/politique de nullité/égalité stricte des jeux de paires/rejet des valeurs non finies/exigence `NUMERIC` des labels de symétrie réaffirmés ; distinction `operational location != normative identity` pour `/workspaces/level1b_campaign_output/` (jamais une exigence scientifique, jamais un chemin figé) ; identité de provenance historique attendue (`campaign_id`/`manifest_fingerprint`/`repository_commit`/`records_sha256` par cas) ; environnement historique `UNAVAILABLE` documenté comme limite non bloquante ; complétude exigée du fingerprint d'environnement courant ; artefact `FAIL` → aucune tolérance publiée ; limite de couverture dimensionnelle (48-496 contre 1000-1504) explicitement disclosée, jamais dissimulée ; précision d'implémentation future sur `CEIL_DECADE_POLICY` (`TOL>=E` garanti, jamais via `round(log10(E),N)`) sans recoder le contrat lui-même. Ajoute une nouvelle section « §20.30 » figeant les statuts de readiness après ce remplacement (`TARGET_ID_FREE_HOLDOUT_CONTRACT_READY=YES`, `HOLDOUT_ARTIFACT_SET_READY=YES`, `CALIBRATION_RUN_READY=NO`, tolérances toujours `PENDING_CALIBRATION`). Ne modifie aucune autre décision Level1C déjà gelée (P/T/R/G, grille 20 cas, `production_window`, tracking, `RESPONSE_ELIGIBILITY`, inter-S, `C_TT_conn`/`rho_QQ` REQUIRED, `G`/`flavor_singular_value_ratio` DEFERRED, `PHYSICAL_RESPONSE_THRESHOLD`). `e98950fc92f2e55ebc4aca3e0bbd9906fe6f17de` reste non accepté, sera corrigé/subsumé par un futur `1C-7c2c` (code + tests, implémentant ce nouveau contrat, sans exécution réelle).

Fichiers de ce lot : `docs/levels/level1c/identifiability-preregistration.md`, `docs/governance/current-task.md`.

**Verdict ChatGPT sur ce commit (`ca79fcb...`)** : `NON ACCEPTÉ DÉFINITIVEMENT` — correctif documentaire minimal requis. Tout le contrat `TARGET_ID_FREE_PERSISTED_GROUP_HOLDOUT` est conforme et ne doit pas être rouvert ; deux défauts identifiés : critère d'échec `T_max absent pour une géométrie entière` resté obsolète en §20.26 (appartenait exclusivement au hold-out historique désormais superseded) ; ordre de numérotation des sections §20.28/§20.30/§20.29 (`20.30` apparaissait physiquement avant `20.29`). Voir « Lot 1C-7c2b-fix » ci-dessous.

## Lot 1C-7c2b-fix — correction documentaire finale du contrat de hold-out (ce commit)

Documentaire uniquement, aucun code, aucun test, aucun run, aucune diagonalisation, aucune calibration, aucun schéma, aucun manifeste, aucun artefact. Corrige exactement les deux défauts identifiés par l'audit ChatGPT sur 1C-7c2b, sans rouvrir aucune autre partie du contrat : en §20.26 (`docs/levels/level1c/identifiability-preregistration.md`), remplace le critère obsolète `T_max absent pour une géométrie entière` par une synthèse explicite des règles déjà gelées en §20.22 pour le hold-out `TARGET_ID_FREE_PERSISTED_GROUP_HOLDOUT` (désaccord de nullité `rho_QQ`, désaccord structurel `CURRENT_MATCH`, absence/invalidité/inutilisabilité d'un des cinq cas requis, absence de groupe éligible dans un cas requis, absence de record `C_TT_conn`/`rho_QQ` numérique comparable global, non-égalité des jeux de paires, valeur non finie, artefact/provenance historique non conforme, fingerprint d'environnement courant incomplet — `ALL_5_HOLDOUT_CASES_REQUIRED=YES` réaffirmé, `FAIL` → les deux tolérances dérivées explicitement `None`) ; réordonne physiquement les sections `§20.28 Readiness après 1C-7c` / `§20.29 Périmètre non ouvert par ce document` / `§20.30 Readiness après 1C-7c2b` en ordre strictement croissant (aucun renommage, aucun changement de contenu substantiel, seule la position physique du bloc `§20.30` change). Aucune référence normative résiduelle à `T_max` ne subsiste hors des marqueurs `[HISTORIQUE — SUPERSEDED BY 1C-7c2b]` déjà posés par 1C-7c2b (vérifié explicitement). Aucune autre décision du contrat (`CALIBRATION_CASE_FILTER`, `CALIBRATION_GROUP_FILTER`, `CURRENT_MATCH`, identité de provenance historique, `MAX_ABS`, `CEIL_DECADE_POLICY`, `SINGLE_ACCEPTED_RUN`, couverture dimensionnelle, P/T/R/G, grille normative, tracking, `PHYSICAL_RESPONSE_THRESHOLD`) n'est modifiée. `e98950fc92f2e55ebc4aca3e0bbd9906fe6f17de` reste non accepté, sera corrigé/subsumé par un futur `1C-7c2c`.

Fichiers de ce lot : `docs/levels/level1c/identifiability-preregistration.md`, `docs/governance/current-task.md`.

**1C-7c2b + 1C-7c2b-fix acceptés définitivement ensemble** (commits `ca79fcb1234d79dd4527c984deeac63bb1547e5d` + `35881d7694e26c458a2cee1793e70ab1aa6a8629`). État gelé : `TARGET_ID_FREE_HOLDOUT_CONTRACT_READY=YES`, `HOLDOUT_ARTIFACT_SET_READY=YES`, `CALIBRATION_RUN_READY=NO`, `NON_REGRESSION_CTT_ABS_TOL=PENDING_CALIBRATION`, `NON_REGRESSION_RHO_ABS_TOL=PENDING_CALIBRATION`, `NON_REGRESSION_TOLERANCE_STATUS=NEEDS_CALIBRATION`. `e98950fc92f2e55ebc4aca3e0bbd9906fe6f17de` (implémentation initiale 1C-7c2, `T_max`-based) reste **non accepté** ; il est corrigé/subsumé par `1C-7c2c` ci-dessous, jamais rétroactivement réhabilité.

## Lot 1C-7c2c — correction/durcissement de l'outil de calibration + adaptation au hold-out target-id-free (ce commit)

Réécrit intégralement `scripts/level1c_calibration/tmax_nonregression.py` (le nom de fichier est conservé — voir la recommandation de renommage ci-dessous, non appliquée dans ce lot) et `tests/scripts/level1c_calibration/test_tmax_nonregression.py` (85 tests, entièrement synthétiques) pour se conformer exactement au contrat gelé en §20.22/§20.26 (1C-7c2a/1C-7c2a2/1C-7c2b/1C-7c2b-fix). Aucune calibration réelle exécutée, aucune diagonalisation réelle, aucun `--confirm-run-calibration`.

**Architecture `T_max` entièrement retirée** : `HistoricalGroupSelector`, `HistoricalCalibrationManifest`, tout `target_id`/`target_twice_T`, et `experiments.level1.target_selection.select_target_group` ont disparu de l'outil — vérifié explicitement par tests (absence d'attribut, absence d'import fonctionnel, aucun flag CLI `--group-index`/`--twice-T`/`--target-id`/`--historical-manifest`). Le nom de cible `T_max` ne subsiste plus que dans les docstrings expliquant pourquoi cette architecture a été abandonnée.

**`CALIBRATION_CASE_FILTER`** : les cinq cas exacts sont désormais sélectionnés via `experiments.level1.planning.build_campaign_plan(load_manifest())`, jamais reconstruits à la main — `select_required_cases` filtre le plan complet (11 cas) sur `REQUIRED_CASE_KEYS = {(triangle,1,reference), (ring4,1,reference), (ring4,2,reference), (ring4,3,reference), (ring5,1,reference)}` et lève `CalibrationConfigError` avant tout calcul si l'un des cinq manque. `Hamiltonian`/`spectrum_options` (fenêtre spectrale historique, tolérance de dégénérescence, garde-fous) proviennent directement du `CampaignCaseSpec` retourné par le plan — jamais dupliqués à la main. Vérifié contre le manifeste réel : les cinq `case_id` dérivés correspondent exactement aux répertoires réellement trouvés en 1C-7c2a2 sous `/workspaces/level1b_campaign_output/`.

**Réutilisation stricte** : `scripts.level1b_analysis.indexing.build_campaign_artifact_index` (et son `load_validated_cases` interne) chargent et valident les 11 cas du plan complet en une fois (schéma v2, provenance, identité de cas) — jamais un second parseur JSONL ; accepté comme lecture-seule même si seuls 5 des 11 cas indexés sont effectivement utilisés (conformément au mandat). `EXPECTED_HISTORICAL_REPOSITORY_COMMIT=0ff65ac66b4aa054f739b350cd384c26ecd19752` est le seul provenance-constant à coder en dur (jamais `campaign_id`/`manifest_fingerprint`, lus directement depuis le même objet `Manifest` utilisé pour construire le plan, donc jamais susceptibles de diverger silencieusement) — passé comme paramètre `repository_commit` à `build_campaign_artifact_index`, qui le cross-vérifie déjà contre chaque document historique.

**`CALIBRATION_GROUP_FILTER`** implémenté exactement (`is_group_eligible` : `complete_multiplet`, `twice_T` résolu, labels `translation`/`reflection` `NUMERIC`, jeux de paires `C_TT_conn`/`rho_QQ` hors-diagonale strictement complets via `set(...) == frozenset(build_ordered_pairs(n_nodes))`) — jamais `target_id`, jamais une magnitude d'observable, jamais un rang énergétique. Vérifié en lecture seule contre le vrai répertoire historique `/workspaces/level1b_campaign_output/` (hors suite de tests automatisée, non portable) : 11 groupes éligibles retrouvés sur les 5 cas requis (2+2+2+2+3), résultat identique à celui déjà audité en 1C-7c2a2.

**`CURRENT_MATCH`** implémenté par lookup direct à l'index `spectral_window_group_index` historique (jamais une recherche d'un groupe alternatif) : `multiplicity`, `twice_T`, labels de translation/réflexion via `symmetry_labels_match` — `representative_energy` n'existe même pas comme champ de `CurrentGroupResult`. Tout défaut de correspondance échoue toute la calibration (`GroupComparison` non-`ok`), jamais une exclusion silencieuse d'un seul groupe.

**Égalité stricte des jeux de paires** corrigée (`compare_group` compare `set(current.ctt) == set(historical_ctt)` et de même pour `rho`, jamais une intersection) ; **rejet des valeurs non finies** avant tout calcul de différence ; **politique de nullité** inchangée (désaccord → échec catégoriel, `null_reason` différent → échec) ; **`E_CTT`/`E_RHO` numériques tous deux requis** pour `SUCCESS` (`aggregate_metrics` + vérification explicite dans `run_calibration`) ; **`FAIL` → les deux tolérances dérivées explicitement `None`**, même si des métriques diagnostiques `E_CTT`/`E_RHO` restent renseignées.

**`CEIL_DECADE_POLICY` corrigée** (`BLOCKER_1` de l'audit initial 1C-7c2) : `derive_absolute_tolerance` n'utilise plus `round(log10(E), N)` — calcule `floor(log10(x))`, promeut une décennie si le candidat reste `< x`, puis vérifie explicitement l'invariant `TOL >= E` (levant `CalibrationInternalFailure` dans le cas — mathématiquement inatteignable — où même la promotion échouerait). Testé explicitement sur `nextafter(1e-12, inf) -> 1e-11`, `1e-12*(1+1e-10) -> 1e-11`, `nextafter(1e-10, inf) -> 1e-9`, plus une vérification `TOL>=E` sur 500 valeurs aléatoires et sur les bornes de plusieurs décennies.

**Complétude de l'environnement durcie** (`environment_fingerprint_is_complete`) : un champ `unavailable`/vide (y compris BLAS/LAPACK) fait désormais échouer la calibration (`CALIBRATION_FAIL`), jamais accepté comme normatif silencieusement.

**Déviation documentée, non appliquée** : le nom de fichier `tmax_nonregression.py` est désormais trompeur (l'outil n'implémente plus aucun hold-out `T_max`) — recommandation de renommage (par exemple vers `structural_holdout_nonregression.py`) rapportée pour un futur lot dédié, jamais effectuée dans ce lot (hors périmètre explicite du mandat).

Tests (85, entièrement synthétiques, aucune diagonalisation réelle, aucun `--confirm-run-calibration`) : dérivation de tolérance (puissances de dix exactes, `nextafter`, invariant `TOL>=E` sur 500 valeurs aléatoires et sur plusieurs décennies, `E=0`) ; JSON canonique ; fingerprint d'environnement et sa complétude (chaque champ testé pour l'incomplétude) ; `select_required_cases` (5 cas exacts contre le manifeste réel, cas manquant → `CalibrationConfigError` avant tout appel git, cas hors périmètre ignorés) ; `is_group_eligible` (chaque critère testé individuellement, y compris paire manquante) ; `compare_group` (chaque mismatch `CURRENT_MATCH`, chaque mismatch de jeu de paires dans les deux sens, chaque combinaison de nullité `rho_QQ`, valeurs non finies des deux côtés) ; `aggregate_metrics` (`MAX_ABS`, statut rho global) ; sécurité CLI (absence de tout flag de sélecteur) ; absence de surface `T_max`/`target_id`/`select_target_group` (vérifiée structurellement, pas seulement textuellement) ; provenance git (arbre sale, SHA invalide, cas requis manquant refusé avant tout appel git) ; firewall (valeur secrète absente du message/de la cause/du rendu de traceback) ; câblage complet de `run_calibration` (succès complet avec dérivation exacte des tolérances depuis des écarts contrôlés ; un cas sans groupe éligible → échec global avec raison explicite ; mismatch structurel → échec global, tolérances `None` ; environnement incomplet → échec ; désaccord de `repository_commit` historique → `CalibrationConfigError` ; répertoire de cas manquant → `CalibrationConfigError`) — `build_case_context`/`compute_current_group_result` (les deux seuls points de diagonalisation réelle) systématiquement moqués, tout le reste s'exécutant réellement contre un répertoire historique synthétique complet à 11 cas (construit via `build_campaign_plan(manifest)` réel + `assemble_execution` réel pour l'ordre canonique). Résultats observés par Claude (non exécutés par ChatGPT) : `tests/scripts/level1c_calibration` = 85 passed ; `tests/scripts/level1c_calibration + level1c_preflight + level1b_analysis + level1b_campaign + tests/level1 + tests/experiments` = 1139 passed ; suite complète du dépôt = 1748 passed, ~48-52s.

Fichiers de ce lot : `scripts/level1c_calibration/tmax_nonregression.py`, `tests/scripts/level1c_calibration/test_tmax_nonregression.py`, `docs/governance/current-task.md`. Aucun autre fichier modifié : aucun `src/cosmobox/*`, aucun `experiments/level1/*`, aucun schéma, aucun document scientifique Level1C.

**1C-7c2c ACCEPTÉ DÉFINITIVEMENT.** État gelé : `CALIBRATION_TOOL_IMPLEMENTATION_READY=YES`, `CALIBRATION_RUN_READY=NO` (readiness d'exécution non encore auditée), `NON_REGRESSION_CTT_ABS_TOL=PENDING_CALIBRATION`, `NON_REGRESSION_RHO_ABS_TOL=PENDING_CALIBRATION`, `NON_REGRESSION_TOLERANCE_STATUS=NEEDS_CALIBRATION`.

## Lot 1C-7c3 — audit de readiness de la calibration (lecture seule, accepté, aucun commit propre)

Audit read-only déterminant si l'outil implémenté en 1C-7c2c est réellement prêt pour l'unique exécution de calibration autorisée. `CALIBRATION_TOOL_CODE_ACCEPTED=YES`, `PRECONDITIONS_CURRENTLY_PASS=YES` (fingerprint manifeste/commit/`case_id`/éligibilité 2,2,2,2,3=11 tous confirmés contre l'artefact réel `/workspaces/level1b_campaign_output/`, environnement courant complet) mais **`CALIBRATION_RUN_READY=NO`**, quatre blocages strictement techniques et bornés identifiés (aucun contrat scientifique rouvert) : **B1** l'environnement incomplet n'arrêtait pas l'exécution avant chargement historique/diagonalisation ; **B2** l'éligibilité des cinq cas était vérifiée cas par cas, entrelacée avec la diagonalisation, risquant de consommer des diagonalisations avant de découvrir qu'un cas ultérieur était inutilisable ; **B3** `REQUIRED_CASE_KEYS` (un `frozenset`) itérait directement, avec un ordre non déterministe entre processus (`PYTHONHASHSEED` non fixé, confirmé empiriquement par trois ordres différents sur trois invocations), affectant l'ordre de `historical_cases`/`failure_reasons` dans l'artefact ; **B4** `derive_absolute_tolerance` levait une `OverflowError` brute non capturée pour des `E` proches de `float64.max`, violant son propre contrat (« pour tout E fini >= 0 »), confirmé empiriquement. Équivalence de recomputation avec le runner historique Level1B confirmée point par point (translation/réflexion, `C_TT_conn`, `rho_QQ`, sémantique `complete_multiplet`, `twice_T`, identité de paires, sémantique `spectral_window_group_index`, garde-fous de ressources) — mêmes primitives, mêmes arguments, aucun décalage de convention. Recommandation normative (non appliquée, hors contrat documentaire) : distinguer `PRE-RUN READINESS FAILURE` (exception, jamais d'artefact, avant `RUN_START`) de `CALIBRATION SCIENTIFIC FAIL` (`CalibrationArtifact(status=FAIL)`, après `RUN_START`).

## Lot 1C-7c3-fix — durcissement final de readiness avant calibration (ce commit)

Corrige exactement les quatre blocages B1-B4 de 1C-7c3, sans rouvrir aucun contrat scientifique (`CALIBRATION_CASE_FILTER`, `CALIBRATION_GROUP_FILTER`, `CURRENT_MATCH`, `MAX_ABS`, `NULLITY_POLICY`, `PAIR_SET_EQUALITY`, `NON_FINITE_POLICY`, `CEIL_DECADE_POLICY` — la règle mathématique elle-même —, `SINGLE_ACCEPTED_RUN`, provenance historique, champs du fingerprint d'environnement, `HOLDOUT_REPRESENTATIVITY`/`HIGH_DIMENSION_COVERAGE` inchangés).

Implémente explicitement `RUN_START_BOUNDARY` : une `PRECONDITION_PHASE` (`select_required_cases` → propreté git → SHA courant → fingerprint d'environnement → complétude d'environnement → validation de l'archive historique → éligibilité des CINQ cas requis via la nouvelle fonction `prepare_holdout_cases`) précède intégralement `RUN_START` (juste avant le premier `build_case_context`) — aucun échec détectable sans calcul scientifique ne peut plus atteindre une diagonalisation.

**B1** : l'environnement incomplet lève désormais immédiatement `CalibrationInternalFailure` (message sanitisé fixe), avant `load_historical_index` — testé : aucun appel à `load_historical_index`/`build_case_context`/`compute_current_group_result` ne se produit.

**B2** : nouvelle fonction `prepare_holdout_cases(index, required_cases, historical_output_dir)` calcule l'éligibilité des cinq cas requis (dans l'ordre canonique, voir B3) avant tout `build_case_context` ; si un seul cas a zéro groupe éligible, lève `CalibrationConfigError` listant tous les cas inutilisables trouvés — jamais après diagonalisation d'un autre cas. Récupère aussi `records_sha256` par cas (jamais recalculé après `RUN_START`). Sa propre boucle est enveloppée dans un `try/except` sanitisant (`CalibrationInternalFailure`) pour toute exception inattendue, cohérent avec la discipline déjà appliquée ailleurs dans le module. Testé : cas inutilisable en première position (`triangle S1`) et en dernière position (`ring5 S1`) de l'ordre canonique, dans les deux cas `build_case_context` n'est jamais appelé.

**B3** : nouvelle constante `REQUIRED_CASE_ORDER` (un `tuple`, jamais un `frozenset` itéré directement) fixant l'ordre canonique `triangle S1, ring4 S1, ring4 S2, ring4 S3, ring5 S1` ; `REQUIRED_CASE_KEYS` (conservé comme `frozenset`, désormais dérivé de `REQUIRED_CASE_ORDER`, réservé aux tests d'appartenance) n'est plus jamais itéré pour produire un ordre visible. `select_required_cases` et `prepare_holdout_cases` itèrent exclusivement `REQUIRED_CASE_ORDER` ; `eligible_groups_for_case` trie explicitement par `spectral_window_group_index` croissant (défense en profondeur, aucun changement d'identité scientifique — l'ordre était déjà canonique en amont). Testé : ordre stable sur plusieurs appels dans le même processus, `historical_cases` de l'artefact final préserve `REQUIRED_CASE_ORDER` exactement.

**B4** : `derive_absolute_tolerance` encapsule désormais le calcul de décennie (`10.0 ** exponent`) dans `_power_of_ten_or_fail`, qui capture `OverflowError` et détecte aussi un éventuel `inf` silencieux, levant dans les deux cas `CalibrationInternalFailure` — jamais une `OverflowError` brute, jamais une substitution par `inf`/`float64.max` (qui ne serait pas la décennie normative). Aucune borne physique nouvelle inventée : c'est une limite de représentation float64, jamais un seuil scientifique. Testé : `float64.max` et `float64.max*0.999` lèvent `CalibrationInternalFailure` (jamais `OverflowError`), le message ne contient aucune valeur numérique brute ; tous les cas déjà gelés (`1e-12`, `nextafter`, `1e-300`, sous-normal, `epsilon`) restent conformes à `TOL>=E`.

Aucune calibration réelle, aucune diagonalisation réelle, aucun `--confirm-run-calibration`. Rappel explicite pour 1C-7c4 : l'exécution réelle devra spécifier `--repo-root` explicitement (comportement de dépendance au cwd ambiant inchangé dans ce lot, hérité du préflight 1C-6k, non un défaut nouveau).

Tests (101, +16 par rapport à 1C-7c2c, entièrement synthétiques, aucune diagonalisation réelle) : les 4 points B1-B4 chacun avec un test dédié de comptage d'appels/d'ordre/de sanitisation, plus déterminisme de `select_required_cases`/`prepare_holdout_cases`/`eligible_groups_for_case` sur plusieurs appels. Résultats observés par Claude (non exécutés par ChatGPT) : `tests/scripts/level1c_calibration` = 101 passed ; `tests/scripts/level1c_calibration + level1c_preflight + level1b_analysis + level1b_campaign + tests/level1 + tests/experiments` = 1155 passed ; suite complète du dépôt = 1764 passed, ~47-50s.

Fichiers de ce lot : `scripts/level1c_calibration/tmax_nonregression.py`, `tests/scripts/level1c_calibration/test_tmax_nonregression.py`, `docs/governance/current-task.md`.

**1C-7c3-fix ACCEPTÉ DÉFINITIVEMENT** (commit `1f06ef91c1157389df7d325cb825ce7508eff52f`). Les quatre blocages B1-B4 sont fermés, `RUN_START_BOUNDARY` est désormais explicite dans le code.

## Lot 1C-7c4 — SINGLE REAL CALIBRATION RUN (exécution unique réelle, aucun commit propre)

Exécution réelle unique et définitive de la calibration du hold-out `TARGET_ID_FREE_PERSISTED_GROUP_HOLDOUT`, au-dessus du code accepté `1f06ef91c1157389df7d325cb825ce7508eff52f`, avec `--repo-root /workspaces/cosmobox` explicite et `--historical-output-dir /workspaces/level1b_campaign_output`. `PRECONDITION_PHASE=PASS` intégralement vérifiée avant `RUN_START` (commit résolu, environnement complet, provenance/manifeste/campagne historique conformes, cinq cas requis dans `REQUIRED_CASE_ORDER` tous éligibles avec comptes `2,2,2,2,3`=`11`). `CALIBRATION_REAL_RUN_INVOCATIONS=1`, `SINGLE_ACCEPTED_RUN_CONSUMED=YES`, exit code `0`, aucune seconde tentative.

Artefact brut conservé (lecture seule, jamais réécrit) : `results/level1c/calibration/level1c_nonregression_calibration_1f06ef91c1157389df7d325cb825ce7508eff52f.json`, taille `2056` bytes, `CALIBRATION_ARTIFACT_SHA256=05aa2e9d77c1314926350f8202e620b7537bdf9f6a7bab12a232151e9cf68da6` ; stderr associé `0` byte. `calibration_contract_version=level1c-nonregression-calibration-v2`, `schema_version=level1c-nonregression-calibration-artifact-v2`, `code_commit=1f06ef91c1157389df7d325cb825ce7508eff52f`, `historical_campaign_id=level1b-reference-v1`, `historical_manifest_fingerprint=159660cac738518dc620b9627ec95fd67c5dbc283707fdf72e572886364693ab`, `historical_repository_commit=0ff65ac66b4aa054f739b350cd384c26ecd19752` — tous conformes. `environment_fingerprint` de l'artefact identique bit-à-bit au fingerprint reconstruit en précondition (`python 3.13.14`, `numpy 2.5.1`, `scipy 1.18.0`, `blas/lapack scipy-openblas 0.3.33.112.0`, `Linux-6.6.87.2-microsoft-standard-WSL2-x86_64-with-glibc2.41`, `x86_64`).

`CALIBRATION_STATUS=SUCCESS`, `failure_reasons=[]`, `E_CTT=0.0`, `E_RHO=0.0`, `NON_REGRESSION_CTT_ABS_TOL=1e-15`, `NON_REGRESSION_RHO_ABS_TOL=1e-15`. Aucun code modifié, aucun test relancé, aucun commit, aucun push — lot purement exécutif. **1C-7c4 ACCEPTÉ DÉFINITIVEMENT.** `SECOND_CALIBRATION_RUN=FORBIDDEN` sous le contrat courant ; toute recalibration future nécessiterait un nouveau jalon documentaire rouvrant explicitement le protocole.

## Lot 1C-7c5 — gel documentaire du résultat de calibration (ce commit)

Documente définitivement dans `docs/levels/level1c/identifiability-preregistration.md` le résultat unique de 1C-7c4 : provenance complète, hash d'artefact, cinq `records_sha256` exacts (relus directement depuis l'artefact accepté, jamais recopiés d'un rapport textuel), tolérances dérivées, dérivation `CEIL_DECADE_POLICY` pour `E=0` (`eps≈2.220446049250313e-16`, `ceil(log10(eps))=-15`, `TOL=1e-15`), interprétation épistémique prudente de `E_CTT=E_RHO=0` (absence de différence numérique observée par `MAX_ABS` sur ce hold-out précis dans cet environnement gelé — jamais « reproductibilité parfaite » ni « erreur nulle du solveur » ni un énoncé de déterminisme universel), limites de représentativité du hold-out (`HOLDOUT_REPRESENTATIVITY=LIMITED`, dimensions `48-496` contre `1000-1504` en campagne normative, `HIGH_DIMENSION_COVERAGE=NO`, `SUFFICIENT_FOR_EMPIRICAL_PIPELINE_GATE=YES`), portée scientifique strictement négative (aucun résultat physique, aucun `Delta_C_TT`, aucun tracking inter-J0, aucune reconstruction de géométrie/métrique, aucune preuve de gravité émergente, aucun énoncé `S→∞`/convergence). Aucun code, aucun test, aucun run, aucune diagonalisation, aucune calibration, aucune modification d'artefact.

Readiness mise à jour : `NON_REGRESSION_TOLERANCE_STATUS=CALIBRATED`, `NON_REGRESSION_CONTRACT_READY=YES`, `NORMATIVE_PRODUCTION_CONTRACT_READY=YES` (les deux tolérances requises sont désormais déterminées et documentées, aucune autre dépendance contractuelle explicite restante en §20.29 hormis l'implémentation elle-même), `NORMATIVE_CAMPAIGN_IMPLEMENTATION_READY=NO` (runner/manifeste/schéma Level1C normatifs toujours inexistants), `CALIBRATION_RUN_STATUS=CLOSED_ACCEPTED`, `CALIBRATION_RUN_READY=NOT_APPLICABLE_CLOSED` (le run a déjà eu lieu et est clos ; ce champ n'exprime plus une permission de lancer un nouveau run). `PHYSICAL_RESPONSE_THRESHOLD` reste `OPEN`, aucun seuil choisi dans ce lot.

Fichiers de ce lot : `docs/levels/level1c/identifiability-preregistration.md`, `docs/governance/current-task.md`. **1C-7c5 ACCEPTÉ.**

## Lot 1C-8a — design read-only du schéma et du manifeste normatifs Level1C (aucun commit propre)

Audit/design en lecture seule transformant le contrat scientifique Level1C désormais gelé (§15-§20) en proposition de spécification machine-readable (manifeste, schéma, contrats d'entrée/sortie du futur runner), sans implémentation. Confirme la réutilisation maximale des structures Level1B déjà existantes (`matching.py`, `results.py`, `restricted.py`, `target_selection.py`, `planning.py::build_ordered_pairs/compute_case_id/derive_case_seed`, `outputs.py`, `scripts/level1b_analysis/indexing.py`) et identifie le trou de traçabilité D022 (target_id → spectral_window_group_index jamais persisté par Level1B) comme la correction structurelle centrale à apporter pour Level1C. **Accepté après correction** (voir 1C-8a-fix) : un blocker a été identifié par l'audit de Lionel (`TARGET_ROLE_NOT_REPRESENTABLE_WITH_CURRENT_TargetGroupSpec` — `TargetGroupSpec` impose `required_spectral_status=complete_multiplet` pour tout `flavor_label`, rendant impossible d'y encoder directement `T_max` en `CALIBRATION_ONLY` non bloquant sans casser le contrat existant).

## Lot 1C-8a-fix — target role separation (aucun commit propre)

Ferme le blocker par composition, sans modifier `TargetGroupSpec` ni aucun contrat Level1B : introduit `TargetRole = REQUIRED | CALIBRATION_ONLY` et `Level1CTargetSpec{target: TargetGroupSpec, role: TargetRole}`, explicitement orthogonal à `required_spectral_status` (jamais inféré depuis lui). `T_max` conserve `required_spectral_status=complete_multiplet` (comme toute cible `flavor_label`) avec `role=CALIBRATION_ONLY` — sélectionnée sur `partial_subspace`, elle reste transcrite `meets_normative_requirements=False`, jamais promue cible valide, mais son absence/ambiguïté ne bloque jamais `campaign_status`. `T_3_2`/`fundamental`/`first_excited` restent `role=REQUIRED`. **Accepté.** Ratifie définitivement DB1 (`J_override=(0, 1.0)` explicite pour `J0=1.0`), DB2 (package `experiments/level1c/`), DB3 (`campaign_id=level1c-j0-response-v1`), DB4 (versions `level1c-campaign-manifest-v1`/`level1c-result-record-v1`/`level1c-campaign-artifact-v1`/`level1c-tracking-record-v1`). `NORMATIVE_SCHEMA_DESIGN_READY=YES`.

## Lot 1C-8b — implémentation manifeste/schéma/planning Level1C (commit `23d3b216500fda8d38dd7952a05ebe8744b57848`, ACCEPTÉ TEL QUE CORRIGÉ PAR 1C-8b-fix)

Implémente, sans aucun runner scientifique, aucune diagonalisation, aucune campagne réelle : le package `experiments/level1c/` (`manifest.py`, `planning.py`, `target_selection.py`, `result_schema.py`), le manifeste JSON normatif (`experiments/level1c/preregistered-manifest-v1.json`, `manifest_version=level1c-campaign-manifest-v1`, `campaign_id=level1c-j0-response-v1`, `schema_version=level1c-result-record-v1`), son schéma Draft 2020-12 dédié (`schemas/level1c/campaign-manifest-v1.schema.json`, jamais une modification de `schemas/level1/preregistered-manifest-v1.schema.json`), un schéma `result-record-v1` dédié (`schemas/level1c/result-record-v1.schema.json`, extension additive autonome de la forme `correlators-v2` : ajoute `group_start_index`/`group_end_index_exclusive` à l'identité de groupe spectral, ajoute le `record_kind` `target_selection`, restreint `geometry` à `{triangle, ring5}` — jamais une modification de `schemas/level1/correlators-v2.schema.json`).

`Level1CManifest` réutilise par composition/import (jamais par modification) `HamiltonianCaseSpec`/`GridPointSpec`/`TargetGroupSpec`/`ResourceGuardrailsSpec`/`compute_manifest_fingerprint` de `experiments.level1.manifest` — n'est ni une sous-classe ni une instance de `experiments.level1.manifest.Manifest` (qui code en dur `manifest_version`/`schema_version` Level1B dans son `__post_init__` et rejetterait tout autre manifeste). `TargetRole`/`Level1CTargetSpec` (1C-8a-fix) intégrés tels que gelés. Cinq `HamiltonianCaseSpec` (`j0-0.50`…`j0-1.50`, `J_override=(0, J0)` explicite pour les cinq valeurs y compris `1.0`, DB1). Vingt points de grille (`triangle`/`ring5` × `S∈{2,3}` × 5 `J0`), **un seul** `hamiltonian_case_id` par point de grille (nécessité structurelle : `production_window` varie par `J0` à `(geometry,S)` fixé, §19.8 — contrairement au patron Level1B où plusieurs `hamiltonian_case_id` pouvaient partager un même point de grille/une même fenêtre). Fenêtres de production reprises telles que gelées par le préflight accepté (`triangle: 5,5,9,5,5`, `ring5: 11,11,15,11,11`), jamais recalculées (`validate_production_windows_match_frozen_table`). `non_regression_calibration` porte les quatre constantes gelées (`calibration_artifact_sha256=05aa2e9d77c1314926350f8202e620b7537bdf9f6a7bab12a232151e9cf68da6`, `code_commit=1f06ef91c1157389df7d325cb825ce7508eff52f`, `contract_version=level1c-nonregression-calibration-v2`, `ctt_abs_tol=rho_abs_tol=1e-15`) — aucun mécanisme de recalibration.

`planning.py::build_level1c_campaign_plan` réutilise verbatim `CampaignCaseSpec`/`compute_case_id`/`derive_case_seed`/`build_ordered_pairs` de `experiments.level1.planning` — produit exactement 20 `CampaignCaseSpec`, ordre canonique déterministe (`triangle S2`, `triangle S3`, `ring5 S2`, `ring5 S3`, J0 croissant dans chaque groupe), identique à l'ordre déjà accepté du préflight `j0_grid_preflight.py`. `build_baseline_mapping`/`build_tracking_edges` dérivent la relation baseline→perturbé explicitement (par `hamiltonian_case_id` exact, jamais une recherche heuristique) : 4 baselines (`J0=1.0`), 16 cas perturbés, 16 relations baseline→perturbé.

`target_selection.py::TargetSelectionRecord`/`build_target_selection_record` ferment le trou D022 : persistance explicite `target_id → spectral_window_group_index → role`, réutilisant exactement les 4 statuts déjà gelés de `experiments.level1.target_selection` (`selected`/`ambiguous`/`not_in_window`/`structurally_not_applicable`), aucune nouvelle taxonomie.

Tests (73, entièrement synthétiques/unitaires, aucune diagonalisation) : schéma manifeste bien formé et rejette toute mutation invalide (campagne_id, géométrie `ring4`, spin `1`, rôle inconnu, tolérance recalibrée, hash de calibration modifié, `J_override` hors node 0, 19 points de grille) ; fingerprint stable/sensible ; comptages 20 cas/4 baselines/16 perturbés/16 relations/70 sorties de cible confirmés programmatiquement ; `T_max` `CALIBRATION_ONLY` + `complete_multiplet` si sélectionné, jamais promue si `partial_subspace` ; `T_3_2` `REQUIRED` ; `case_id` conforme à `compute_case_id` ; fenêtres de production conformes à la table gelée ; schéma `result-record-v1` accepte les documents valides (`C_TT_conn`, `rho_QQ` numérique/`null`, `symmetry_label`, `target_selection` sélectionné/non sélectionné) et rejette `ring4`/`spin=1`/champs de bornes manquants/`schema_version`/`campaign_id` erronés/`record_kind` Level1B-only/rôle ou statut inconnu ; le manifeste Level1B continue de charger sans changement. Résultats observés par Claude (non exécutés par ChatGPT) : `tests/experiments/level1c` = 73 passed ; `tests/experiments + tests/scripts/level1c_calibration + tests/scripts/level1c_preflight + tests/scripts/level1b_analysis + tests/scripts/level1b_campaign + tests/level1` = 1228 passed ; suite complète du dépôt = 1837 passed, ~52-56s.

`NORMATIVE_SCHEMA_DESIGN_READY=YES`, `NORMATIVE_MANIFEST_IMPLEMENTATION_READY=YES`, `NORMATIVE_SCHEMA_IMPLEMENTATION_READY=YES`, `NORMATIVE_PLANNING_IMPLEMENTATION_READY=YES`, `NORMATIVE_CAMPAIGN_IMPLEMENTATION_READY=NO` (runner scientifique, gate de non-régression, tracking et réponse restent des lots futurs distincts, non ouverts ici).

Fichiers de ce lot : `experiments/level1c/__init__.py`, `experiments/level1c/manifest.py`, `experiments/level1c/planning.py`, `experiments/level1c/target_selection.py`, `experiments/level1c/result_schema.py`, `experiments/level1c/preregistered-manifest-v1.json`, `schemas/level1c/campaign-manifest-v1.schema.json`, `schemas/level1c/result-record-v1.schema.json`, `tests/experiments/level1c/test_level1c_manifest.py`, `tests/experiments/level1c/test_level1c_planning.py`, `tests/experiments/level1c/test_level1c_target_selection.py`, `tests/experiments/level1c/test_level1c_result_schema.py`, `docs/governance/current-task.md`. Aucun fichier Level1B modifié (`experiments/level1/manifest.py`, `schemas/level1/*` inchangés).

**Audit ChatGPT : NON ACCEPTÉ EN L'ÉTAT.** Verdict détaillé : `MANIFEST=OK`, `PLANNING=OK`, `TARGET_ROLE=OK`, `CALIBRATION_PROVENANCE=OK`, `RESULT_SCHEMA=BLOCKED`. Blocker exact : `TARGET_SELECTION_RECORD_REQUIRES_FAKE_SPECTRAL_GROUP_IDENTITY` — le `record_kind=target_selection` ajouté à `schemas/level1c/result-record-v1.schema.json` héritait de `identity.spectral_group` obligatoire (forme `ResultRecord`), alors qu'un statut `ambiguous`/`not_in_window`/`structurally_not_applicable` ne possède structurellement aucun groupe spectral sélectionné — cela aurait exigé de fabriquer une identité de groupe fictive, réintroduisant exactement la confusion `case-level target-selection outcome` vs `physical spectral-group identity` que D022 impose de séparer. Corrigé sans réouverture scientifique par `1C-8b-fix` ci-dessous.

## Lot 1C-8b-fix — persistance de target-selection au niveau du cas (commit `5159c2d68a060858cfd751e9b76365eecb2aba3e`, ACCEPTÉ DÉFINITIVEMENT)

Corrige uniquement le blocker `RESULT_SCHEMA` de 1C-8b, sans toucher au manifeste, au planning, à `TargetRole`/`Level1CTargetSpec`, ni à aucune décision scientifique (grille `J0`, fenêtres de production, cibles, tracking, tolérances, `PHYSICAL_RESPONSE_THRESHOLD` — tous inchangés).

Retire intégralement `record_kind=target_selection`/`observable_kind=target_selection`/`$defs.targetSelectionRecord` de `schemas/level1c/result-record-v1.schema.json`, qui redevient strictement réservé aux documents réellement attachés à un groupe spectral physique (`raw_observable`, `normalized_observable`, `symmetry_label`). `experiments/level1c/target_selection.py` (`TargetSelectionRecord`, statuts existants, `role` jamais inféré) reste inchangé.

**Décision de design gelée** : `TARGET_SELECTION_PERSISTENCE = CASE_RUN_ARTIFACT_ONLY` — un `TargetSelectionRecord` est une structure de niveau CAS (jamais un `ResultRecord`, jamais une identité de groupe physique), persistée dans un nouvel artefact de run de cas dédié, jamais dans `records.jsonl`.

Nouveau schéma `schemas/level1c/case-run-v1.schema.json` (`schema_version=level1c-case-run-v1`, ratifié) et nouveau module `experiments/level1c/case_artifact.py` : `Level1CCaseRunArtifact{schema_version, campaign_id, manifest_fingerprint, repository_commit, case_id, geometry, spin, hamiltonian_case_id, sector_id, spectral_window, run_status, target_selections, records_sha256, record_count}` — `run_status` reprend verbatim la convention Level1B (`success`/`failed`/`resource_guardrail_exceeded`, `scripts/level1b_campaign/outputs.py`). Invariants : `run_status=success` exige `target_selections` non vide, `records_sha256` (SHA-256 hex valide) et `record_count>0` ; `run_status` d'échec exige `target_selections=()`, `records_sha256=None`, `record_count=0` (aucune sélection fictive pour un cas non réussi). `validate_target_selections_match_manifest` vérifie l'égalité exacte et l'ordre exact entre `target_selections` et les `Level1CTargetSpec` du manifeste pour la géométrie du cas (jamais un ordre `set`/`dict` reconstruit) — rejette toute cible manquante, supplémentaire, ou mal ordonnée. `to_json_dict`/`canonical_json_bytes` sérialisent canoniquement (UTF-8, `sort_keys`, `ensure_ascii`, `allow_nan=False`, une seule newline finale — convention `run.json` Level1B reprise verbatim). `cosmobox.level1.results.ScientificIdentity`/`SpectralGroupIdentity` non modifiées.

Tests (30 nets ajoutés, 3 modifiés dans `test_level1c_result_schema.py` pour confirmer le rejet de `target_selection` — plus aucun test n'y fabrique de fausse `identity.spectral_group`) : schéma `case-run-v1` bien formé ; `triangle`=3/`ring5`=4 sorties de cible, total campagne=70 ; rejet cible manquante/supplémentaire/mal ordonnée ; invariants `selected`/non-`selected` (les 4 statuts existants, aucune nouvelle taxonomie) ; `T_max` `CALIBRATION_ONLY`+`complete_multiplet` si sélectionné sur `partial_subspace`, jamais promue ; `T_3_2` `REQUIRED` ; `role` jamais inféré ; artefact succès/échec valides et conformes au schéma ; rejet `ring4`/`spin=1`/`run_status` inconnu/incohérences succès-échec ; `target_selection` désormais rejeté par `result-record-v1` même avec une identité spectrale par ailleurs valide. Résultats observés par Claude (non exécutés par ChatGPT) : `tests/experiments/level1c` = 103 passed ; suite connexe (`tests/experiments + tests/scripts/level1c_calibration + tests/scripts/level1c_preflight + tests/scripts/level1b_analysis + tests/scripts/level1b_campaign + tests/level1`) = 1258 passed ; suite complète du dépôt = 1867 passed, ~52-56s.

Fichiers de ce lot : `schemas/level1c/result-record-v1.schema.json` (modifié), `schemas/level1c/case-run-v1.schema.json` (créé), `experiments/level1c/case_artifact.py` (créé), `tests/experiments/level1c/test_level1c_result_schema.py` (modifié), `tests/experiments/level1c/test_level1c_case_artifact.py` (créé), `docs/governance/current-task.md`. Aucun runner scientifique, aucune diagonalisation, aucune campagne réelle.

**1C-8b + 1C-8b-fix ACCEPTÉS DÉFINITIVEMENT ENSEMBLE.** État gelé : `TARGET_SELECTION_PERSISTENCE=CASE_RUN_ARTIFACT_ONLY`, `NORMATIVE_SCHEMA_DESIGN_READY=YES`, `NORMATIVE_MANIFEST_IMPLEMENTATION_READY=YES`, `NORMATIVE_SCHEMA_IMPLEMENTATION_READY=YES`, `NORMATIVE_PLANNING_IMPLEMENTATION_READY=YES`, `NORMATIVE_CAMPAIGN_IMPLEMENTATION_READY=NO`.

## Lot 1C-8c — implémentation de PHASE_P Level1C, single-case production runner (commit `bc51ff5d972c05d2027076a5e9e8ded074d6d6e9`, NON ACCEPTÉ EN L'ÉTAT)

Implémente le moteur de production scientifique d'UN cas Level1C (`scripts/level1c_campaign/runner.py::run_level1c_case`), sans lancer aucune campagne réelle des 20 cas, aucune gate de non-régression baseline, aucun tracking inter-J0, aucune phase R, aucun `campaign.json` final, aucun statut `campaign_status`. Réutilise verbatim toutes les primitives scientifiques déjà validées (`cosmobox.level0.*`, `cosmobox.level1.local_observables`/`matching`/`restricted`/`results`/`automorphisms`, `experiments.level1.target_selection.select_target_group`) — aucune duplication, seule l'orchestration Level1C (identité étendue avec `group_start_index`/`group_end_index_exclusive`, filtrage `record_kind`/`observable_kind` restreint au contrat `result-record-v1`) est nouvelle.

Respecte exactement `SPECTRAL_STRUCTURE_PRODUCTION=ALL_COMPLETE_GROUPS_IN_PRODUCTION_WINDOW` (records `symmetry_label` produits pour tout groupe `complete_multiplet` du `production_window`, jamais pour un groupe `partial_subspace`) et `PHYSICAL_OBSERVABLE_PRODUCTION=PREREGISTERED_TARGETS_ONLY` (`C_TT_conn`/`rho_QQ` produits uniquement pour les groupes effectivement sélectionnés par une cible du manifeste, complet ou partiel — jamais supprimés pour une sélection non conforme, exactement comme Level1B ne bloque jamais la production sur `meets_normative_requirements`). Ces deux règles sont extraites en deux fonctions pures indépendamment testables (`_complete_group_indices`, `_selected_group_indices`), jamais mélangées avec l'orchestration de diagonalisation.

`TargetSelectionRecord` construit depuis les mêmes `TargetSelectionOutcome` que la sélection produit déjà (aucune double sélection), `role` transcrit depuis `Level1CTargetSpec.target_groups[geometry]` du manifeste, jamais inféré. `validate_target_selections_match_manifest` appelée avant tout retour du résultat (défense en profondeur, réappelée une seconde fois dans `outputs.write_case_success`, au point exact où le résultat quitte la mémoire). Aucun `TargetSelectionRecord` n'apparaît jamais dans `documents` (confirmé par test).

**Required-target case gate — résolu sans `DESIGN_BLOCKER`** : la convention `run_status` existante (`success`/`failed`/`resource_guardrail_exceeded`, reprise verbatim de Level1B, décrivant exclusivement si le calcul s'est exécuté sans erreur technique) suffit et n'est pas modifiée — aucune nouvelle valeur de statut n'est introduite. Le fait supplémentaire nécessaire (« toutes les cibles `REQUIRED` sont trouvées et conformes ») est entièrement dérivable des champs déjà persistés (`role`, `selection_status`, `meets_normative_requirements`) : exposé comme fonction pure `experiments.level1c.case_artifact.required_targets_are_satisfied` + propriété `Level1CCaseRunArtifact.required_targets_satisfied` (vérifie `run_status` d'abord, jamais `True` vacueux pour un cas non réussi) — aucun champ nouveau ajouté au schéma `case-run-v1` déjà accepté, aucune nouvelle taxonomie de statut. `CALIBRATION_ONLY` (`T_max`) n'affecte jamais cette porte, quel que soit son résultat.

`scripts/level1c_campaign/outputs.py::write_case_success`/`write_case_failure` persistent atomiquement `records.jsonl` (schéma `result-record-v1`) + `run.json` (`Level1CCaseRunArtifact`, schéma `case-run-v1`), reprenant verbatim le patron d'écriture atomique de `scripts/level1b_campaign/outputs.py` (dupliqué, jamais importé, utilitaire OS générique sans contenu scientifique, package séparé). `write_case_failure` ne produit jamais de `records.jsonl` ni de `target_selections` fictifs ; un `records.jsonl` résiduel d'un succès antérieur est supprimé.

Exercé réellement (aucune campagne) sur trois cas réels de petite taille : `triangle S=2 J0=1.00` (fenêtre 9, `T_max` résout sur le même groupe que `first_excited`), `triangle S=2 J0=0.50` (fenêtre 5, `T_max` sélectionné sur un groupe `partial_subspace` réel — `meets_normative_requirements=False`, jamais promu, jamais bloquant), `ring5 S=2 J0=1.00` (fenêtre 15, `T_max` naturellement `not_in_window`, non bloquant) — ces trois exécutions confirment en conditions réelles (sans mock) tous les cas limites du contrat sans qu'aucune campagne des 20 cas n'ait été exécutée.

Tests (47 ajoutés : 26 dans `test_level1c_runner.py`, 12 dans `test_level1c_outputs.py`, 9 dans `test_level1c_case_artifact.py` pour `required_targets_are_satisfied`) : ordre des cibles triangle/ring5 ; `T_max` non bloquant (`not_in_window` réel et `selected`+`partial_subspace` réel) ; `T_3_2` `REQUIRED` ; porte required-target vraie/fausse (cible manquante/ambiguë, `CALIBRATION_ONLY` ignoré) ; tous les groupes complets structurellement présents, jamais un groupe partiel ; observables uniquement sur les groupes sélectionnés (égalité exacte testée) ; `group_start_index`/`group_end_index_exclusive` cohérents ; aucun `target_selection` dans `documents` ; validation de schéma sur chaque document réel ; absence de NaN/inf ; rejet d'un cas non planifié ; cohérence hash/count `run.json`↔`records.jsonl` ; déterminisme de la sérialisation canonique ; `write_case_failure` sans `records.jsonl`/sélections fictives, suppression du résiduel. Résultats observés par Claude (non exécutés par ChatGPT) : `tests/experiments/level1c + tests/scripts/level1c_campaign` = 150 passed ; suite connexe (`tests/experiments + tests/scripts/level1c_campaign + tests/scripts/level1c_calibration + tests/scripts/level1c_preflight + tests/scripts/level1b_analysis + tests/scripts/level1b_campaign + tests/level1`) = 1305 passed ; suite complète du dépôt = 1914 passed, ~57-60s (inclut trois diagonalisations réelles de petite taille, dont une ring5 S=2 dimension 1000, ~8-9s, jamais une campagne).

`NORMATIVE_PHASE_P_IMPLEMENTATION_READY=YES`, `NORMATIVE_CAMPAIGN_IMPLEMENTATION_READY=NO` (gate de non-régression baseline, tracking inter-J0, phase R et exécution réelle de la campagne des 20 cas restent des lots futurs distincts, non ouverts ici).

Fichiers de ce lot : `experiments/level1c/case_artifact.py` (étendu : `required_targets_are_satisfied`, `Level1CCaseRunArtifact.required_targets_satisfied`), `scripts/level1c_campaign/__init__.py`, `scripts/level1c_campaign/runner.py`, `scripts/level1c_campaign/outputs.py`, `tests/experiments/level1c/test_level1c_case_artifact.py` (étendu), `tests/scripts/level1c_campaign/conftest.py`, `tests/scripts/level1c_campaign/test_level1c_runner.py`, `tests/scripts/level1c_campaign/test_level1c_outputs.py`, `docs/governance/current-task.md`. Aucun manifeste, schéma, ou décision scientifique modifié.

**Audit ChatGPT : NON ACCEPTÉ EN L'ÉTAT.** Verdict détaillé : `PHASE_P_COMPUTATION=OK`, `STRUCTURAL_PRODUCTION=OK`, `TARGET_SELECTION_PERSISTENCE=OK`, `OBSERVABLE_PRODUCTION=OK`, `ATOMIC_OUTPUTS=OK`. Blocker exact : `NORMATIVE_CASE_SUCCESS_SEMANTICS_NOT_FROZEN` — la séparation introduite entre `run_status` (technique) et le required-target gate (scientifique) était conceptuellement correcte mais n'avait jamais été explicitement ratifiée avant implémentation, et le verdict scientifique du cas n'était exposé que comme une propriété calculée à la volée, jamais persisté durablement dans l'artefact lui-même. Corrigé sans réouverture scientifique par `1C-8c-fix` ci-dessous, qui ratifie formellement `RUN_STATUS_SEMANTICS=TECHNICAL_EXECUTION_STATUS` et introduit le champ persistant `normative_case_valid`.

## Lot 1C-8c-fix — gel et persistance de la validité normative du cas (commit `79c0b8b8a5f2208acb6c4b8776ef6323ad8bbd98`, ACCEPTÉ DÉFINITIVEMENT)

Corrige uniquement le blocker `NORMATIVE_CASE_SUCCESS_SEMANTICS_NOT_FROZEN` de 1C-8c, sans toucher au moteur de production (`scripts/level1c_campaign/runner.py` inchangé, aucune diagonalisation nécessaire), au manifeste, au planning, ni à aucune décision scientifique (grille `J0`, fenêtres, cibles, tracking, tolérances, `PHYSICAL_RESPONSE_THRESHOLD` — tous inchangés). Aucun campagne runner, aucune gate de non-régression baseline, aucun tracking inter-J0, aucune phase R, aucun `campaign.json`, aucune campagne réelle des 20 cas.

**Décision gelée, ratifiée par ChatGPT** : `RUN_STATUS_SEMANTICS = TECHNICAL_EXECUTION_STATUS` — `run_status` (`success`/`failed`/`resource_guardrail_exceeded`, taxonomie Level1B inchangée, aucun nouveau statut) décrit exclusivement l'exécution technique, jamais le verdict scientifique du cas. Séparation explicite : `NORMATIVE_CASE_VALID = (run_status == success) AND (toutes les cibles REQUIRED satisfont selection_status==selected ET meets_normative_requirements==true)`. `CALIBRATION_ONLY` (`T_max`) n'influence jamais ce verdict. Une invalidité scientifique n'est jamais convertie en `run_status=failed` — cela confondrait échec d'exécution et échec de gate normatif ; l'artefact d'un cas normativement invalide reste intégralement disponible pour diagnostic.

Nouveau champ persistant `normative_case_valid: bool | None` sur `Level1CCaseRunArtifact` (schéma `case-run-v1` étendu de façon additive, `schema_version` inchangé — modification encore dans le lot d'implémentation initial, avant toute campagne normative réelle, DB4 déjà couvre ce cas). Sémantique : `run_status==success` → `normative_case_valid` booléen obligatoire, égal exactement à `required_targets_are_satisfied(target_selections)` (vérifié dans `__post_init__`, un booléen incohérent avec `target_selections` est rejeté — même discipline d'auto-vérification que `CampaignCaseSpec.case_id`) ; `run_status!=success` → `normative_case_valid=None` (`NOT_EVALUATED`, jamais un synonyme de `False` : un échec technique n'a jamais été évalué scientifiquement). La propriété `required_targets_satisfied` est conservée comme vue booléenne pratique (`bool(self.normative_case_valid)`, jamais un recalcul indépendant — ne peut plus jamais diverger du champ persisté).

`write_case_success` calcule `normative_case_valid = required_targets_are_satisfied(result.target_selections)` après `validate_target_selections_match_manifest`, et le persiste **sans jamais refuser l'écriture** quelle que soit sa valeur — un cas normativement invalide reste pleinement écrit et consultable. `write_case_failure` persiste toujours `normative_case_valid=None`.

Documenté pour les lots futurs (non implémenté ici) : `CASE_TECHNICALLY_SUCCESSFUL = (run_status==success)`, `CASE_NORMATIVELY_VALID = (run_status==success AND normative_case_valid==true)` ; une future campagne ne pourra être `NORMATIVE_CAMPAIGN_VALID=YES` que si tous les cas `REQUIRED` sont `CASE_NORMATIVELY_VALID` — l'agrégation elle-même reste un lot distinct.

Tests (14 nets ajoutés : 12 dans `test_level1c_case_artifact.py`, 2 dans `test_level1c_outputs.py`) : succès avec toutes cibles `REQUIRED` valides → `normative_case_valid=True` ; `REQUIRED` `ambiguous`/`not_in_window`/`selected`+`partial_subspace` → `False` ; `T_max` `ambiguous`/`selected`+`partial_subspace` (CALIBRATION_ONLY) → toujours `True` si les `REQUIRED` sont valides ; `failed`/`resource_guardrail_exceeded` → `None` ; booléen incohérent avec `target_selections` (dans les deux sens) → rejet ; `normative_case_valid` doit être un booléen strict pour un succès (jamais `None`) → rejet ; `run.json` porte le champ correctement pour succès et échec ; `write_case_success` n'échoue jamais pour un cas normativement invalide construit à partir d'un résultat réel altéré (`first_excited` rendu `ambiguous`), `run_status` reste `success`, artefact conforme au schéma. Aucune nouvelle diagonalisation nécessaire (`scripts/level1c_campaign/runner.py` inchangé ; le seul test touchant un résultat réel réutilise les fixtures déjà calculées de 1C-8c). Résultats observés par Claude (non exécutés par ChatGPT) : `tests/experiments/level1c + tests/scripts/level1c_campaign` = 164 passed ; suite connexe = 1319 passed ; suite complète du dépôt = 1928 passed, ~55-60s (aucune diagonalisation supplémentaire par rapport à 1C-8c).

Fichiers de ce lot : `experiments/level1c/case_artifact.py` (étendu), `schemas/level1c/case-run-v1.schema.json` (étendu, `schema_version` inchangé), `scripts/level1c_campaign/outputs.py` (étendu), `tests/experiments/level1c/test_level1c_case_artifact.py` (étendu), `tests/scripts/level1c_campaign/test_level1c_outputs.py` (étendu), `docs/governance/current-task.md`. `scripts/level1c_campaign/runner.py` non modifié.

**1C-8c + 1C-8c-fix ACCEPTÉS DÉFINITIVEMENT ENSEMBLE.** État gelé : `RUN_STATUS_SEMANTICS=TECHNICAL_EXECUTION_STATUS`, `NORMATIVE_PHASE_P_IMPLEMENTATION_READY=YES`, `NORMATIVE_CAMPAIGN_IMPLEMENTATION_READY=NO`.

## Lot 1C-8d — implémentation de la gate de non-régression baseline (commit `324f4e4e8a4e76cd7c6ead65d0a724a2631522d2`, NON ACCEPTÉ EN L'ÉTAT)

Implémente l'outil situé exactement entre P et T (§20.19) : `P -> BASELINE_NON_REGRESSION_GATE -> T`. Ce lot livre l'outil, son schéma et ses tests ; **aucune exécution normative réelle de la gate sur les quatre baselines n'a été effectuée** (aucun cas Level1C réel n'existe encore sur disque, aucune campagne des 20 cas n'a été lancée).

**Audit D022 préalable (§22 du mandat)** : un premier arrêt `BLOCKED` a été produit avant tout code, correctement identifié par l'audit de Lionel comme une prudence justifiée mais une conclusion excessive. Après audit conceptuel du sélecteur et du runner historique Level1B, la conclusion corrigée et retenue est :

```text
BASELINE_REQUIRED_TARGET_MAPPING_FROM_PERSISTED_LEVEL1B
= PROVABLE
```

`fundamental`/`first_excited` sont positionnels (`spectral_window_group_index` 0/1, une conséquence directe et déjà gelée de D022 — aucune preuve nécessaire). `T_3_2` (ring5, `flavor_label`) est prouvé par **exclusion fermée** sur l'ensemble clos des cibles du manifeste historique : le runner Level1B ne persiste jamais un groupe non sélectionné par au moins une cible ; pour chaque groupe persisté, chaque cible du manifeste est testée contre sa propre condition nécessaire et déterministe (`fundamental`→index 0, `first_excited`→index 1, `flavor_label`→`twice_T==target_twice_T`) ; un groupe n'est retenu comme preuve de `T_3_2` que s'il satisfait la condition de `T_3_2` **et** qu'aucune autre cible du manifeste ne peut également l'expliquer ; `FAIL` si zéro ou plus d'un groupe satisfont cette preuve. Ceci n'est **jamais** la règle rejetée « unique groupe avec ce `twice_T` = la cible » (testée et explicitement rejetée, voir `test_no_unique_twice_t_heuristic_shortcut_exists`) — c'est une preuve par élimination sur un ensemble de causes fermé, jamais une reconstruction du pool de candidats historique.

**`T_3_2_HISTORICAL_MAPPING_PROOF`** (vérifié en lecture seule contre l'archive réelle `/workspaces/level1b_campaign_output/`, aucune diagonalisation) :

```text
ring5 S2 reference : groupes persistés = [(0,twice_T=1), (1,twice_T=1), (2,twice_T=3)]
  exclusion : groupe 0 -> causes possibles = [fundamental] seul
              groupe 1 -> causes possibles = [first_excited] seul
              groupe 2 -> causes possibles = [T_3_2] seul (fundamental exclu : index!=0 ;
                          first_excited exclu : index!=1 ; T_max exclu : twice_T=5!=3)
  => T_3_2 prouvé = spectral_window_group_index 2

ring5 S3 reference : identique -> T_3_2 prouvé = spectral_window_group_index 2
```

`2` est un résultat de cette exécution historique précise, jamais codé en dur comme règle universelle — l'algorithme (`historical_group_index_for_required_target`) recalcule la preuve générique à chaque appel.

**Périmètre** : 4 baselines REQUIRED (`triangle`/`ring5` × `S∈{2,3}`) ; cibles comparées `triangle`={fundamental, first_excited}, `ring5`={fundamental, first_excited, T_3_2} ; `T_max` **exclu** de la comparaison (mais toujours consulté en interne par la preuve d'exclusion). Champs REQUIRED comparés : `C_TT_conn(i,j) i!=j`, `rho_QQ(i,j)`, `multiplicity`, `twice_T`, labels de translation/réflexion (`symmetry_labels_match`, `SYMMETRY_TOLERANCE` — jamais `1e-15`). `representative_energy` = `OPTIONAL_DIAGNOSTIC`, jamais dans PASS/FAIL. `CTT`/`rho` : égalité stricte des jeux de paires puis `max_abs_diff <= tol` ; `rho` null/null exige `null_reason` identique, numeric/null toujours `FAIL`. Tolérances consommées depuis `Level1CManifest.non_regression_calibration` (`1e-15`/`1e-15`), jamais recopiées arbitrairement, aucune recalibration.

Réutilise `scripts/level1b_analysis/indexing.py` (`build_campaign_artifact_index`, `IndexedSpectralGroup`) et `scripts/level1c_calibration/tmax_nonregression.py::extract_historical_ctt_pairs`/`extract_historical_rho_pairs` (primitives génériques d'extraction de paires, aucune sémantique de holdout). Provenance vérifiée des deux côtés (`campaign_id`/`manifest_fingerprint`/`repository_commit` Level1C et Level1B). Artefact dédié `schemas/level1c/baseline-nonregression-v1.schema.json` (`schema_version=level1c-baseline-nonregression-v1`, ratifié) : 4 `case_comparisons` exactement, statuts `PASS`/`FAIL` uniquement (aucun `PARTIAL`), `E_CTT`/`E_RHO` diagnostiques globaux jamais utilisés pour dériver une nouvelle tolérance. `gate_status=PASS` ssi les 4 baselines sont présentes/valides (`run_status=success` ET `normative_case_valid=true`, sinon `FAIL` sans comparaison partielle silencieuse) ET toutes les comparaisons structurelles/CTT/rho passent ET la provenance est cohérente — un seul échec suffit, l'absence d'une seule baseline n'empêche jamais l'évaluation des trois autres.

Tests (52, entièrement synthétiques sauf lecture réelle en lecture seule de l'archive Level1B pour la preuve `T_3_2` et les scénarios d'intégration PASS/FAIL) : preuve d'exclusion (positionnel, exclusion réussie, aucun candidat, plusieurs candidats, collision avec une autre cible, `T_max` jamais exclu du calcul mais jamais comparé, rejet explicite de l'heuristique interdite) ; comparaisons structurelles/CTT/rho (bornes de tolérance `<`/`==`/`>`, `null`/`null` même raison/raison différente, `numeric`/`null`, jeux de paires différents, valeurs non finies, `representative_energy` seul jamais bloquant) ; intégration complète (4/4 correspondance exacte → `PASS`, une baseline manquante → `FAIL` sans bloquer les 3 autres, provenance incohérente → `FAIL`, `run_status`/`normative_case_valid` invalides → rejet avant comparaison, `T_max` jamais dans le périmètre REQUIRED) ; validation de schéma et sérialisation canonique. Résultats observés par Claude (non exécutés par ChatGPT) : `tests/scripts/level1c_baseline_gate` = 52 passed ; suite connexe (`tests/experiments + tests/scripts/level1c_campaign + tests/scripts/level1c_baseline_gate + tests/scripts/level1c_calibration + tests/scripts/level1c_preflight + tests/scripts/level1b_analysis + tests/scripts/level1b_campaign + tests/level1`) = 1371 passed ; suite complète du dépôt = 1980 passed, ~65-70s.

`NORMATIVE_PHASE_P_IMPLEMENTATION_READY=YES`, `BASELINE_NON_REGRESSION_GATE_IMPLEMENTATION_READY=YES`, `NORMATIVE_CAMPAIGN_IMPLEMENTATION_READY=NO` (aucune vraie gate exécutée, tracking inter-J0/phase R/campagne réelle restent des lots futurs distincts).

Fichiers de ce lot : `scripts/level1c_baseline_gate/__init__.py`, `scripts/level1c_baseline_gate/gate.py`, `schemas/level1c/baseline-nonregression-v1.schema.json`, `tests/scripts/level1c_baseline_gate/conftest.py`, `tests/scripts/level1c_baseline_gate/test_level1c_baseline_gate_exclusion_proof.py`, `tests/scripts/level1c_baseline_gate/test_level1c_baseline_gate_comparisons.py`, `tests/scripts/level1c_baseline_gate/test_level1c_baseline_gate_artifact.py`, `tests/scripts/level1c_baseline_gate/test_level1c_baseline_gate_t_3_2_historical_mapping_proof.py`, `docs/governance/current-task.md`. Aucun fichier Level1C/Level1B existant modifié (manifeste, planning, runner, outputs, schémas déjà acceptés tous inchangés).

**Audit ChatGPT : NON ACCEPTÉ EN L'ÉTAT.** Verdict détaillé : `D022_MAPPING=OK`, `STRUCTURAL_COMPARISON=OK`, `CTT_COMPARISON=OK`, `RHO_COMPARISON=OK`, `SYMMETRY_COMPARISON=OK`, `ARTIFACT_SCHEMA=OK`. La preuve D022 de `T_3_2` est définitivement acceptée et fermée. Blocker exact : `LEVEL1C_RECORDS_INTEGRITY_NOT_VERIFIED` — `load_level1c_baseline_case` vérifiait `run_status`/`normative_case_valid`/schéma/provenance de campagne, mais jamais que `records.jsonl` réellement lu correspondait à `run.json.records_sha256`/`record_count` — un `records.jsonl` modifié après production aurait pu être comparé malgré une rupture d'intégrité. Corrigé sans réouverture scientifique par `1C-8d-fix` ci-dessous.

## Lot 1C-8d-fix — vérification d'intégrité de l'artefact de cas Level1C (ce commit)

Corrige uniquement le blocker `LEVEL1C_RECORDS_INTEGRITY_NOT_VERIFIED` de 1C-8d. Aucun changement à la preuve D022/`T_3_2`, au périmètre de cibles, aux contrats CTT/rho/symétrie, à l'exclusion de l'énergie, aux tolérances, ou à la sémantique `PASS`/`FAIL` — tous inchangés et re-testés identiques.

`load_level1c_baseline_case` durci : (1) `run_document["case_id"] == case_id` vérifié explicitement ; (2) SHA-256 calculé sur les **bytes exacts** persistés de `records.jsonl` (`hashlib.sha256(records_path.read_bytes())`, jamais reconstruit depuis le JSON parsé) et comparé à `run.json.records_sha256` ; (3) nombre de documents effectivement chargés comparé à `run.json.record_count` ; (4) parsing réutilise désormais `scripts.level1c_campaign.outputs.load_case_records` (au lieu d'un filtre `if line` qui supprimait silencieusement toute ligne vide interne) — seule la newline finale conventionnelle est tolérée, toute ligne vide interne lève désormais une erreur ; (5) pour chaque record : `campaign_id`/`manifest_fingerprint`/`repository_commit` comparés à ceux de `run.json` (le schéma ne valide que la forme, jamais cette cohérence inter-artefacts) ; (6) identité scientifique (`geometry`/`spin`/`sector`/Hamiltonien complet) comparée au `CampaignCaseSpec` exact que `build_level1c_campaign_plan(manifest)` produit pour ce `case_id` — jamais une nouvelle règle d'identité, le même plan déjà gelé. Un `case_id` ne correspondant à aucun cas planifié réel est désormais explicitement rejeté (`BaselineGateInputError`), symétrique à l'exigence déjà en vigueur côté Level1B (`load_validated_cases`).

**Côté historique (§7 du mandat, audité avant toute modification)** : `HISTORICAL_RECORDS_INTEGRITY = VERIFIED_BY_EXISTING_INDEXER` — `build_campaign_artifact_index` route déjà par `scripts.level1b_analysis.loader.load_validated_cases`, qui exige déjà `scripts.level1b_campaign.outputs.validate_existing_case_run().is_valid` (SHA-256 sur bytes exacts, `record_count`, cohérence `campaign_id`/`manifest_fingerprint`/`repository_commit`/`case_id` par document) avant tout chargement. Aucune duplication ajoutée côté historique — seulement documentée.

Tests (13 nets ajoutés dans `test_level1c_baseline_gate_artifact.py`, réécrits pour utiliser des `case_id` réels issus de `build_level1c_campaign_plan` partout où une correspondance/chargement réussi est attendu) : hash/count valides → accepté ; `records.jsonl` modifié d'un octet → `FAIL` (SHA-256) ; `records_sha256` erroné dans `run.json` → `FAIL` ; `record_count` erroné → `FAIL` ; ligne vide interne (SHA-256 recalculé pour isoler ce cas précis) → `FAIL` ; `run.json.case_id` incohérent avec le répertoire → `FAIL` ; `campaign_id`/`manifest_fingerprint`/`repository_commit` de record incohérents avec `run.json` → `FAIL` (3 tests) ; `geometry`/`spin`/Hamiltonien de record incohérents avec le `CampaignCaseSpec` attendu → `FAIL` (3 tests) ; `case_id` absent du plan réel → `FAIL`. Résultats observés par Claude (non exécutés par ChatGPT) : `tests/scripts/level1c_baseline_gate` = 65 passed ; suite connexe = 1384 passed ; suite complète du dépôt = 1993 passed, ~65-70s.

Fichiers de ce lot : `scripts/level1c_baseline_gate/gate.py` (durci), `tests/scripts/level1c_baseline_gate/conftest.py` (étendu : helper `real_level1c_baseline_case_id`, fixture `level1c_case_ids`), `tests/scripts/level1c_baseline_gate/test_level1c_baseline_gate_artifact.py` (réécrit), `docs/governance/current-task.md`. Aucun runner, aucune diagonalisation, aucune vraie gate sur les 4 baselines, aucune campagne. Ce commit reste en attente d'audit, non présenté comme accepté.

**1C-8d + 1C-8d-fix → ACCEPTÉS DÉFINITIVEMENT ENSEMBLE** (`CURRENT_ACCEPTED_HEAD = 4f1ff7d648c26efee8bc867cf5a79e1b5e720b66`). La chaîne d'intégrité Level1C est gelée dans cet ordre exact et ne doit plus être réordonnée sans mandat explicite : `run.json` → `case_id` → `run_status` → `normative_case_valid` → `records_sha256` → `record_count` → provenance par record → identité scientifique exacte (`CampaignCaseSpec`) → comparaison baseline.

## Lot 1C-8e — tracking inter-J0 (ce commit)

Implémente PHASE_T, positionné exactement après la gate de non-régression baseline (§20.19) : `P -> BASELINE_NON_REGRESSION_GATE -> T`. Ce lot livre l'outil, son schéma et ses tests ; **aucune exécution normative réelle du tracking sur les 16 arêtes n'a été effectuée** (aucune campagne des 20 cas n'a été lancée).

**Deux questions de conception préalables résolues sans `DESIGN_BLOCKER`** :

1. `PERTURBED_NORMATIVE_CASE_VALIDITY_ROLE = IRRELEVANT_TO_STRUCTURAL_TRACKING`. `CLASS_POOL` (§20.12) est construit à partir de **tous** les groupes `complete_multiplet` de la fenêtre de production perturbée, sans filtrage par le succès de sélection de cible (§20.6, `SPECTRAL_STRUCTURE_PRODUCTION=ALL_COMPLETE_GROUPS_IN_PRODUCTION_WINDOW`) : `normative_case_valid` du côté perturbé n'est donc jamais consulté par `track_target_edge`. Seul `run_status == success` (fait technique) conditionne l'exploitabilité du côté perturbé. Un cas perturbé techniquement en échec produit `AMBIGUOUS` pour toutes ses cibles REQUIRED — jamais une exclusion silencieuse, jamais un nouveau statut inventé (« un calcul non résolu ne prouve ni présence ni absence », §20.12).
2. `REFLECTION_RESTRICTION_VALID_PERSISTENCE = PROVABLE_BY_NUMERIC_LABEL_AS_SUFFICIENT_WITNESS` (formulation corrigée par `1C-8e-fix`, voir ci-dessous — la formulation initiale `PROVABLE_EQUIVALENT_TO_NUMERIC_KIND` affirmait à tort une équivalence). Inspection directe de `cosmobox.level1.matching.compute_restricted_symmetry_label` (inchangé) : `kind==NUMERIC` ssi le défaut de commutateur (si applicable), le défaut de stabilité et le défaut d'unitarité sont tous sous tolérance. `reflection_restriction_valid` (§18.13, « stabilité + unitarité ») ne porte que sur les deux derniers de ces trois défauts : `NUMERIC` est donc un témoin **suffisant** que `reflection_restriction_valid` est vrai (jamais une réciproque) — un opérateur peut en principe être stable/unitaire mais rester `NOT_APPLICABLE` si le commutateur global échoue seul. Aucun champ `reflection_restriction_valid` séparé n'est persisté ni vérifié : `reflection_label.kind == NUMERIC` est utilisé directement partout comme ce témoin suffisant, à sens unique.

**Construction de `CLASS_POOL`** (`build_class_pool`, section 5/8 du mandat) : pour une branche baseline de twice_T/réflexion déjà résolus, un groupe perturbé `complete_multiplet` entre dans le pool ssi son propre `twice_T` est résolu et égal à celui de la baseline, ET sa réflexion est `NUMERIC` et concorde (`symmetry_labels_match`, `SYMMETRY_TOLERANCE` — jamais `1e-15`). Un groupe dont le `twice_T` ou la réflexion est non résolu ne peut jamais être exclu avec confiance : il est retourné séparément (`unresolved_candidates`), jamais fondu dans le pool ni silencieusement écarté. Multiplicité et translation ne sont jamais des critères d'identité (translation `NOT_AN_IDENTITY_REQUIREMENT`) ; `representative_energy`/rang spectral/`spectral_window_group_index` ne sont jamais des discriminants.

**Décision de statut** (`track_target_edge`) : identité baseline non résolue (`twice_T=None` ou réflexion non `NUMERIC`) → `AMBIGUOUS` avant toute construction de pool ; côté perturbé indisponible (technique ou intégrité) → `AMBIGUOUS` ; au moins un candidat perturbé pertinent non résolu → `AMBIGUOUS` (jamais fondu dans `class_pool_size`, qui ne compte que les membres prouvés) ; pool vide et tout résolu → `NOT_AVAILABLE` (seul chemin vers ce statut, §9) ; pool à 2+ membres → `AMBIGUOUS` ; pool à 1 membre et multiplicités égales → `TRACKED_ONE_TO_ONE` ; pool à 1 membre et multiplicités différentes → `AMBIGUOUS` (jamais `NOT_AVAILABLE`). `TRACKED_SPLIT_BRANCH`/`DISCONTINUOUS` réservés, jamais émis par ce lot.

**Périmètre** : 16 arêtes (`build_tracking_edges`, réutilisé verbatim, `INTER_J0_TRACKING_TOPOLOGY=BASELINE_CENTERED`, jamais chaîné) × cibles REQUIRED par géométrie (`triangle`={fundamental, first_excited}, `ring5`={fundamental, first_excited, T_3_2} — `REQUIRED_TARGET_IDS` réutilisé verbatim depuis `scripts.level1c_baseline_gate.gate`) = 40 `TrackingRecord` attendus (16 pour `triangle`, 24 pour `ring5`). `T_max` exclu, ne produit jamais de record. Chaque baseline (4) et chaque cas perturbé (16) est chargé/vérifié exactement une fois par `run_inter_j0_tracking`, jamais une fois par cible.

Chargement baseline (`load_baseline_for_tracking`) : réutilise `load_level1c_baseline_case` (gate, `run_status=success` ET `normative_case_valid=true`) + vérification de provenance externe (`campaign_id`/`manifest_fingerprint`/`repository_commit` attendus, comme `compare_baseline_case` le fait déjà côté gate). Chargement perturbé (`load_perturbed_for_tracking`) : `load_and_verify_case_run_document` + `run_status=success` uniquement + `load_and_verify_case_records` + même vérification de provenance externe — jamais `normative_case_valid`. Les deux fonctions partagées de `scripts.level1c_campaign.outputs` (`load_and_verify_case_run_document`/`load_and_verify_case_records`, extraites de `1C-8d-fix`'s `load_and_verify_case_artifact` **corrigé dans ce même lot** pour restaurer l'ordre gelé exact — voir ci-dessous) sont réutilisées par les deux côtés, jamais dupliquées.

**Correctif d'ordre de validation (auto-détecté avant tout code de tracking)** : le refactor initial de `scripts.level1c_campaign.outputs.load_and_verify_case_artifact` regroupait par erreur la vérification `records_sha256` avec `case_id` dans une seule fonction partagée, cassant l'ordre gelé (`case_id` → `run_status` → `normative_case_valid` → `records_sha256` → ...) : un test de la gate (`test_run_status_not_success_fails`) échouait avec une erreur SHA-256 au lieu de `run_status`. Corrigé en scindant la fonction en deux primitives (`load_and_verify_case_run_document` : `run.json` + `case_id` seul ; `load_and_verify_case_records` : SHA-256/`record_count`/provenance/identité scientifique), permettant à chaque appelant d'insérer son propre contrôle de validité (`run_status`/`normative_case_valid`) exactement entre les deux étapes, sans jamais dupliquer la logique lourde. `scripts/level1c_baseline_gate/gate.py::load_level1c_baseline_case` mis à jour en conséquence — aucun changement de comportement observable pour la gate elle-même, seulement la structure interne.

Artefact dédié `schemas/level1c/tracking-record-v1.schema.json` (`schema_version=level1c-tracking-record-v1`, ratifié). Contrat de nullité : `baseline_group_index`/`baseline_multiplicity`/`baseline_reflection_label` sont `None` ensemble ssi la branche baseline est totalement indisponible ; `baseline_twice_T` peut être `None` même si le groupe baseline est connu (échec de calcul de `twice_T` propre au groupe, distinct de l'indisponibilité) ; `candidate_*` peuplés ssi `status==TRACKED_ONE_TO_ONE` ; `class_pool_size` est `None` ssi `CLASS_POOL` n'a jamais été bien défini (baseline non résolue/indisponible, ou cas perturbé inexploitable), un entier ≥ 0 sinon ; `failure_reasons` vide ssi `status==TRACKED_ONE_TO_ONE`.

Tests (56, dont l'orchestration complète construit les 20 cas Level1C réels sur disque en fixtures synthétiques cohérentes) : `extract_complete_groups`/`build_class_pool` (exclusion `partial_subspace`, ordre déterministe, correspondance unique, `twice_T`/réflexion erronés exclus, translation/énergie/index jamais discriminants, candidats multiples, pool vide, candidat non résolu jamais exclu à tort, priorité de l'exclusion `twice_T` résolue sur la réflexion non résolue) ; `TrackingRecord` (contrat de nullité complet, rejet de statut réservé, validation de schéma pour les trois statuts émissibles) ; `track_target_edge` (les 17 scénarios du mandat : unique+même multiplicité, unique+multiplicité différente, deux candidats, pool vide propre, aucun groupe perturbé, baseline `twice_T`/réflexion non résolus, candidat perturbé pertinent non résolu, translation/énergie/index jamais bloquants, exclusions correctes, baseline indisponible, échec technique perturbé → `AMBIGUOUS` jamais `NOT_AVAILABLE`, `normative_case_valid=false` perturbé jamais consulté) ; chargement sur disque (succès/cas manquant/`normative_case_valid=false` rejeté/provenance incohérente pour la baseline ; succès/échec technique/`normative_case_valid=false` ignoré pour le perturbé) ; orchestration complète (exactement 40 records, répartition 16/24 par géométrie, ordre déterministe arêtes×cibles reproductible, échec technique d'un seul cas perturbé → `AMBIGUOUS` isolé sans affecter les autres, écriture/relecture de `tracking.jsonl`). Résultats observés par Claude (non exécutés par ChatGPT) : `tests/scripts/level1c_tracking` = 56 passed ; suite Level1C réunie (`tests/scripts/level1c_tracking + tests/scripts/level1c_baseline_gate + tests/scripts/level1c_campaign + tests/experiments/level1c`) = 285 passed ; suite complète du dépôt = 2049 passed, ~90s.

`TRACKING_IMPLEMENTATION_READY=YES`, `NORMATIVE_CAMPAIGN_IMPLEMENTATION_READY=NO` (aucun vrai tracking exécuté, phase R/campagne réelle restent des lots futurs distincts).

Fichiers de ce lot : `scripts/level1c_tracking/__init__.py` (nouveau), `scripts/level1c_tracking/tracking.py` (nouveau), `schemas/level1c/tracking-record-v1.schema.json` (nouveau), `scripts/level1c_campaign/outputs.py` (fonction partagée scindée en deux, voir correctif d'ordre ci-dessus), `scripts/level1c_baseline_gate/gate.py` (adapté au nouvel appel en deux étapes, comportement inchangé), `tests/scripts/level1c_baseline_gate/conftest.py` (helpers non-fixtures déplacés vers `level1c_baseline_gate_helpers.py`, voir correctif ci-dessous), `tests/scripts/level1c_baseline_gate/level1c_baseline_gate_helpers.py` (nouveau), `tests/scripts/level1c_baseline_gate/test_level1c_baseline_gate_artifact.py` (import corrigé), `tests/scripts/level1c_tracking/` (nouveau, 6 fichiers), `docs/governance/current-task.md`. Aucun runner, aucune diagonalisation, aucune vraie gate/tracking exécuté, aucune campagne réelle.

**Correctif additionnel (collision `conftest.py` inter-répertoires)** : exécuter `tests/scripts/level1c_baseline_gate` et `tests/scripts/level1c_campaign` ensemble faisait échouer un `from conftest import ...` dans `test_level1c_baseline_gate_artifact.py` — un import Python nu de `conftest` est un import de module ordinaire indexé par le nom nu dans `sys.modules`, qui entre en collision dès que deux répertoires de tests avec chacun leur propre `conftest.py` sont collectés dans la même invocation pytest (mécanisme pytest de `conftest.py` par répertoire ≠ import Python explicite). Corrigé en déplaçant tous les helpers non-fixtures de `conftest.py` vers un module au nom globalement unique (`level1c_baseline_gate_helpers.py`), ne laissant que des fixtures pytest dans `conftest.py` (les fixtures sont namespacées en sécurité par pytest lui-même, jamais importées explicitement par un module de test). Le même schéma est appliqué dès le départ pour `tests/scripts/level1c_tracking/` (`level1c_tracking_helpers.py`).

Ce commit reste en attente d'audit, non présenté comme accepté. Attends l'audit de ChatGPT. Aucun lot suivant ne démarre avant validation explicite.

**Audit ChatGPT sur 1C-8e (commit `a7de0a4163a1f1ceeeab22a0a090a8c29b98c4bc`) : CODE SCIENTIFIQUE ACCEPTABLE, PAS ENCORE ACCEPTÉ DÉFINITIVEMENT.** Verdict détaillé : `TRACKING_CORE=OK`, `CLASS_POOL=OK`, `UNRESOLVED_IDENTITY_SEMANTICS=OK`, `MULTIPLICITY_SEMANTICS=OK`, `TRANSLATION_EXCLUSION=OK`, `PERTURBED_NORMATIVE_CASE_VALIDITY_ROLE=ACCEPTED`. Blockers : `REFLECTION_VALIDITY_EQUIVALENCE_WORDING` (la formulation `PROVABLE_EQUIVALENT_TO_NUMERIC_KIND` affirmait à tort une équivalence bidirectionnelle entre `reflection_label.kind==NUMERIC` et `reflection_restriction_valid`, alors que `NUMERIC` exige en plus le défaut de commutateur — la relation correcte est une implication à sens unique, `NUMERIC → reflection_restriction_valid`, jamais la réciproque) + `STALE_CURRENT_ACCEPTED_HEAD` (le pointeur « Dernier commit accepté » était resté à `79c0b8b8a5f2208acb6c4b8776ef6323ad8bbd98`, antérieur à `1C-8d`, au lieu de `4f1ff7d648c26efee8bc867cf5a79e1b5e720b66`). Aucune logique scientifique du tracking mise en cause. Voir « Lot 1C-8e-fix » ci-dessous.

## Lot 1C-8e-fix — reflection witness wording + governance pointer (ce commit)

Corrige exclusivement les deux blockers de l'audit 1C-8e : `REFLECTION_VALIDITY_EQUIVALENCE_WORDING` et `STALE_CURRENT_ACCEPTED_HEAD`. Aucun changement de comportement scientifique ni structurel.

**Formulation corrigée** : `REFLECTION_RESTRICTION_VALID_PERSISTENCE = PROVABLE_BY_NUMERIC_LABEL_AS_SUFFICIENT_WITNESS` (remplace `PROVABLE_EQUIVALENT_TO_NUMERIC_KIND`), dans le docstring de module de `scripts/level1c_tracking/tracking.py` et dans la section « Lot 1C-8e » ci-dessus. `compute_restricted_symmetry_label` retourne `kind==NUMERIC` ssi défaut de commutateur (si applicable) ET défaut de stabilité ET défaut d'unitarité tous sous tolérance ; `reflection_restriction_valid` (§18.13) ne porte que sur les deux derniers de ces trois défauts. La preuve exacte retenue : `NUMERIC` implique `reflection_restriction_valid==true` (implication à sens unique, jamais une réciproque) — un opérateur peut en principe avoir une restriction stable/unitaire mais rester `NOT_APPLICABLE` parce que le commutateur global échoue seul, auquel cas `reflection_restriction_valid` pourrait être vrai sans que `kind==NUMERIC`. `CLASS_POOL`/`track_target_edge` inchangés : le contrat inter-J0 exige `NUMERIC` des deux côtés (baseline et candidat), et `NUMERIC → reflection_restriction_valid=true` suffit à garantir que cette condition n'admet jamais un candidat dont la restriction serait en réalité invalide — aucun champ `reflection_restriction_valid` séparé n'est ajouté, `build_class_pool` et `track_target_edge` ne sont pas modifiés.

**Pointeur de gouvernance corrigé** : « Dernier commit accepté » ramené à `4f1ff7d648c26efee8bc867cf5a79e1b5e720b66` (voir en tête de ce fichier) — `1C-8d`+`1C-8d-fix` restent le dernier jalon définitivement accepté ; `a7de0a4163a1f1ceeeab22a0a090a8c29b98c4bc` (1C-8e) et ce commit (1C-8e-fix) restent tous deux `pending ChatGPT audit`, non avancés comme pointeur accepté.

`PRE_COMMIT_WORKTREE_NOTE` : un état de travail non commité lié à 1C-8e (le refactor de `load_and_verify_case_artifact` en cours de correction) existait au moment de la reprise après compactage de contexte ; aucun fichier étranger au lot n'a été incorporé ; la livraison finale de 1C-8e est restée exactement un commit au-dessus du parent accepté (`4f1ff7d648c26efee8bc867cf5a79e1b5e720b66` → `a7de0a4163a1f1ceeeab22a0a090a8c29b98c4bc`). Aucune réécriture d'historique.

Tests : `tests/scripts/level1c_tracking` re-exécutés (aucun changement de comportement attendu, correctif de formulation uniquement) ; suite complète du dépôt re-exécutée par prudence. Aucune diagonalisation, aucun tracking réel, aucune gate réelle, aucune campagne.

Fichiers de ce lot : `scripts/level1c_tracking/tracking.py` (docstring de module uniquement), `docs/governance/current-task.md`. Aucun autre fichier touché — `build_class_pool`/`track_target_edge`/le schéma `tracking-record-v1`/les tests ne sont pas modifiés.

Ce commit reste en attente d'audit, non présenté comme accepté. Attends l'audit de ChatGPT. Aucun lot suivant ne démarre avant validation explicite.

**Audit ChatGPT sur 1C-8e-fix : `1C-8e` → ACCEPTÉ TEL QUE CORRIGÉ PAR `1C-8e-fix` ; `1C-8e-fix` → ACCEPTÉ DÉFINITIVEMENT.** `CURRENT_ACCEPTED_HEAD = 5c09039a33584b6a8592a4dceae486497d580f31`. Formulation désormais gelée : `REFLECTION_RESTRICTION_VALID_PERSISTENCE = PROVABLE_BY_NUMERIC_LABEL_AS_SUFFICIENT_WITNESS`, relation exacte `reflection_label.kind == NUMERIC → reflection_restriction_valid == true`, aucune réciproque revendiquée. `CLASS_POOL`/`track_target_edge` confirmés inchangés.

**Audit pré-implémentation `1C-8f` (PHASE_R) : `DESIGN_BLOCKED`, ACCEPTÉ.** Avant tout code, audit obligatoire de `identifiability-preregistration.md` §20 : `RESPONSE_STATUS_TAXONOMY=FROZEN` (§20.17 : `RESPONSE_ELIGIBILITY = RESPONSE_AVAILABLE | RESPONSE_NOT_AVAILABLE`), mais `RHO_RESPONSE_CONTRACT=NOT_FROZEN` (aucune formule de réponse `rho_QQ` — `Delta_rho`, ratio, changement relatif — n'est définie nulle part ; `rho_QQ` reste partout « contrôle de concordance, jamais une sonde primaire ») et `RESPONSE_ARTIFACT_SCHEMA=NOT_FROZEN` (aucune version de schéma ratifiée pour un artefact `R`). Conformément au mandat, STOP appliqué : aucun code, aucun schéma, aucun commit. Confirmé identique sur deux reprises consécutives du même mandat (aucun changement d'état entre-temps).

## Lot 1C-8f-design — gel conceptuel du contrat PHASE_R (read-only, accepté)

Ferme les deux éléments `NOT_FROZEN` identifiés par l'audit pré-implémentation, sans aucun code/schéma/commit (lot strictement documentaire/conceptuel, exécuté en conversation, jamais un fichier versionné séparé).

Décisions ratifiées : `RHO_RESPONSE_ROLE = PAIRWISE_CONCORDANCE_CONTROL` — `rho_QQ` reste REQUIRED en PHASE_R mais uniquement juxtaposé côte à côte (`rho_QQ_baseline(i,j)`, `rho_QQ_perturbed(i,j)`), jamais de `Delta_rho`/ratio/changement relatif/score. Null semantics P préservées exactement (`value`/`null_reason`, jamais de coercition `null→0`, jamais de verdict numeric/null). Pair-set stricte des deux côtés (`CTT` et `rho` séparément), sinon `RESPONSE_NOT_AVAILABLE`, jamais d'intersection silencieuse. `PRIMARY_RESPONSE_QUANTITY = Delta_C_TT` inchangé. `RESPONSE_AVAILABLE` gelé comme conjonction stricte de 10 conditions (tracking `TRACKED_ONE_TO_ONE`, intégrité P baseline/perturbée, sélection LOCALE de la cible sur le bon `group_index` des deux côtés, jeux de paires CTT/rho exacts, cross-check tracking/P). Point crucial (§20.17 rendu machine-actionable) : `tracking.candidate_group_index == perturbed.target_selections[target_id].spectral_window_group_index` avec `selection_status==selected` — sinon `RESPONSE_NOT_AVAILABLE` même si `TRACKED_ONE_TO_ONE`. `PERTURBED_NORMATIVE_CASE_VALIDITY_ROLE_IN_R = NOT_A_DIRECT_GATE` : le gate R est local par `target_id` (`selection_status==selected` ET `meets_normative_requirements==true` ET bon `group_index`), jamais le `normative_case_valid` global du cas — une défaillance d'une AUTRE cible REQUIRED du même cas ne contamine jamais cette réponse. Baseline plus stricte (`run_status=success` ET `normative_case_valid=true`, déjà exigé par la gate de non-régression). Artefact conceptuel : un `ResponseRecord` par tracking record (40→40, y compris `RESPONSE_NOT_AVAILABLE`, aucune disparition silencieuse), nom de schéma ratifié `level1c-response-record-v1` (disponible, aucune collision vérifiée), champs minimaux listés (identité, `tracking_status`, `response_eligibility`, `baseline_group_index`/`perturbed_group_index`, `delta_ctt_pairs`, `rho_pair_controls`, `failure_reasons`). Aucun résumé scalaire (`max_delta_ctt`/`mean_delta_ctt`/`response_score`/etc.) en v1. `INCIDENT_NON_INCIDENT_CLASSIFICATION = DEFERRED_WITHIN_R` (confirmé : §16.21 ne gèle qu'une partition binaire conceptuelle `NONLOCAL_RESPONSE_TEST=ADMISSIBLE`, aucun critère opérationnel). `PHYSICAL_RESPONSE_THRESHOLD=OPEN` et `G=DEFERRED` inchangés.

**Audit critique livré (contre `identifiability-preregistration.md`, le manifeste, `case_artifact.py`, `tracking.py`, `tracking-record-v1.schema.json`)** : aucune contradiction trouvée avec §20.17, avec le rôle historique de `rho_QQ`, avec la cardinalité target-edge (40), avec la disponibilité d'observables P pour la cible perturbée (`PHYSICAL_OBSERVABLE_PRODUCTION=PREREGISTERED_TARGETS_ONLY`, §20.6/20.7), avec la sémantique `normative_case_valid` (`_meets_normative_requirements` déjà calculée par cible, jamais par cas, confirmé dans `experiments/level1/target_selection.py`), ni collision de version/schéma. Notes non-bloquantes documentées : `load_baseline_for_tracking`/`load_perturbed_for_tracking` (1C-8e) couvrent déjà toute l'intégrité P nécessaire à R ; aucun chargeur/vérificateur de `tracking.jsonl` n'existait encore avant ce lot (implémenté par `1C-8f-impl` ci-dessous) ; la condition « baseline SELECTED sur `baseline_group_index` » est automatiquement garantie par la construction même de `TrackingRecord.baseline_group_index`, re-vérifiée en R par prudence seulement (« aucune confiance aveugle »).

**Audit ChatGPT sur 1C-8f-design : `ACCEPTÉ`.** `RHO_RESPONSE_CONTRACT_READY=YES`, `RESPONSE_ELIGIBILITY_CONTRACT_READY=YES`, `RESPONSE_ARTIFACT_DESIGN_READY=YES`, `INCIDENT_NON_INCIDENT_CAN_BE_DEFERRED=YES`, `PHASE_R_CORE_DESIGN_READY=YES`. `CURRENT_ACCEPTED_HEAD` reste `5c09039a33584b6a8592a4dceae486497d580f31` (lot read-only, aucun commit).

## Lot 1C-8f-impl — implémentation du cœur PHASE_R (ce commit)

Implémente `T -> R` pour les 40 tracking records normatifs, sans lancement scientifique réel. R v1 contient exclusivement : éligibilité de réponse, `Delta_C_TT`, contrôle de concordance pairwise `rho_QQ`. Exclus explicitement : classification incident/non-incident, seuil physique, `PHYSICAL_PASS`/`FAIL`, `G`, reconstruction géométrique, exécution de campagne réelle.

**Chargeur `tracking.jsonl` (manquant avant ce lot, comblé)** : `scripts/level1c_tracking/tracking.py::load_and_verify_tracking_records` (nouveau, `TrackingArtifactIntegrityError` nouveau) — valide chaque ligne JSON, chaque document contre `level1c-tracking-record-v1`, `campaign_id`/`manifest_fingerprint`/`repository_commit` exacts, puis compare la séquence COMPLÈTE `(perturbed_case_id, baseline_case_id, target_id)` à la séquence canonique reconstruite indépendamment depuis `build_level1c_campaign_plan` + `build_tracking_edges` + `REQUIRED_TARGET_IDS` (jamais l'ordre rencontré dans le fichier) — une seule comparaison d'égalité de séquence détecte simultanément manquant/dupliqué/en-trop/réordonné, sans jamais réparer ou réordonner silencieusement.

**Package `scripts/level1c_response/`** (`__init__.py`, `response.py`) :
- `ResponseRecord` (+ `DeltaCTTPair`, `RhoPairControl`) : contrat de nullité complet (`RESPONSE_AVAILABLE` ⟹ `tracking_status==TRACKED_ONE_TO_ONE`, `baseline_group_index`/`perturbed_group_index`/`delta_ctt_pairs`/`rho_pair_controls` tous non-`None`, `failure_reasons=()` ; `RESPONSE_NOT_AVAILABLE` ⟹ les deux payloads `None`, `failure_reasons` non-vide — jamais un payload scientifique partiel). `DeltaCTTPair.delta` auto-vérifié égal à `perturbed_value - baseline_value` (échec de provenance sinon, même discipline que `Level1CCaseRunArtifact.normative_case_valid`). `RhoPairControl` sans aucun champ `delta`/`ratio`/`relative_change`.
- `build_response_record` : chaîne de décision jamais levée en exception — `tracking_status != TRACKED_ONE_TO_ONE` → `RESPONSE_NOT_AVAILABLE` immédiat sans tentative d'extraction ; sinon gate baseline locale (`_target_locally_selected_on` : `selection_status==selected` ET `meets_normative_requirements==true` ET `group_index==tracking.baseline_group_index`) réutilisant `load_baseline_for_tracking` (intégrité + `run_status=success` + `normative_case_valid=true`) ; puis gate perturbée locale identique sur `tracking.candidate_group_index`, réutilisant `load_perturbed_for_tracking` (intégrité + `run_status=success` SEULEMENT — `normative_case_valid` jamais consulté, `PERTURBED_NORMATIVE_CASE_VALIDITY_ROLE_IN_R=NOT_A_DIRECT_GATE` implémenté exactement) ; puis cross-check structurel tracking/P (`multiplicity`/`twice_T`/label de réflexion re-dérivés des records P réellement chargés, comparés à la représentation exacte persistée dans `tracking.jsonl`, sans nouvelle tolérance — `symmetry_labels_match`/`SYMMETRY_TOLERANCE` déjà appliqués en T) ; puis extraction `C_TT_conn`/`rho_QQ` (`_extract_unique_ctt_pairs`/`_extract_unique_rho_pairs`, rejet explicite de tout doublon avant même la comparaison de jeux de paires, rejet de toute valeur non finie, jamais un choix arbitraire) ; égalité stricte des jeux de paires CTT puis rho (jamais une intersection) ; enfin `Delta_C_TT` calculé et `rho_pair_controls` juxtaposés, ordre canonique `(i,j)` lexicographique sur les paires réellement présentes.
- `run_phase_r` : charge `tracking.jsonl` (chargeur ci-dessus), charge chaque baseline/cas perturbé distinct exactement une fois (cache), produit exactement un `ResponseRecord` par tracking record, dans le même ordre.
- `write_response_records` : écriture atomique de `response.jsonl` (fichier temporaire/flush/fsync/`os.replace`, canonicalisation identique à `tracking.jsonl`), un document par ligne, chaque document schema-validé avant écriture. Aucun artefact de niveau campagne (`campaign.json`/`response-summary.json`/etc.) créé nulle part.

Schéma dédié `schemas/level1c/response-record-v1.schema.json` (`schema_version=level1c-response-record-v1`, seule version, aucune collision) : `if/then/else` sur `response_eligibility` reproduisant le contrat de nullité ; `$defs.deltaCttPair`/`$defs.rhoPairControl` sans aucun champ interdit (`delta`/`ratio`/`relative_change` uniquement côté CTT, jamais côté rho).

Tests (61) : extraction (doublon/valeur non finie/préservation null pour CTT et rho séparément) ; `ResponseRecord`/`DeltaCTTPair`/`RhoPairControl` (contrat de nullité complet, auto-cohérence de `delta`, validation de schéma pour les deux statuts, absence vérifiée de tout champ suggérant un concept interdit — physique/incident/gravité/`G`/`Delta_rho`/ratio) ; `build_response_record` (les scénarios du mandat : `TRACKED_ONE_TO_ONE` complet → disponible ; `AMBIGUOUS`/`NOT_AVAILABLE` → indisponible sans tentative d'extraction ; groupe perturbé structurellement suivi ≠ cible pré-enregistrée → indisponible [point crucial §20.17] ; `normative_case_valid` global faux mais cible locale valide → **disponible** [test explicite dédié] ; cible perturbée sélectionnée mais `meets_normative_requirements=false` → indisponible ; désaccord baseline/tracking → indisponible ; désaccords `multiplicity`/`twice_T`/réflexion tracking-vs-P → indisponible chacun ; jeux de paires CTT/rho exacts/manquants/en-trop/dupliqués → disponible/indisponible selon le cas ; arithmétique `Delta_C_TT` exacte ; les quatre combinaisons numeric/null de rho préservées avec raisons de chaque côté) ; chargeur `tracking.jsonl` (succès, fichier manquant, provenance incohérente, nombre ≠ 40, doublon, réordonnancement, ligne vide interne — tous rejetés) ; orchestration complète (40 tracking → 40 réponses, toutes disponibles avec fixture cohérente, répartition 16/24 par géométrie, ordre déterministe identique à `tracking.jsonl`, `Delta_C_TT` vérifié exactement égal à la différence de `J0`, écriture/relecture de `response.jsonl`, absence de tout artefact de niveau campagne). Résultats observés par Claude (non exécutés par ChatGPT) : `tests/scripts/level1c_response` = 61 passed ; suite Level1C réunie (`level1c_response + level1c_tracking + level1c_baseline_gate + level1c_campaign + experiments/level1c`) = 346 passed ; suite complète du dépôt = 2110 passed, ~70s.

`RESPONSE_IMPLEMENTATION_READY=YES`, `NORMATIVE_CAMPAIGN_IMPLEMENTATION_READY=NO` (aucune vraie diagonalisation, aucune vraie gate, aucun vrai tracking, aucune vraie réponse de campagne, aucune campagne des 20 cas exécutée par ce lot).

Fichiers de ce lot : `scripts/level1c_response/__init__.py` (nouveau), `scripts/level1c_response/response.py` (nouveau), `schemas/level1c/response-record-v1.schema.json` (nouveau), `scripts/level1c_tracking/tracking.py` (ajout de `TrackingArtifactIntegrityError`/`load_and_verify_tracking_records`, aucune modification de `build_class_pool`/`track_target_edge`), `tests/scripts/level1c_response/` (nouveau, 6 fichiers), `docs/governance/current-task.md`. Aucune diagonalisation, aucune vraie gate, aucun vrai tracking, aucune vraie réponse de campagne, aucune campagne.

Ce commit reste en attente d'audit, non présenté comme accepté. Le pointeur « Dernier commit accepté » reste `5c09039a33584b6a8592a4dceae486497d580f31` jusqu'à audit. Attends l'audit de ChatGPT. Aucun lot suivant ne démarre.

**Audit ChatGPT sur 1C-8f-impl (commit `4555f3f3e33544ac8715b46d7e9a49aefd64993f`) : cœur PHASE_R jugé cohérent avec `1C-8f-design`, acceptation suspendue pour deux écarts strictement contractuels entre les dataclasses Python et le schéma `level1c-response-record-v1`.** Aucune erreur scientifique/algorithmique dans le cœur PHASE_R. Écart 1 : `failure_reasons` pouvait rester vide sous `RESPONSE_NOT_AVAILABLE` alors que `ResponseRecord` l'interdit. Écart 2 : le schéma ne reproduisait pas l'équivalence `value == null ⟺ null_reason non vide` de `RhoPairControl`, indépendamment baseline/perturbed. Décision explicite : l'écart `i != j` (présent dans `DeltaCTTPair`/`RhoPairControl`, absent du schéma) est hors-périmètre de `1C-8f-fix` — JSON Schema Draft 2020-12 ne fournit aucune comparaison native entre deux propriétés sœurs pour l'exprimer proprement ; aucune action demandée sur ce point.

**Audit d'ingénierie préalable (read-only, accepté)** : les deux écarts confirmés empiriquement par validation locale directe (`jsonschema.Draft202012Validator`) — trois documents délibérément incohérents (`failure_reasons=[]` sous `RESPONSE_NOT_AVAILABLE` ; `rho` valeur numérique + raison non-null ; `rho` valeur null + raison null) produisaient zéro erreur contre le schéma réel tout en étant rejetés par `ResponseRecord`/`RhoPairControl`. Aucun test existant n'exerçait le schéma directement (seulement via des instances déjà valides des dataclasses), donc le gap n'avait jamais été détecté. Correctif minimal proposé et validé empiriquement dans un fichier de travail hors dépôt : ajout de `"failure_reasons": {"minItems": 1}` dans la branche `"else"` déjà existante, et ajout d'un `"allOf"` de deux `if/then/else` indépendants dans `$defs.rhoPairControl`, réutilisant à l'identique l'idiome déjà gelé de `result-record-v1.schema.json::$defs.normalizedMoment`. `1C-8f-fix AUDIT_READY`.

## Lot 1C-8f-fix — alignement du schéma `response-record-v1` sur les invariants dataclass (ce commit)

Corrige exclusivement les deux écarts confirmés par l'audit d'ingénierie préalable. Aucun changement de comportement scientifique/algorithmique, aucune modification de `scripts/level1c_response/response.py` (`ResponseRecord`/`DeltaCTTPair`/`RhoPairControl`/`build_response_record`/`run_phase_r` tous inchangés), aucune modification du tracking inter-J0/`CLASS_POOL`/sélection de cibles/`Delta_C_TT`/pair-set/`normative_case_valid`/manifeste/taxonomie/seuil physique/incident-non-incident/`G`.

**Correctif 1 (`failure_reasons` non vide sous `RESPONSE_NOT_AVAILABLE`)** : la branche `"else"` du `if/then/else` racine gagne `"failure_reasons": {"minItems": 1}`, aux côtés des contraintes déjà présentes (`delta_ctt_pairs`/`rho_pair_controls` à `null`) — aucune autre condition modifiée.

**Correctif 2 (équivalence `value`/`null_reason` pour `rho_pair_controls`)** : `$defs.rhoPairControl` gagne un `"allOf"` de deux `if/then/else` strictement indépendants (baseline, perturbed), chacun reproduisant l'idiome déjà gelé de `result-record-v1.schema.json::$defs.normalizedMoment` : `value=null` ⟹ `null_reason` est une chaîne non vide (`minLength:1`, exigence explicite du mandat, plus stricte que la simple proposition d'audit) ; `value≠null` ⟹ `null_reason=null` (`const:null`). Les deux règles ne s'influencent jamais l'une l'autre.

**Écart `i != j` explicitement hors-périmètre** : audité, confirmé de même nature (dataclass strict, schéma silencieux) mais non traité ici sur décision explicite — aucune tentative d'expression via un keyword custom ou une extension non standard.

Tests (7 nouveaux, JSON-directs — documents construits à la main, jamais via `ResponseRecord`/`RhoPairControl`, pour prouver que le schéma seul est désormais étanche, indépendamment des garde-fous Python) dans `tests/scripts/level1c_response/test_level1c_response_record.py` : `RESPONSE_NOT_AVAILABLE` + `failure_reasons=[]` → rejeté ; `RESPONSE_NOT_AVAILABLE` + `failure_reasons` non vide → accepté ; `rho` baseline numérique + raison non-null → rejeté ; `rho` baseline null + raison null → rejeté ; `rho` perturbed numérique + raison non-null → rejeté ; `rho` perturbed null + raison null → rejeté ; les quatre combinaisons valides (numeric/numeric, numeric/null, null/numeric, null/null-avec-raisons-des-deux-côtés) réunies dans un seul document → toutes acceptées. Résultats observés par Claude (non exécutés par ChatGPT) : `tests/scripts/level1c_response/test_level1c_response_record.py` = 24 passed (17 existants + 7 nouveaux) ; `tests/scripts/level1c_response` = 68 passed ; suite Level1C réunie (`level1c_response + level1c_tracking + level1c_baseline_gate + level1c_campaign + experiments/level1c`) = 353 passed ; suite complète du dépôt = 2117 passed, ~68s. Aucune régression.

Fichiers de ce lot : `schemas/level1c/response-record-v1.schema.json` (les deux ajouts ci-dessus, uniquement), `tests/scripts/level1c_response/test_level1c_response_record.py` (7 tests ajoutés), `docs/governance/current-task.md`. Aucun autre fichier modifié.

Ce commit reste en attente d'audit, non présenté comme accepté. Le pointeur « Dernier commit accepté » reste `5c09039a33584b6a8592a4dceae486497d580f31` (non avancé — `1C-8f-impl` et `1C-8f-fix` restent tous deux `pending`). Attends l'audit de ChatGPT puis la décision de Lionel. Aucun lot suivant ne démarre.

**Acceptation ultérieure** : `1C-8f-impl` + `1C-8f-fix` sont désormais `ACCEPTÉS DÉFINITIVEMENT ENSEMBLE` (`CURRENT_ACCEPTED_HEAD = 650ead1886b8db24f6eecd4c08c894b76bf35fa6`).

## Lots 1C-8g / 1C-8h (audits read-only, acceptés, aucun commit)

`1C-8g` : audit de l'orchestration normative `P -> BASELINE_NON_REGRESSION_GATE -> T -> R`. Constats principaux : `NORMATIVE_ORCHESTRATION_EXISTS = NO` (aucun `campaign.py`/`launch.py`/CLI Level1C n'existe) ; ni `run_inter_j0_tracking` ni `run_phase_r` ne consultent jamais l'artefact/verdict de la gate (`grep "gate_status"` vide dans `level1c_tracking`/`level1c_response`) — rien n'empêche aujourd'hui d'exécuter T/R après un `gate_status=FAIL` ; aucun writer d'artefact gate sur disque n'existe (`gate.py` a `to_json_dict`/`validate_gate_artifact_document` mais aucun `_atomic_write_bytes`) ; `NON_REGRESSION_CTT_ABS_TOL`/`NON_REGRESSION_RHO_ABS_TOL = 1e-15`/`1e-15` vérifiés réellement figés (constantes dans `experiments/level1c/manifest.py`, artefact de calibration réel présent sur disque, SHA-256 identique bit-à-bit à `CALIBRATION_ARTIFACT_SHA256`) ; `repository_commit` toujours un paramètre libre, jamais dérivé automatiquement de `git rev-parse HEAD` ; `NORMATIVE_CAMPAIGN_VALID` reste un concept textuel (§20.19), aucun artefact machine-readable, aucun n'est inventé. Décision de gouvernance issue de la revue : `BASELINE_NON_REGRESSION_GATE == FAIL -> STOP_NORMATIVE_PIPELINE_BEFORE_T` (frontière d'admission du futur launcher normatif ; `GATE_FAIL_DOWNSTREAM_DIAGNOSTICS = EXPLICIT_NON_NORMATIVE_ONLY` pour tout usage diagnostique hors launcher).

`1C-8h` : audit préalable de la conception de la boucle PHASE_P et de la reprise (avant l'implémentation ci-dessous). A précisément localisé l'écart entre `load_and_verify_case_run_document`/`load_and_verify_case_records` (vérifient uniquement la cohérence *interne* — jamais `campaign_id`/`manifest_fingerprint`/`repository_commit` contre une attente de l'appelant) et le besoin réel d'un validateur de reprise, et a défini l'algorithme exact de reprise (chaîne à 4 étapes, voir ci-dessous), en s'appuyant sur `scripts/level1b_campaign/campaign.py`/`outputs.py::validate_existing_case_run` comme patron d'ingénierie uniquement (jamais une source normative Level1C, jamais un import direct).

## Lot 1C-8h — boucle normative PHASE_P et reprise (ce commit)

Implémente exclusivement la boucle PHASE_P sur les 20 cas et son mécanisme de reprise, conformément à la conception acceptée du même nom. Aucune orchestration gate/T/R, aucun launcher global, aucun artefact global (`NORMATIVE_CAMPAIGN_VALID` non implémenté, hors-périmètre).

**Validateur de reprise** (`scripts/level1c_campaign/outputs.py::validate_existing_level1c_case_run`, nouveau, + `Level1CCaseRunValidation(is_valid, reason)`, nouveau) : réutilise `load_and_verify_case_run_document`/`load_and_verify_case_records` verbatim (aucun troisième parseur JSON/JSONL), capture `Level1CArtifactIntegrityError` et la convertit en `is_valid=False` — jamais une exception pour un « non réutilisable » simple. Chaîne exacte : (1) `run.json` existe/parse/schéma-valide/`case_id` cohérent (via `load_and_verify_case_run_document`) ; (2) `run_status == "success"` (toute autre valeur, y compris `failed`/`resource_guardrail_exceeded` → non réutilisable, retenté automatiquement à la prochaine reprise — aucun état terminal, cohérent avec `RUN_STATUS_SEMANTICS=TECHNICAL_EXECUTION_STATUS`, 1C-8c-fix) ; (3) **vérification externe ajoutée** : `campaign_id`/`manifest_fingerprint`/`repository_commit` du `run.json` comparés aux valeurs *attendues par l'appelant* (`level1c_manifest.campaign_id`/`.fingerprint`, `repository_commit` du paramètre) — le trou précisément identifié par l'audit 1C-8h, comblé ici avec le même motif à 3 lignes déjà appliqué deux fois par `scripts/level1c_tracking/tracking.py` (`load_baseline_for_tracking`/`load_perturbed_for_tracking`) ; (4) `records.jsonl` SHA-256/`record_count`/schéma/identité scientifique (via `load_and_verify_case_records`). `normative_case_valid` n'est **jamais** consulté : un `run_status=success` structurellement intact et de provenance correcte est réutilisable quelle que soit sa valeur — ce critère appartient exclusivement à PHASE_P, jamais à la baseline gate ou au tracking, qui appliquent leurs propres critères plus stricts sur ce même artefact.

**Boucle** (`scripts/level1c_campaign/campaign.py`, nouveau) : `run_level1c_campaign(manifest, *, output_dir, repository_commit) -> Level1CCampaignExecutionReport`, mirror exact de `scripts/level1b_campaign/campaign.py::run_campaign` (patron d'ingénierie uniquement, jamais un import direct — même raisonnement déjà documenté dans `outputs.py` pour `_atomic_write_bytes`). Boucle sur `build_level1c_campaign_plan(manifest)` (ordre du plan, jamais reconstruit) ; par cas : `validate_existing_level1c_case_run` → skip si valide ; sinon garde-fou ressource défensif (`_exceeds_resource_guardrail`, jamais déclenché par la grille réelle : dimension max `1504` ≪ `max_sparse_dimension=200000`) ; sinon `run_level1c_case`+`write_case_success` sous `except Exception` (jamais `except BaseException` — `KeyboardInterrupt`/`SystemExit` toujours propagés, testé explicitement) ; un échec ordinaire de `run_level1c_case` **ou** de `write_case_success` est capturé de façon identique (conforme au comportement déjà accepté de Level1B, vérifié par relecture de son propre test `test_write_case_success_failure_yields_failed` : les deux deviennent un échec `failed` isolé via `write_case_failure`, jamais un cas spécial) ; un échec de `write_case_failure` lui-même n'est **jamais** capturé, il propage et abandonne la boucle (l'état durable du cas n'est plus garanti).

**Rapport en mémoire** (`Level1CCaseOrchestrationOutcome(case_id, status, errors)` + `Level1CCampaignExecutionReport(case_outcomes, total_required, executed_success_count, reused_success_count, resource_guardrail_exceeded_count, failed_count, global_success)`, nouveaux, shape identique à Level1B) : jamais persisté sur disque (aucun nouveau schéma, aucun fichier de campagne global). `global_success` reste purement technique — ne signifie jamais `NORMATIVE_CAMPAIGN_VALID=YES`.

Tests (44 nouveaux : 14 dans `test_level1c_outputs.py` pour le validateur — fichier absent, JSON invalide, SHA mismatch, `record_count` incohérent, `campaign_id`/`manifest_fingerprint`/`repository_commit` différents [les 3 preuves explicites du trou comblé], `run_status=failed`/`resource_guardrail_exceeded` non réutilisables, `run_status=success`+`normative_case_valid=false` réutilisable, jamais une exception pour un cas ordinaire invalide, `Level1CCaseRunValidation` auto-vérifiée ; 30 dans le nouveau `test_level1c_campaign.py` pour la boucle — les 20 cas réels du plan réel parcourus/exécutés dans l'ordre exact [aucune diagonalisation : seuls `run_level1c_case`/les writers/le validateur sont monkeypatchés, `build_level1c_campaign_plan` reste réel], skip total/exécution totale/mélange, aucun writer appelé pour un skip, échec `run_level1c_case`/`write_case_success` → `failed` isolé + cas suivants exécutés, message d'erreur jamais vide, `KeyboardInterrupt`/`SystemExit` jamais avalés, échec de `write_case_failure` propagé, garde-fou ressource synthétique, `repository_commit`/provenance manifeste propagés exactement à `validate_existing_level1c_case_run`, `repository_commit` vide rejeté, compteurs exacts, dataclasses de rapport rejetant une incohérence interne/un statut inconnu/des `case_id` dupliqués). Résultats observés par Claude (non exécutés par ChatGPT) : `tests/scripts/level1c_campaign` = 83 passed ; suite Level1C réunie (`level1c_campaign + level1c_baseline_gate + level1c_tracking + level1c_response + experiments/level1c`) = 396 passed ; suite complète du dépôt = 2160 passed, ~68s. Aucune régression.

`P_CAMPAIGN_LOOP_IMPLEMENTED=YES`, `P_RESUME_IMPLEMENTED=YES`, `P_EXTERNAL_PROVENANCE_CHECK=YES`, `REAL_NORMATIVE_CAMPAIGN_EXECUTED=NO`, `NORMATIVE_ORCHESTRATION_EXISTS=NO` (ce lot ne construit que PHASE_P multi-cas ; l'orchestrateur global `P -> gate -> T -> R`, l'application effective de `STOP_NORMATIVE_PIPELINE_BEFORE_T`, le writer de gate, et le launcher/CLI restent des lots futurs distincts, non ouverts ici).

Fichiers de ce lot : `scripts/level1c_campaign/outputs.py` (ajout de `Level1CCaseRunValidation`/`validate_existing_level1c_case_run` uniquement), `scripts/level1c_campaign/campaign.py` (nouveau), `tests/scripts/level1c_campaign/test_level1c_outputs.py` (14 tests ajoutés), `tests/scripts/level1c_campaign/test_level1c_campaign.py` (nouveau, 30 tests), `docs/governance/current-task.md`. Aucune modification de `runner.py`, `experiments/level1c/planning.py`, `experiments/level1c/manifest.py`, d'aucun schéma. Aucune vraie campagne lancée, aucune diagonalisation des 20 cas réels, aucune gate/tracking/réponse/PHASE_G ouverts.

Ce commit reste en attente d'audit, non présenté comme accepté. Le pointeur « Dernier commit accepté » reste `650ead1886b8db24f6eecd4c08c894b76bf35fa6` (non avancé). Attends l'audit de ChatGPT puis la décision de Lionel. Aucun lot suivant ne démarre.

**Audit ChatGPT sur 1C-8h (commit `72fa7ebf9168fe75c6282015912f543bc21d019f`)** : cœur accepté sur le fond (`P_CAMPAIGN_LOOP=OK`, `P_RESUME=OK`, `P_EXTERNAL_PROVENANCE_CHECK=OK`, `EXCEPTION_SEMANTICS=OK`, `SCIENTIFIC_DRIFT=NONE`), acceptation définitive suspendue pour un seul défaut mécanique : un second `return tuple(documents)` inatteignable, introduit accidentellement après le `return Level1CCaseRunValidation(is_valid=True, reason=None)` final de `validate_existing_level1c_case_run` — aucun effet comportemental ou scientifique, corrigé par `1C-8h-fix` ci-dessous.

## Lot 1C-8h-fix — suppression du code mort résiduel (ce commit)

Supprime exclusivement la ligne `return tuple(documents)` inatteignable (et la ligne vide qui la précédait) après le retour final de `validate_existing_level1c_case_run` (`scripts/level1c_campaign/outputs.py`). Le `return tuple(documents)` légitime de `load_case_records` reste inchangé. Aucune autre modification — diff fonctionnel réel : suppression de 2 lignes.

Tests : `tests/scripts/level1c_campaign` = 83 passed (inchangé) ; suite Level1C réunie (`level1c_campaign + level1c_baseline_gate + level1c_tracking + level1c_response + experiments/level1c`) = 396 passed (inchangé) ; suite complète du dépôt = 2160 passed (inchangé), ~71s. Aucune régression, comme attendu pour un correctif de code mort.

`DEAD_CODE_REMOVED=YES`, `PHASE_P_BEHAVIOR_CHANGED=NO`, `SCIENTIFIC_BEHAVIOR_CHANGED=NO`, `REAL_NORMATIVE_CAMPAIGN_EXECUTED=NO`.

Fichiers de ce lot : `scripts/level1c_campaign/outputs.py` (suppression de 2 lignes uniquement), `docs/governance/current-task.md`. Aucun autre fichier touché.

Ce commit reste en attente d'audit, non présenté comme accepté. Le pointeur « Dernier commit accepté » reste `650ead1886b8db24f6eecd4c08c894b76bf35fa6` (non avancé tant que ChatGPT n'a pas audité ce correctif). Attends l'audit de ChatGPT puis la décision de Lionel. Aucun lot suivant ne démarre.

**Acceptation ultérieure** : `1C-8h` + `1C-8h-fix` sont désormais `ACCEPTÉS DÉFINITIVEMENT ENSEMBLE` (`CURRENT_ACCEPTED_HEAD = b9b0a21262e26a945b974cfbdcdb1a1ffbc3867c`). État des briques : `PHASE_P=IMPLEMENTED`, `P_CAMPAIGN_LOOP=IMPLEMENTED`, `P_RESUME=IMPLEMENTED`, `BASELINE_NON_REGRESSION_GATE=IMPLEMENTED`, `PHASE_T=IMPLEMENTED`, `PHASE_R_CORE=IMPLEMENTED` ; mais `NORMATIVE_ORCHESTRATION_EXISTS=NO`, `REAL_NORMATIVE_CAMPAIGN_EXECUTED=NO`, `PHASE_G_READY=NO`.

## Lot 1C-8i (audit read-only, accepté, aucun commit)

Audit préalable du launcher normatif global Level1C (`P -> BASELINE_NON_REGRESSION_GATE -> T -> R`, jamais `PHASE_G`). A lu intégralement `scripts/level1b_campaign/launch.py` comme précédent d'ingénierie uniquement, confirmé `check_repository_cleanliness`/`NORMATIVE_REPOSITORY_PATHS` réutilisables verbatim (génériques, aucun contenu Level1B-spécifique), et tranché les points d'architecture explicitement audités : frontière `P -> gate` (`P_GLOBAL_SUCCESS==true -> GATE_ALLOWED`, sinon `STOP_BEFORE_GATE` — motivé par le risque déjà documenté en 1C-8g qu'un cas P manquant soit confondu avec une ambiguïté scientifique par T) ; persistance de la gate (aucun nom normatif n'existait — décision d'ingénierie : `<output_dir>/baseline-nonregression.json`, un seul objet JSON) ; politique `output_dir` (`results/` interdit uniquement à sa racine exacte — jamais une sous-arborescence —, `results/level1c/campaign/` explicitement autorisé, justifié par la séparation des espaces d'artefacts Level1C, jamais le vocabulaire « legacy » de Level1B) ; TOCTOU (narrowing jamais fermeture complète, sans verrou Git) ; reprise T/R (`T_R_CACHE_REUSE_IMPLEMENTED=NO`, différée, jamais un cache inventé — T/R sont des recalculs purs, déterministes, sans diagonalisation).

## Lot 1C-8j — launcher normatif global Level1C (ce commit)

Implémente le point d'entrée normatif unique Level1C, conformément à la conception acceptée `1C-8i` et aux décisions ratifiées après revue.

**Writer de gate** (`scripts/level1c_baseline_gate/gate.py::write_gate_artifact`, nouveau, + `GATE_ARTIFACT_FILENAME="baseline-nonregression.json"`) : sérialise l'artefact déjà calculé via `to_json_dict`/`canonical_json_bytes` (inchangés), valide le document contre le schéma, puis écrit atomiquement — ne recalcule jamais la gate elle-même. Un échec de validation ou d'écriture propage toujours.

**Package `scripts/level1c_launcher/`** (`__init__.py`, `launch.py`, nouveau — jamais imbriqué dans `level1c_campaign/`, puisqu'il traverse 4 paquets distincts) :
- `NormativeLaunchError(message, dirty_paths=())`, `Level1CNormativeLaunchContext(repository_commit, branch, manifest, output_dir, historical_output_dir)` (auto-vérifié : SHA 40-hex, branche non vide/≠`"HEAD"`, `branch==manifest.branch`, `output_dir`/`historical_output_dir` absolus résolus).
- `prepare_normative_launch` : HEAD/branche/manifeste (`load_manifest()`, jamais de chemin alternatif)/propreté (`check_repository_cleanliness`/`NORMATIVE_REPOSITORY_PATHS` réutilisés **verbatim**, sémantique inchangée)/`output_dir` (interdits : `repo_root` lui-même, `repo_root/results` **exact** uniquement — `results/level1c/campaign/` explicitement accepté —, tout chemin sous `src/`,`docs/`,`schemas/`,`experiments/`,`scripts/`)/`historical_output_dir` (doit exister comme répertoire — chemin opérationnel, jamais un paramètre scientifique).
- `launch_normative_campaign` : **trois** vérifications Git (préparation, immédiatement avant `PHASE_P`, immédiatement après `PHASE_P`/avant la gate) — toute dérive de HEAD/branche/propreté à l'une des trois lève `NormativeLaunchError`, jamais une continuation avec une provenance différente ; `repository_commit` reste toujours celui résolu à la préparation, jamais remplacé par une relecture ultérieure (les relectures ne servent qu'à l'égalité, jamais à l'adoption).
- Orchestration stricte : `run_level1c_campaign` (P, jamais de reconstruction du plan) → si `global_success==false` : retour immédiat `pipeline_status=STOP_BEFORE_GATE`, gate/T/R jamais appelés → sinon `run_baseline_nonregression_gate` → `write_gate_artifact` (persistée **avant** toute inspection de `gate_status`, jamais recalculée) → si `gate_status==FAIL` : `pipeline_status=STOP_NORMATIVE_PIPELINE_BEFORE_T`, T/R jamais appelés (aucun diagnostic aval dans ce launcher, `GATE_FAIL_DOWNSTREAM_DIAGNOSTICS=EXPLICIT_NON_NORMATIVE_ONLY` reste un outil séparé, non ouvert) → sinon `run_inter_j0_tracking` → `write_tracking_records` (échec propagé, R jamais appelé si cette écriture échoue) → `run_phase_r` → `write_response_records` → `pipeline_status=COMPLETED`. `T_R_CACHE_REUSE_IMPLEMENTED=NO` : T/R toujours entièrement recalculés et réécrits, jamais réutilisés, même si P était entièrement `skipped_existing_valid`.
- `Level1CNormativeLaunchReport(phase_p_report, gate_artifact, tracking_records, response_records, pipeline_status)` : purement en mémoire, jamais persistée, jamais de champ `NORMATIVE_CAMPAIGN_VALID` ni de verdict physique. `__post_init__` interdit toute combinaison impossible des trois statuts (`STOP_BEFORE_GATE` ⟹ gate/tracking/response absents ; `STOP_NORMATIVE_PIPELINE_BEFORE_T` ⟹ gate présente et `FAIL`, tracking/response absents ; `COMPLETED` ⟹ P `global_success`, gate présente et `PASS`, tracking et response présents).
- Aucune exception de P/gate/T/R ou de leurs writers n'est jamais convertie en `NormativeLaunchError` (`except Exception` volontairement absent autour de l'orchestration elle-même) — seules les primitives de précondition (git, `load_manifest`, `check_repository_cleanliness`) sont normalisées.

**CLI mince** (`scripts/run_level1c_campaign.py`, nouveau) : `--output-dir` et `--historical-output-dir` (obligatoires, chemins opérationnels uniquement), `--repo-root` (optionnel). Aucune option J0/S/target/`production_window`/seuil/`repository_commit`/branche/manifeste alternatif.

Tests (75 nouveaux : 6 dans `tests/scripts/level1c_baseline_gate/test_level1c_baseline_gate_writer.py` pour le writer de gate ; 69 dans `tests/scripts/level1c_launcher/` répartis en préconditions [HEAD/branche/propreté/`output_dir`/`historical_output_dir`, y compris `results/level1c/campaign/` accepté et `results/` bare rejeté], TOCTOU+orchestration [dérive avant P, dérive après P, `P global_success=false` → `STOP_BEFORE_GATE` sans appel gate/T/R, `gate FAIL` → artefact persisté puis `STOP_NORMATIVE_PIPELINE_BEFORE_T` sans T/R, `gate PASS` → T→write→R→write→`COMPLETED` dans l'ordre exact, R jamais appelé si l'écriture de `tracking.jsonl` échoue, `repository_commit`/manifeste identiques à travers toutes les phases, relance avec P entièrement réutilisé → gate/T/R quand même exécutés et T/R quand même recalculés], rapport [combinaisons impossibles rejetées pour les trois statuts], CLI [aucune option scientifique, codes de sortie]). Résultats observés par Claude (non exécutés par ChatGPT) : `tests/scripts/level1c_launcher` = 69 passed ; `tests/scripts/level1c_baseline_gate` = 71 passed ; suite Level1C réunie (`level1c_launcher + level1c_baseline_gate + level1c_campaign + level1c_tracking + level1c_response + experiments/level1c`) = 471 passed ; suite complète du dépôt = 2235 passed, ~72s. Aucune régression, aucune vraie campagne exécutée, aucune diagonalisation.

`LEVEL1C_NORMATIVE_LAUNCHER_IMPLEMENTED=YES`, `GATE_ARTIFACT_PERSISTENCE_IMPLEMENTED=YES`, `P_FAILURE_STOPS_BEFORE_GATE=YES`, `GATE_FAIL_STOPS_BEFORE_T=YES`, `TOCTOU_POST_P_CHECK_IMPLEMENTED=YES`, `T_R_CACHE_REUSE_IMPLEMENTED=NO`, `REAL_NORMATIVE_CAMPAIGN_EXECUTED=NO`, `PHASE_G_OPENED=NO`.

Fichiers de ce lot : `scripts/level1c_baseline_gate/gate.py` (ajout de `write_gate_artifact`/`GATE_ARTIFACT_FILENAME` uniquement), `scripts/level1c_launcher/__init__.py` (nouveau), `scripts/level1c_launcher/launch.py` (nouveau), `scripts/run_level1c_campaign.py` (nouveau), `tests/scripts/level1c_baseline_gate/test_level1c_baseline_gate_writer.py` (nouveau), `tests/scripts/level1c_launcher/` (nouveau, 5 fichiers), `docs/governance/current-task.md`. Aucune modification scientifique de PHASE_P/comparaison baseline/tracking/response. Aucune vraie campagne lancée, aucun préflight relancé, aucune `PHASE_G` ouverte.

Ce commit reste en attente d'audit, non présenté comme accepté. Le pointeur « Dernier commit accepté » reste `b9b0a21262e26a945b974cfbdcdb1a1ffbc3867c` (non avancé). Attends l'audit de ChatGPT puis la décision de Lionel. Aucun lot suivant ne démarre.

**Audit ChatGPT sur 1C-8j (commit `3cab331f9cc614c64dba512b6259895355f62384`) : cœur du launcher accepté sur le fond** (`LAUNCHER_CORE=OK`, `P_TO_GATE=OK`, `GATE_PERSISTENCE=OK`, `GATE_TO_T=OK`, `T_TO_R=OK`, `TOCTOU_POLICY=OK`, `PROVENANCE=OK`, `PHASE_G=NOT_OPENED`), acceptation définitive suspendue pour un seul défaut de contrat : `Level1CNormativeLaunchReport.__post_init__` n'imposait pas `STOP_BEFORE_GATE -> phase_p_report.global_success==false` ni `STOP_NORMATIVE_PIPELINE_BEFORE_T -> phase_p_report.global_success==true` — le launcher réel construisait déjà correctement ces états, seule l'étanchéité du contrat de la dataclass était en cause. Corrigé par `1C-8j-fix` ci-dessous.

## Lot 1C-8j-fix — durcissement des invariants Level1CNormativeLaunchReport (ce commit)

Ajoute exactement les deux contrôles manquants dans `Level1CNormativeLaunchReport.__post_init__` (`scripts/level1c_launcher/launch.py`) : `pipeline_status==STOP_BEFORE_GATE` exige désormais `phase_p_report.global_success==False` ; `pipeline_status==STOP_NORMATIVE_PIPELINE_BEFORE_T` exige désormais `phase_p_report.global_success==True`. Aucune autre logique modifiée (`gate_artifact`/`tracking_records`/`response_records`/`COMPLETED` inchangés), aucune modification de `launch_normative_campaign`, `prepare_normative_launch`, des helpers Git, de la politique TOCTOU, ou d'aucune autre phase.

Tests (2 nouveaux dans `tests/scripts/level1c_launcher/test_level1c_launcher_report.py`) : `STOP_BEFORE_GATE` + `global_success=true` → `ValueError` ; `STOP_NORMATIVE_PIPELINE_BEFORE_T` + `global_success=false` → `ValueError`. Tests positifs existants inchangés (déjà conformes au contrat durci). Résultats observés par Claude (non exécutés par ChatGPT) : `tests/scripts/level1c_launcher` = 71 passed (69+2) ; suite Level1C réunie (`level1c_launcher + level1c_baseline_gate + level1c_campaign + level1c_tracking + level1c_response + experiments/level1c`) = 473 passed ; suite complète du dépôt = 2237 passed, ~66s. Aucune régression.

`STOP_BEFORE_GATE_P_INVARIANT=ENFORCED`, `STOP_BEFORE_T_P_INVARIANT=ENFORCED`, `LAUNCHER_ORCHESTRATION_CHANGED=NO`, `SCIENTIFIC_BEHAVIOR_CHANGED=NO`, `REAL_NORMATIVE_CAMPAIGN_EXECUTED=NO`, `PHASE_G_OPENED=NO`.

Fichiers de ce lot : `scripts/level1c_launcher/launch.py` (2 contrôles ajoutés uniquement), `tests/scripts/level1c_launcher/test_level1c_launcher_report.py` (2 tests ajoutés), `docs/governance/current-task.md`. Aucun autre fichier touché.

Ce commit reste en attente d'audit, non présenté comme accepté. Le pointeur « Dernier commit accepté » reste `b9b0a21262e26a945b974cfbdcdb1a1ffbc3867c` (non avancé). Attends l'audit de ChatGPT puis la décision de Lionel. Aucun lot suivant ne démarre.

## Lot 1C-8k — gel méthodologique du verdict de réponse physique (ce commit, documentaire uniquement)

Abandonne, après réexamen scientifique, la notion de seuil physique scalaire binaire pour Level1C. Dans `docs/levels/level1c/identifiability-preregistration.md` : §20.27 réécrit (« abandon méthodologique (1C-8k, gelé) ») pour figer `PHYSICAL_RESPONSE_THRESHOLD = NOT_APPLICABLE` (jamais résolu par un choix de valeur — retiré comme catégorie de décision), la distinction non négociable `NUMERICAL_REPRODUCIBILITY != PHYSICAL_RESPONSE_MAGNITUDE`, la confirmation que `NON_REGRESSION_CTT_ABS_TOL`/`NON_REGRESSION_RHO_ABS_TOL` (`1e-15`/`1e-15`) restent exclusivement des garde-fous numériques de reproductibilité (jamais un seuil physique, ni directement ni par multiplication arbitraire), que `Delta_C_TT(i,j)` reste une grandeur continue descriptive jamais binarisée/scorée/normalisée physiquement, et que la future hiérarchie interprétative `H0`–`H4` n'est nommée qu'à titre de repère conceptuel — aucune définition opérationnelle, aucun moteur de classification, `PHASE_R` n'en calcule/stocke/automatise aucun. Pointeurs de supersession ajoutés à `§16.23` (introduction historique de `OPEN`) et à `§20.31` (occurrence textuellement postérieure qui aurait sinon semblé contredire §20.27) — aucune réécriture d'historique, chaque section datée reste un instantané fidèle à son propre lot.

**Audit read-only préalable de PHASE_R** (§6 du mandat) : confirmé par lecture directe de `scripts/level1c_response/response.py` — `delta = perturbed_ctt[(i,j)] - baseline_ctt[(i,j)]` (différence arithmétique directe, `DeltaCTTPair.__post_init__`) ; aucune référence à `NON_REGRESSION_CTT_ABS_TOL`/`NON_REGRESSION_RHO_ABS_TOL` nulle part dans ce module (`grep` vide) ; aucun champ/mécanisme `threshold`/`score`/`significance` (`grep` vide hors docstring). Les quatre points du mandat sont vrais : `IMPLEMENTATION_CHANGE_REQUIRED = NO`, `AUDIT_PHASE_R = PASS`. Aucune modification de code.

`PHYSICAL_RESPONSE_THRESHOLD=NOT_APPLICABLE`, `RESPONSE_MAGNITUDE_POLICY=CONTINUOUS`, `NUMERICAL_TOLERANCE_ROLE=REPRODUCIBILITY_ONLY`, `PHASE_R_BEHAVIOR_CHANGED=NO`, `NORMATIVE_LAUNCHER_CHANGED=NO`, `NEW_SCIENTIFIC_SCORING=NO`, `REAL_NORMATIVE_CAMPAIGN_EXECUTED=NO`, `PHASE_G_OPENED=NO`.

Fichiers de ce lot : `docs/levels/level1c/identifiability-preregistration.md` (§16.23, §20.27, §20.31), `docs/governance/current-task.md`. Aucun fichier de code touché — `scripts/level1c_response/*`, `scripts/level1c_tracking/*`, `scripts/level1c_launcher/*`, `scripts/level1c_baseline_gate/*` tous inchangés.

Ce commit reste en attente d'audit, non présenté comme accepté. Le pointeur « Dernier commit accepté » reste `b9b0a21262e26a945b974cfbdcdb1a1ffbc3867c` (non avancé). Aucune campagne normative réelle lancée, aucune `PHASE_G` ouverte, aucun lot suivant démarré. Attends l'audit de ChatGPT puis la décision de Lionel.

## Référence : lanceur normatif 1B-8e (accepté, non modifié depuis)

Entrée de lancement explicite (`prepare_normative_launch`/`launch_normative_campaign` dans `scripts/level1b_campaign/launch.py`, plus une CLI mince `scripts/run_level1b_campaign.py`) qui vérifie toutes les préconditions normatives de la campagne puis appelle `run_campaign` (lot 1B-8d, inchangé) exactement comme celui-ci est déjà défini. Aucune logique scientifique nouvelle ; aucune reconstruction du plan (`run_campaign` construit déjà `build_campaign_plan(manifest)` exactement une fois).

## Préconditions et TOCTOU

`repository_commit` n'est jamais un paramètre libre : il est dérivé exclusivement via `git rev-parse HEAD` depuis `repo_root` (40 hex minuscules exigés, jamais un SHA court). La branche attendue n'est jamais une constante codée dans `launch.py` : elle est comparée à `manifest.branch` (déjà `"research/level1-correlators"` dans le manifeste normatif actuel). Le manifeste est chargé exclusivement via `load_manifest()` sans argument — aucun `manifest_path` n'est jamais exposé. `check_repository_cleanliness` (campaign.py, inchangée sauf export public de sa liste de chemins) est réutilisée telle quelle, jamais réimplémentée.

`launch_normative_campaign` effectue une SECONDE vérification (HEAD, branche, propreté) immédiatement avant `run_campaign`, après le premier passage complet de `prepare_normative_launch` — mitigation explicite et documentée honnêtement comme non atomique (aucun verrou Git introduit) : une fenêtre résiduelle subsiste entre cette seconde vérification et le premier appel interne de `run_campaign`. Le `repository_commit` transmis à `run_campaign` est exactement celui de cette seconde vérification.

Toute erreur de précondition lève `NormativeLaunchError` (avec `dirty_paths` peuplé uniquement pour une erreur de propreté) **avant** tout appel à `run_campaign` — aucun `run.json` de cas n'est jamais créé pour ce type d'échec. Ceci inclut les erreurs des primitives de précondition elles-mêmes : `load_manifest()` (via le helper privé `_load_normative_manifest`) et `check_repository_cleanliness()` (via `_check_repository_cleanliness_or_fail`, aux deux points d'appel — premier et second contrôle TOCTOU) normalisent toute exception sous-jacente en `NormativeLaunchError` (`raise ... from exc`, cause préservée dans `__cause__`), avec `dirty_paths=()` quand aucune liste fiable de chemins sales n'a pu être obtenue. `_resolve_head_sha`/`_current_branch` faisaient déjà cette normalisation depuis la livraison initiale. Cette normalisation reste strictement bornée aux appels de précondition : `launch_normative_campaign` n'enveloppe jamais `run_campaign` dans un `except Exception` — une exception levée par `run_campaign` reste son propre comportement, jamais requalifiée en échec de précondition.

## Périmètre autorisé (fichiers)

- `docs/governance/current-task.md` (ce fichier).
- `scripts/level1b_campaign/launch.py` (nouveau).
- `tests/scripts/level1b_campaign/test_launch.py` (nouveau).
- `scripts/run_level1b_campaign.py` (nouveau, CLI mince).
- `scripts/level1b_campaign/campaign.py` : adaptation minimale unique (export public de `NORMATIVE_REPOSITORY_PATHS`, remplaçant l'ancienne constante privée `_CLEANLINESS_PATHS`, utilisée à la fois par `check_repository_cleanliness` et par `launch.py`).
- Documents Level 1 directement concernés, mise à jour minimale, uniquement si nécessaire.

Ne modifie pas : `runner.py`, `outputs.py`, `planning.py`, le manifeste, `results.py`, `serialization.py`, `assembly.py`, les schémas, `target_selection.py`, `local_observables.py`, `matching.py`, le niveau 0, D020/D021/D022. `run_campaign` lui-même (logique interne) n'est pas modifié.

## Hors périmètre strict

Comparaisons inter-S, `gamma_O`, verdicts de robustesse, `G_occ`, `path_phase_coherence`, agrégation scientifique entre cas, lecture de payload physique pour comparer des cas. Aucun fichier global de campagne (`campaign.json`, résumé/index global) : `CampaignExecutionReport` reste un objet Python en mémoire.

## API de l'orchestrateur (campaign.py, inchangée depuis 1B-8d/correctif)

`CASE_ORCHESTRATION_STATUSES = ("success", "skipped_existing_valid", "resource_guardrail_exceeded", "failed")`. `CaseOrchestrationOutcome(case_id, status, errors)` et `CampaignExecutionReport(case_outcomes, total_required, executed_success_count, reused_success_count, resource_guardrail_exceeded_count, failed_count, global_success)`, tous deux `frozen`/auto-vérifiants. `run_campaign(manifest, *, output_dir, repository_commit) -> CampaignExecutionReport` : construit `build_campaign_plan(manifest)` exactement une fois, aucun tri supplémentaire, aucune liste libre de cas acceptée ; `repository_commit` non vide obligatoire ; `global_success` vrai ssi tous les statuts sont `success`/`skipped_existing_valid`. Un cas `spectrum_options.force is not False` est rejeté avant tout contrôle de garde-fou, comme `failed` ordinaire, jamais `resource_guardrail_exceeded`.

`check_repository_cleanliness(repo_root) -> tuple[bool, tuple[str, ...]]` : fonction séparée, lecture seule (`git status --porcelain`), jamais appelée automatiquement par `run_campaign`, jamais de paramètre de contournement. Utilise désormais `NORMATIVE_REPOSITORY_PATHS` (publique) — seule source de vérité, réutilisée par `launch.py` pour les restrictions de sortie. Une entrée de renommage (`old -> new`) est non propre si le chemin source OU le chemin destination commence par un préfixe normatif. Le répertoire racine `results/` reste hors filtre.

## API du lanceur normatif (launch.py, nouveau)

`NormativeLaunchError(RuntimeError)` avec `dirty_paths: tuple[str, ...]` (vide sauf erreur de propreté). `NormativeLaunchContext(repository_commit, branch, manifest, output_dir)` — `frozen`, invariants auto-vérifiés (SHA 40 hex, branche non vide et != `"HEAD"`, `branch == manifest.branch`, `output_dir` absolu/résolu) ; ne duplique jamais `campaign_id`/`fingerprint` (accessibles via `context.manifest.campaign_id`/`context.manifest.fingerprint`). `prepare_normative_launch(repo_root, *, output_dir) -> NormativeLaunchContext` : vérifie tout une fois, n'appelle jamais `run_campaign`. `launch_normative_campaign(repo_root, *, output_dir) -> CampaignExecutionReport` : `prepare_normative_launch` puis seconde vérification HEAD/branche/propreté, puis `run_campaign`. `output_dir` relatif est résolu par rapport à `repo_root`, jamais au cwd implicite ; rejette `repo_root` lui-même, `repo_root/results`, et tout chemin sous `NORMATIVE_REPOSITORY_PATHS`.

`scripts/run_level1b_campaign.py` : CLI mince (`--output-dir` obligatoire, `--repo-root` optionnel, défaut dérivé de l'emplacement du script) — aucune logique normative hors `launch.py`, aucun paramètre `repository_commit`/`manifest_path`/`branch`/`force`/contournement de propreté. Codes de sortie : `0` succès global, `1` campagne exécutée mais `global_success=False`, `2` `NormativeLaunchError`.

## records.jsonl

Contient exactement les documents v2 retournés par `run_single_case().documents`, un document JSON par ligne, dans l'ordre déterministe déjà fourni par `assemble_execution` — jamais réordonnés. Canonicalisation de chaque ligne : `json.dumps(document, sort_keys=True, separators=(",", ":"), ensure_ascii=True, allow_nan=False)`, terminée par `\n`.

`records_sha256` : SHA-256 sur les octets exacts du fichier `records.jsonl` final (retours à la ligne inclus). `record_count` : nombre exact de lignes/documents.

## run.json

Contient au minimum : `case_id`, `campaign_id`, `manifest_fingerprint`, `repository_commit`, `run_status`, `record_count`, `records_sha256`, `errors`.

Statuts finaux autorisés pour ce sous-lot : `success`, `resource_guardrail_exceeded`, `failed`. `running` n'existe que comme état interne/temporaire, jamais comme marqueur de complétion. `skipped_existing_valid` est un résultat de la logique de reprise, pas un statut stocké dans `run.json` — un `run.json` valide réutilisé n'est jamais réécrit.

## Ordre d'écriture (succès)

1. `records.jsonl` écrit dans un fichier temporaire du même répertoire/filesystem ;
2. flush ;
3. fsync ;
4. `os.replace()` vers `records.jsonl` ;
5. `run.json` temporaire ;
6. flush ;
7. fsync ;
8. `os.replace()` vers `run.json` (marqueur de complétion, toujours écrit en dernier).

Aucun fichier temporaire ou partiel n'est jamais considéré valide.

## Validation d'un run existant (reprise)

Réutilisable seulement si : `run.json` existe et est un JSON valide ; `run_status == "success"` ; `case_id`/`campaign_id`/`manifest_fingerprint`/`repository_commit` correspondent exactement ; `records.jsonl` existe, son SHA-256 correspond à `records_sha256`, son nombre de lignes correspond à `record_count` ; chaque ligne est un JSON objet valide passant `validate_document` et portant le même `campaign_id`/`manifest_fingerprint`/`repository_commit` ; l'assemblage de l'ensemble ne produit ni contradiction ni divergence de métadonnées, et l'ordre assemblé est identique à l'ordre du fichier. Au moindre échec : non réutilisable — jamais réparé silencieusement. Retourne `CaseRunValidation(is_valid, reason)`, jamais une exception, pour une simple invalidité de run réutilisable.

## API finalisée (décisions du rapport de conception validé)

`output_dir` désigne le parent de `runs/` : toute fonction construit elle-même `<output_dir>/runs/<case_id>/` ; l'appelant ne fournit jamais directement un chemin `runs/<case_id>` à une fonction d'écriture. `load_case_records(case_dir)` est la seule exception : elle reçoit le répertoire précis du cas.

`write_case_success(output_dir, result)` dérive `case_id`/`campaign_id`/`manifest_fingerprint`/`repository_commit` exclusivement de `result` (jamais des paramètres libres), et vérifie défensivement avant toute écriture : documents non vides, chacun valide (`validate_document`), métadonnées homogènes, `assemble_execution` réussit sans doublon et égal à `tuple(result.documents)`.

`write_case_failure(output_dir, case_id, *, campaign_id, manifest_fingerprint, repository_commit, run_status, errors)` : `run_status` ∈ {`failed`, `resource_guardrail_exceeded`} exclusivement, `errors: list[str]` non vide. **N'écrit jamais `records.jsonl`** — un échec n'a pas de résultats exploitables. Si un ancien `records.jsonl` existe (run antérieur), il est supprimé avant l'écriture du nouveau `run.json` d'échec, pour qu'il ne soit jamais pris à tort pour les résultats du nouveau run. `record_count=0`, `records_sha256=null`.

## Garde-fous

Seuls `max_dense_dimension = 2000` et `max_sparse_dimension = 200000` restent normatifs ; aucun nouveau seuil mémoire/temps inventé. `resource_guardrail_exceeded`/`failed` appartiennent à `run.json`, jamais à un `ResultRecord` synthétique.

## Règle « un mandat = un commit »

Toute la portée de ce lot est livrée en un seul commit, après validation explicite du rapport de conception. `results/` reste exclu de tout commit.

## Procédure en cas de compactage

Si le contexte conversationnel a été compacté ou paraît incomplet :

1. ne rien coder ;
2. ne rien commiter ;
3. ne rien pousser ;
4. relire ce fichier et les documents normatifs qu'il référence (`docs/decisions/decisions.md`, `docs/levels/level1/specification.md`, `docs/levels/level1/implementation-design.md`, `docs/levels/level1/validation-plan.md`, `docs/governance/collaboration-governance.md`) ;
5. produire une synthèse de reprise explicite (dernier commit accepté, lot actif, périmètre autorisé, ce qui reste à faire) ;
6. ne poursuivre que si toutes les contraintes ci-dessus sont retrouvées sans ambiguïté — sinon, signaler le blocage plutôt que d'inventer.
